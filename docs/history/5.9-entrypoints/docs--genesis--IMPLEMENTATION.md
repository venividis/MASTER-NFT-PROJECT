# Anima Genesis — fourth experience

## Original foundation

The blue source is the approved v1.2 OpticalField, extended by the existing v1.7 lived-field bridge. The approved HTML remains hash-pinned and unmodified. This is the blue interface previously reached by Anima Begins → Original. The archived v1.0 remains separately accessible.

The new bridge extends only the screen projection of `render/living/field.frag`. At formation zero, the original coordinate mapping and complete `radiance()` are retained. The original renderer continues its single animation loop, including history, evolution, lenses, fold, motion, quality and actual WASM fallback. Lived traits and graphics restoration retain their existing wrapper chain.

The added transparent layer gathers particles into rasterized words and values in the same positions as semantic controls. The blue field unfolds into an organic perimeter. There is no second opaque particle renderer concealing it. Formation lasts twelve seconds on a first opening, with 2.4-second same-surface redraws and a 4.2-second return. Complete formation and reduced motion restore usable controls immediately. Focused ready fields survive keyboard/viewport reflow. Input estimates do not restart formation.

The geometry and perimeter vary with deterministic identity axes. Onchain animation metadata can apply the NFT seed/genome as a visual lens without mutating the local ledger. Original live-chain connection remains authoritative when selected. Original and expanded wallet connections remain distinct; local economic rehearsal is not represented as live settlement.

## Atlas

Visible both in the original entry bar and the expanded header/dock. Original links dispatch the actual retained controls, honoring disabled states and original consent/transaction flows. Mint begins with the original connection dialog. Atlas does not click final confirmation controls. The original actions, seventeen extended economic/world/minting capabilities, and seven secondary instrument views are present. The 5.1 additions are documented in EXIT-AND-MINT-SANCTUARY.md. Native history opens in the original interface, keeping its timeline and return-to-present controls.

## Validation scope

The latest added-function validation is in [5.1 validation](releases/5.1-VALIDATION.md). The evidence below records the original 5.0 optical implementation.

- Sixteen targeted semantic and renderer fixtures verify word/value destinations, control readiness, focused-input reflow, reduced motion, transparent WebGL and 2D fallback, interrupted transitions, graphics recovery, original Atlas dispatch, and lived-uniform restoration.
- The inherited financial, memory, operating, archive, identity, field and contract tests were run with the Genesis changes.
- Native Mesa GLES renders the actual blue original and new projection at desktop and portrait ratios. Ten zero-formation comparisons are byte-identical across bound, lived, sovereign, folded and changed-root states; six opening-state renders compile and produce nonempty pixels. See `native-rendering.json`.
- No browser/device testing was performed. Native software GLES and code fixtures do not certify Android Brave, hardware drivers, or every GPU.
- Shanghai compilation covers 103 contract entries. The full runtime was rebuilt, chunked and verified, and exercised through an ephemeral local mint/recovery fixture with 46 deployment entries. It contains all Genesis modules and the preserved original application. No public blockchain transaction was submitted.

The three earlier published Sites are separate and unchanged by this version.
