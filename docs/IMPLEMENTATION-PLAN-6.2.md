# ANIMA 6.2 — complete restored implementation scope

This plan includes every original addition and every item formerly listed as “add later”. The 6.1 checkpoint is `4247350480ffcc1a49e42b8dbb19747b1f6fcb2a`. This ledger records the implemented source scope; validation and external environment requirements are stated explicitly. Source code, passing local journeys, configured public deployments and owner-device validation remain separate evidence.

| Requirement | Implemented behavior and acceptance evidence |
|---|---|
| Continuous object and controls | Preserved the 6.1 fixed field/camera and original blue artwork; runtime integration and parity passed. Physical phone gestures require an actual phone. |
| NFT launch ownership | Retain exact account execution; integrate new optional lifecycle rights without silently switching payer. |
| Participant pages | Keep sale/auction links; add direct pool and persistent launch record pages with read-only terms. |
| Persistent launch record | Enumerable chain registry, append-only relationships and fresh-device recovery. Public registration must explicitly disclose funding linkage. |
| Allocations, vesting and liquidity custody | Atomic optional retained-token and LP allocations into real TimeVault; exact beneficiaries, unlocks and transfer rights. |
| Fee management | Fee-only collection, principal-preserving accounting, reinvestment, exact payout conversion, committed fee policy and optional immutable splitter weights. |
| Encrypted conversations | Preserve legacy history, usable Commons, consent, invitations, recovery and deliberate manager migration. |
| Shielded creator hooks | Preserve the real private composition and output recovery. Public funded proof/broadcaster verification requires configured services and funds. |
| Conserved simulation | Account for selected contracts, creator charges, claims/refunds, liquidity, fee strategies and official protocol rules; no price predictions. |
| Setup and transaction recovery | Exact release runtime checks, real metadata, wallet replacements, cancellations and reorganizations; wire new receipts. |
| Private panel isolation | Preserve isolation and lock clearing through new conversation state and panels. |
| Exact delegated permissions | New editions reject unsafe selector sessions; all delegated actions use exact calldata and the account's asset budget. Migrate workers and sponsorship. |
| Broken-token recovery | Explicit quarantine of unreadable tracked assets, continued disclosure, unrelated operations, restoration and zero-balance retirement. |
| External-agent identity | Verify current agent-token ownership in the selected registry; invalidate stale claims, allow authenticated unbind/rebind. |
| Functional onchain modules | Independently versioned immutable feature archives, pinned dependencies/hashes, real loader resolution and onchain recovery. Capacity shards alone do not satisfy this. |
| Official CCA | Pin actual Uniswap CCA and Liquidity Launcher; create, bid, settle, refund, claim and migrate with exact protocol economics. |
| Official Doppler | Pin actual Airlock/Doppler; creation, discovery, trades, failure recovery and migration. Disclose inherited protocol restrictions. |
| Forward-secure messaging | Actual RFC 9420 MLS, erased message state, fresh key packages, membership commits, encrypted durable state and honest backup/compromise boundaries. |
| Operating shared ownership | Separate fixed-supply governance custody, checkpoint voting, exact call budget, quorum, timelock, bounded operators and funded buyout exit. Preserve frozen-share contracts. |
| Scheduled v4 exits | Compatible real adapter, funded slices and keeper rewards, minimum output, pause/cancel, retry and status. |
| Worlds | Durable atomic state/receipts, idempotent commands, authenticated deltas, reconnect, bounded region pages and crash recovery. |
| Cross-chain operations | Actual pinned OFT transport, authenticated destination, exact asset payout/optional vault action, retry/refund and both-chain receipts. Local transport is not public DVN evidence. |
| Product integration and capability documentation | Mount all complete desks in the existing GUI, generate capability documentation from a shared registry, verify canonical/runtime/site parity. |

## Implementation status

All rows above are implemented in the 6.2 source and mounted in the existing interface, or preserve the already implemented 6.1 capability named in the row. The former “add later” items were not removed or deferred. `docs/RELEASE-6.2.md` explains the resulting user journeys, limits and exact source guides.

The final integration also repaired official share-link preservation, shared-sign setup persistence, official deployment state after a reorganization, unsupported token-decimal formatting in recovery, and generic custody wording that could misstate selected beneficiaries.

Local evidence covers real NFT/account ownership transfers, public and SDK-composed shielded v4 flows, all five economic simulation mechanisms, official protocol settlement/refunds/migration, MLS state and transport, operating governance, durable worlds, two-chain OFT delivery and immutable module recovery. `reports/implementation-6.2/verification.json` records the release checks; `local-deployment.json` records the complete locally minted runtime.

**Remaining external gates:** the selected public release still needs actual deployments, configuration, funds and receipts; shielded services need funded public proof/broadcast verification; public cross-chain routes need configured transport/DVNs; keepers and world/scenario services need running hosts. Physical phone gesture testing requires an actual device. These requirements are not source features silently moved back into a later phase. Existing minted immutable editions are not modified by a source or hosted-app update.

