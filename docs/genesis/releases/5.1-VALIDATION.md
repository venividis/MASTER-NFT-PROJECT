# Anima Genesis 5.1 validation

Validation completed on 2026-09-06 for the remembered-exit and mint-sanctuary source.

- **250 automated tests passed**, zero failures/skips: kingdom, instruments, operating modules, memory, Confluence, Spirit/Begins/Genesis formation fixtures, vested exits and restricted mint wallets.
- **110 Solidity compilation entries** passed with solc 0.8.30, optimizer/viaIR, Shanghai. VestedExitVault deployed code is 8,199 bytes, below EIP-170's limit. Compilation warnings retained from existing hook methods concern possible `view` mutability.
- **Six new EVM exit tests** exercise actual NativeMarket token→native and native→token settlement, multiple segregated plans, commitment indexing, allowance reset, failed minima, false returns, partial consumption, unexpected locks, custody changes, receiver/market callbacks, reentrancy, cancellation, expiry and fixed-recipient recovery.
- The wallet EVM fixture creates and verifies an encrypted wallet; rejects altered signed policy, excessive KDF work, unsupported methods, wrong origin, budget overruns and stale reviews; cancels an in-flight preparation on lock; signs a direct collection mint; discovers and inspects NFTs; explicitly transfers a token; preserves an uncertain broadcast; rebroadcasts exact bytes; and recovers native funds after the mint contract code changes. It restores the encrypted backup and rejects duplicate restoration.
- Local model checks cover atomic note+swap+funding, exact allocations/dust, pending-token conservation on failed sale, no clock-triggered execution, keeper recipient bounds, custody reauthorization, archive consistency, ledger/projection inclusion and integer/fractional form values.
- Genesis semantic fixtures retain word/value formation, private-password exclusion in the controller, reduced motion, focused-field reflow, interruption and fallback behavior. **No browser or mobile-device test was performed in this release.** Existing native GLES visual evidence from 5.0 remains historical; the original optical shader was not modified here.
- Build, JavaScript syntax, referenced HTML assets and embedded imports passed. The recoverable runtime is **673,101 bytes in 30 chunks**, expands to **1,478,572 bytes**, and contains **27 embedded ES modules**.
- Runtime SHA-256: `a0014d3ffaf19350c36f6974175afe1b7d85ee2544345553d1795eaf97653a19`.
- An ephemeral local deployment exercised **52 entries**, minted the original NFT and verified its compact metadata loader and exact runtime recovery. Its new CommitmentIndex includes the funded exit vault. Reports contain local-only addresses.

No user wallet, public chain, hosted keeper or paid execution service was used. Passing these tests is not an independent audit, universal token compatibility, protection against compromised clients/RPCs, or a promise of future execution.
