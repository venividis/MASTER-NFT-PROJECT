# Current implementation

[Current capabilities](CURRENT-CAPABILITIES.md), [the cleanup record](../../CLEANUP.md) and [the contributor gate](../../CONTRIBUTING.md) describe active 6.0 source.

Authored modules in `web/` and shaders in `render/` drive the build. Approved historical HTML remains comparison evidence. The original renderer owns the visible-frame lifecycle, including memory subscribers. Selected minted identity is isolated from local rehearsal. Confirmed account actions refresh a block-consistent snapshot with visible stale-state handling.

First formation, immediate completion and reduced motion remain. Repeat formation is short. Main destinations are defined together; Memory offers storage choices and Advanced contains specialist/developer/authority tools.

Canonical memory has one authoritative root. Former-owner reflections branch without changing its head. Membership/moderator consent follows custody epochs. Sessions roll back custody-changing callbacks. Promotion requires recognized frozen authority contracts.

The compiler records source-qualified artifacts, immutable maps and freshness. The UI builder traverses authored imports, structurally composes HTML and regenerates current runtime. Recovery checks verify its module graph and hashes.

The [previous implementation report](../history/5.9-entrypoints/docs--genesis--IMPLEMENTATION.md) is preserved unchanged. Its test counts and timing describe that version only.
