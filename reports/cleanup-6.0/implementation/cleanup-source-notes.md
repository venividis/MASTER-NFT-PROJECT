# Current source and release cleanup

Implemented source-to-runtime consolidation in ANIMA-NFT-cleanup.

- `build-confluence.mjs` composes the current application directly from `web/index.html`, authored original model/codec/renderer/audio/controller, instrument model modules/controllers, and current Confluence module entry.
- `esbuild` validates and resolves real ESM imports. The five mutually dependent original instrument view controllers retain an explicit shared lexical scope during this migration; engine/codec definitions no longer have imports/exports stripped or collide in a concatenated bundle. `opGet` and `MEM_ZERO` are now explicit exported controller dependencies.
- Current original wallet guards and lockSigningSession are authored in `web/app.js`, rather than applied by substring replacement to archived JavaScript. Original frame subscribers use the existing renderer loop (including paused-motion frames). Original Ascend is in a deliberate Authority experiments disclosure.
- Approved v1.2 HTML remains byte-identical at SHA256 6b1fb23f63cf5aa65283b8be2f1446dddae38622b7ea8c164249c8cc74146bb6. Source optical shader, original shader, Wasm and original embedded reference all compare exactly against the approved fixture before any current build. The archived application's executable JS is never reused as the current application foundation.
- `es-module-lexer` discovers exact relative module closure, including side-effect imports, reexports, literal dynamic imports and cycles. Missing/bare/computed imports and path escapes fail closed. Import-map rewriting uses parser offsets, not regex.
- Normal `dist` includes only that module closure, current styles, local cartridge, pinned private worker, one required CartridgeRegistry ABI, one deliberate original comparison file and current manifests. Full web trees, generated test bundles, mock/developer ABIs and unused variants are not recursively copied.
- A clean staged dist is verified and swapped into place; generator failure retains the previous output. Current source build manifest binds every authored/compiled input consumed by the build, exact module graph and all runtime output hashes. Extra orphan output files and stale source fail verification and default serving.
- Current onchain archive uses the same exact dist. CSS/modules/cartridge/ABI are embedded into recovered HTML; the dedicated private worker remains its existing separately hash-verified resource. Archive generation cleanly replaces old chunks.
- Archive verification recovers gzip, verifies ordered chunks and byte-for-byte runtime.html equality, rejects unlisted stale chunks, compares complete expanded HTML against current source build, validates every recovered module dependency form and syntax-checks all recovered scripts. This checks source freshness and exact recovery, not only archive self-consistency.
- Historical builders require explicit --historical and write named outputs under history, never preview.html/current dist. Default demo serves current verified dist; reusable server test API retained.
- `npm run release:package` produces source/runtime/reference ZIPs with per-file SHA256 manifests and ZIP CRC checks. Current source excludes dormant donor trees but retains required v4 source and fixtures; reference package preserves donor documentation/reports/reference bytes. This does not remove the original uploaded archive.

Focused test coverage: seven build tests pass (closure forms/cycles, unsafe/missing refs, atomic cleanup/failure, input/output/orphan freshness, source/optical preservation, controller startup shared loop, package selection). Migrated old preview preservation assertions to test the actual current source composition instead of stale preview.html. Additional web/instrument tests running. Full build initially correctly refused stale OnchainRenderer Solidity output; fresh compile required before final generation.

Remaining deliberate structure: mutually dependent legacy view controllers still have a shared scope; historical packages retain original builders and donor code explicitly. This is a source/build consolidation, not a complete rewrite of every controller or independent runtime security audit.

## Completed verification and packaging refinements

- 49/49 build, original web, instrument-model and static-server tests passed after migrating tests away from stale preview.html. The seven focused builder tests pass after the final package-classification assertions, including private-resource runtime membership and abandoned staging exclusion.
- Fresh main compilation produced 163 source-qualified Solidity artifacts with Shanghai compatibility; fresh v4 compilation and its authored/imported source fingerprint verification passed.
- Current build produces 66 native modules and 78 runtime files. Gzip archive is 1,123,901 bytes / 49 chunks, expanded size 2,805,607 bytes. Stored runtime-envelope SHA256 1374b40cb8f87ece65035e2e317e4fbeffbadf88a08fc51470a29face9dadb26 refers to the stored runtime envelope, not a claim of deployment.
- Current verify-confluence explicitly rechecks both compiler source closures, build input/output freshness, exact recovered graph and every script. Default serving performs both compiler freshness checks too.
- Source/runtime/reference ZIPs now each receive PACKAGE-MANIFEST.json, PACKAGE-START-HERE.md and a package-specific SOURCE-SHA256.json. Existing root START-HERE.md is preserved. ZIP entries are verified against the embedded SHA256 manifest as well as ZIP CRCs.
- Source package excludes bulky vendor audit PDFs/docs and generated vendor test JavaScript; all required v4 Solidity source/tests and licenses remain. The full vendor evidence bytes remain in the reference package. Root and v4 compiled artifacts are regenerated in the clean source gate.
- Active onchain privacy-worker resource shards belong to the runtime package alongside current Confluence archive. Historical legacy archive chunks remain reference material.
- A repeated package gate found an abandoned release.stage directory with prior generated ZIPs. Added scoped staging/backup exclusion guards and regression assertions; removed only that verified generated staging directory. These files no longer leak recursively into source packages.
- Final source ZIP extraction/install/recompile gate is owned independently by repair_build. It already found and repaired root lock metadata for optional platform-specific Ganache fsevents, then installed all four pinned dependency sets offline. This is an actual clean install, not inferred from previously installed root dependencies.
