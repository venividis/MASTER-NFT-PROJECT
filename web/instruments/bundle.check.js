(()=>{
"use strict";
/** Small, dependency-free Ethereum codec and explicit testnet transaction client.
 * No private keys, RPC endpoints, automatic accounts requests, or signing shortcuts.
 */
const MASK64=(1n<<64n)-1n;
const RC=[0x1n,0x8082n,0x800000000000808an,0x8000000080008000n,0x808bn,0x80000001n,0x8000000080008081n,0x8000000000008009n,0x8an,0x88n,0x80008009n,0x8000000an,0x8000808bn,0x800000000000008bn,0x8000000000008089n,0x8000000000008003n,0x8000000000008002n,0x8000000000000080n,0x800an,0x800000008000000an,0x8000000080008081n,0x8000000000008080n,0x80000001n,0x8000000080008008n];
const ROT=[0,1,62,28,27,36,44,6,55,20,3,10,43,25,39,41,45,15,21,8,18,2,61,56,14];
const rol=(n,r)=>r?((n<<BigInt(r))|(n>>BigInt(64-r)))&MASK64:n;
function bytesOf(hex){if(typeof hex!=='string'||!/^0x(?:[0-9a-fA-F]{2})*$/.test(hex))throw Error('Invalid hex bytes.');return Uint8Array.from(hex.slice(2).match(/../g)||[],x=>parseInt(x,16));}
function hexOf(bytes){return '0x'+Array.from(bytes,x=>x.toString(16).padStart(2,'0')).join('');}
function keccak256(input){const data=typeof input==='string'?new TextEncoder().encode(input):input;if(!(data instanceof Uint8Array))throw Error('Expected UTF-8 text or bytes.');const rate=136,pad=new Uint8Array(Math.ceil((data.length+1)/rate)*rate);pad.set(data);pad[data.length]=1;pad[pad.length-1]|=128;const a=Array(25).fill(0n);for(let off=0;off<pad.length;off+=rate){for(let i=0;i<rate;i++)a[i>>3]^=BigInt(pad[off+i])<<BigInt((i%8)*8);for(const rc of RC){const c=Array.from({length:5},(_,x)=>a[x]^a[x+5]^a[x+10]^a[x+15]^a[x+20]),d=c.map((_,x)=>c[(x+4)%5]^rol(c[(x+1)%5],1));for(let y=0;y<5;y++)for(let x=0;x<5;x++)a[x+y*5]^=d[x];const b=Array(25).fill(0n);for(let y=0;y<5;y++)for(let x=0;x<5;x++)b[y+((2*x+3*y)%5)*5]=rol(a[x+y*5],ROT[x+y*5]);for(let y=0;y<5;y++)for(let x=0;x<5;x++)a[x+y*5]=b[x+y*5]^((~b[(x+1)%5+y*5])&b[(x+2)%5+y*5]);a[0]^=rc;}}
 const out=new Uint8Array(32);for(let i=0;i<32;i++)out[i]=Number((a[i>>3]>>BigInt((i%8)*8))&255n);return hexOf(out);}
function address(value){if(typeof value!=='string'||!/^0x[0-9a-fA-F]{40}$/.test(value)||/^0x0{40}$/i.test(value))throw Error('Enter a nonzero 20-byte address.');return value.toLowerCase();}
function quantity(value){const n=BigInt(value);if(n<0n)throw Error('Negative quantity.');return '0x'+n.toString(16);}
const word=n=>{n=BigInt(n);if(n<0n||n>=1n<<256n)throw Error('ABI integer out of range.');return n.toString(16).padStart(64,'0');};
function fixed(type,value){if(Array.isArray(type)){if(!Array.isArray(value)||type.length!==value.length)throw Error('Tuple shape mismatch.');return type.map((t,i)=>fixed(t,value[i])).join('');}if(type==='address'){if(typeof value!=='string'||!/^0x[0-9a-fA-F]{40}$/.test(value))throw Error('Invalid ABI address.');return value.slice(2).toLowerCase().padStart(64,'0');}if(type==='bool'){if(typeof value!=='boolean')throw Error('Expected boolean.');return word(value?1:0);}if(/^uint(?:[0-9]+)?$/.test(type)){const bits=Number(type.slice(4)||256);if(bits<8||bits>256||bits%8)throw Error('Unsupported integer width.');const n=BigInt(value);if(n<0n||n>=(1n<<BigInt(bits)))throw Error('ABI integer out of range.');return word(n);}if(/^bytes\d+$/.test(type)){const size=Number(type.slice(5)),b=bytesOf(value);if(size<1||size>32||b.length!==size)throw Error('ABI fixed bytes size mismatch.');return value.slice(2).padEnd(64,'0');}throw Error('Unsupported static ABI type: '+type);}
function abiEncode(types,values){if(types.length!==values.length)throw Error('ABI arity mismatch.');const dynamic=t=>t==='bytes'||t==='string';let offset=types.reduce((n,t)=>n+(Array.isArray(t)?t.length:1)*32,0);const head=[],tail=[];types.forEach((t,i)=>{if(dynamic(t)){const b=t==='bytes'?bytesOf(values[i]):new TextEncoder().encode(values[i]);const body=word(b.length)+hexOf(b).slice(2).padEnd(Math.ceil(b.length/32)*64,'0');head.push(word(offset));tail.push(body);offset+=body.length/2;}else head.push(fixed(t,values[i]));});return '0x'+head.join('')+tail.join('');}
function callData(signature,types,values){return keccak256(signature).slice(0,10)+abiEncode(types,values).slice(2);}
function words(data,count){bytesOf(data);if(data.length!==2+count*64)throw Error('Unexpected contract response length.');return Array.from({length:count},(_,i)=>'0x'+data.slice(2+i*64,66+i*64));}
function safeNumber(v){const n=BigInt(v);if(n>BigInt(Number.MAX_SAFE_INTEGER))throw Error('Value exceeds the client safe-integer range.');return Number(n);}
const TEST_CHAINS=new Map([[31337,'LOCAL EVM'],[11155111,'SEPOLIA'],[84532,'BASE SEPOLIA']]);
const INTENT_TYPES=['address','uint256','bytes32','uint256','uint48','uint48','bytes32','bytes32','bytes32','bytes32','bytes32','uint32'];
const INTENT_FIELDS=['target','value','dataHash','nonce','validAfter','validUntil','priorStateRoot','nextStateRoot','nextMemoryRoot','policyHash','evidenceHash','verifierId'];
const intentValues=i=>INTENT_FIELDS.map(k=>i[k]);
const VERIFIED_SIG='executeVerified((address,uint256,bytes32,uint256,uint48,uint48,bytes32,bytes32,bytes32,bytes32,bytes32,uint32),bytes,bytes)';
function encodeVerified(intent,data,proof){if(keccak256(bytesOf(data))!==intent.dataHash.toLowerCase())throw Error('Intent does not bind this calldata.');return callData(VERIFIED_SIG,[INTENT_TYPES,'bytes','bytes'],[intentValues(intent),data,proof]);}
class TestnetClient {
 constructor(provider,onChange=()=>{}){if(!provider?.request)throw Error('No injected Ethereum wallet found. Open in a wallet-enabled browser.');this.provider=provider;this.onChange=onChange;this.revision=0;this.pending=false;this.connected=false;this.handle=()=>{this.revision++;this.connected=false;this.onChange('Wallet account or network changed. Reconnect before continuing.');};provider.on?.('accountsChanged',this.handle);provider.on?.('chainChanged',this.handle);provider.on?.('disconnect',this.handle);}
 async connect(collection,requestAccounts=true){this.collection=address(collection);const rev=this.revision;this.chainId=Number(BigInt(await this.provider.request({method:'eth_chainId'})));if(!TEST_CHAINS.has(this.chainId))throw Error('Mainnet and unknown chains are blocked. Select Sepolia, Base Sepolia, or local chain 31337.');const accounts=await this.provider.request({method:requestAccounts?'eth_requestAccounts':'eth_accounts'});if(!accounts?.length)throw Error('Wallet did not provide an account.');this.owner=address(accounts[0]);const code=await this.provider.request({method:'eth_getCode',params:[this.collection,'latest']});if(!code||code==='0x')throw Error('No collection contract exists at that address on this chain.');if(rev!==this.revision)throw Error('Wallet changed while connecting.');this.connected=true;return this;}
 async assertContext(){if(!this.connected)throw Error('Reconnect the wallet.');const [chain,accounts]=await Promise.all([this.provider.request({method:'eth_chainId'}),this.provider.request({method:'eth_accounts'})]);if(Number(BigInt(chain))!==this.chainId||!accounts?.length||address(accounts[0])!==this.owner){this.handle();throw Error('Wallet context changed. Nothing was sent.');}}
 async read(to,data,block='latest'){return this.provider.request({method:'eth_call',params:[{to:address(to),data},block]});}
 async snapshot(tokenId){await this.assertContext();const id=BigInt(tokenId);if(id<1n)throw Error('Token ID must be positive.');const rev=this.revision;const block=await this.provider.request({method:'eth_blockNumber'});const r=words(await this.read(this.collection,callData('renderSnapshot(uint256)',['uint256'],[id]),block),17);if(rev!==this.revision)throw Error('Wallet changed during the read.');if(BigInt(r[0])!==id)throw Error('Token ID response mismatch.');const meta=await this.provider.request({method:'eth_getBlockByNumber',params:[block,false]});if(!meta?.hash)throw Error('Block identity unavailable.');const bool=BigInt(r[14]);if(bool!==0n&&bool!==1n)throw Error('Invalid authority mode.');this.tokenId=id.toString();this.account=address('0x'+r[15].slice(-40));const s={tokenId:id.toString(),seed:r[1],genome:r[2],root:r[3],memory:r[4],lineage:r[5],audit:r[6],policy:r[7],createdAt:safeNumber(r[8])*1000,updatedAt:safeNumber(r[9])*1000,generation:safeNumber(r[10]),evolutions:safeNumber(r[11]),parentId:BigInt(r[12]).toString(),nonce:safeNumber(r[13]),sovereign:bool===1n,account:this.account,owner:address('0x'+r[16].slice(-40)),children:[],epoch:0,chainId:this.chainId,blockNumber:BigInt(block).toString(),blockHash:meta.hash};this.state=s;return s;}
 async prepare(label,to,data,{note='',onReceipt=null}={}){await this.assertContext();const revision=this.revision,tx={from:this.owner,to:address(to),data,value:'0x0',chainId:quantity(this.chainId)};bytesOf(data);await this.provider.request({method:'eth_call',params:[tx,'latest']});const estimate=BigInt(await this.provider.request({method:'eth_estimateGas',params:[tx]}));if(estimate<=0n||estimate>15000000n)throw Error('Gas estimate outside client safety bounds.');const gas=(estimate*120n+99n)/100n;const gasPrice=await this.provider.request({method:'eth_gasPrice'});return {label,tx:{...tx,gas:quantity(gas)},chainId:this.chainId,revision,note,estimatedGas:estimate.toString(),maxEstimatedFeeWei:(gas*BigInt(gasPrice)).toString(),onReceipt};}
 async send(plan,progress=()=>{},timeoutMs=180000){if(this.pending)throw Error('A transaction is already pending.');await this.assertContext();if(plan.revision!==this.revision||plan.chainId!==this.chainId||plan.tx.from!==this.owner)throw Error('Stale transaction review. Prepare it again.');this.pending=true;try{await this.provider.request({method:'eth_call',params:[plan.tx,'latest']});await this.assertContext();if(plan.revision!==this.revision)throw Error('Wallet changed during simulation. Nothing was sent.');const hash=await this.provider.request({method:'eth_sendTransaction',params:[plan.tx]});if(!/^0x[0-9a-fA-F]{64}$/.test(hash))throw Error('Wallet returned an invalid transaction hash.');progress('SUBMITTED',hash);const started=Date.now();while(Date.now()-started<timeoutMs){await this.assertContext();const r=await this.provider.request({method:'eth_getTransactionReceipt',params:[hash]});if(r){if(BigInt(r.status)!==1n)throw Error('Transaction reverted. Local identity was not advanced. Hash: '+hash);if(!r.blockHash||!r.blockNumber)throw Error('Receipt is not included in a block.');const block=await this.provider.request({method:'eth_getBlockByNumber',params:[r.blockNumber,false]});if(block?.hash?.toLowerCase()!==r.blockHash.toLowerCase())throw Error('Receipt block changed. Refresh chain state; do not assume finality.');progress('INCLUDED',hash);await plan.onReceipt?.(r);return r;}await new Promise(resolve=>setTimeout(resolve,900));}throw Error('Still pending. Do not send again blindly. Transaction hash: '+hash);}finally{this.pending=false;}}
 async evolution(salt,verifierId=1){const s=await this.snapshot(this.tokenId);const newGenome=keccak256(bytesOf(abiEncode(['bytes32','bytes32'],[s.genome,salt]))),newMemory=keccak256(bytesOf(abiEncode(['bytes32','bytes32'],[s.memory,newGenome]))),evidence=keccak256(bytesOf(abiEncode(['bytes32','bytes32'],[s.root,salt])));if(!s.sovereign)return this.prepare('EVOLVE',s.account,callData('evolveBound(uint256,bytes32,bytes32,bytes32)',['uint256','bytes32','bytes32','bytes32'],[s.nonce,newGenome,newMemory,evidence]),{note:'Requires the v1.2 account implementation. Genome and roots advance atomically; ETH value is zero.'});const data=callData('commitEvolution(uint256,bytes32,bytes32,bytes32)',['uint256','bytes32','bytes32','bytes32'],[this.tokenId,newGenome,newMemory,evidence]);const nextStateRoot=words(await this.read(this.collection,callData('deriveEvolutionStateRoot(uint256,bytes32,bytes32,bytes32)',['uint256','bytes32','bytes32','bytes32'],[this.tokenId,newGenome,newMemory,evidence])),1)[0];const latest=await this.provider.request({method:'eth_getBlockByNumber',params:['latest',false]}),now=safeNumber(latest.timestamp);const intent={target:this.collection,value:'0',dataHash:keccak256(bytesOf(data)),nonce:String(s.nonce),validAfter:now,validUntil:now+1800,priorStateRoot:s.root,nextStateRoot,nextMemoryRoot:newMemory,policyHash:s.policy,evidenceHash:evidence,verifierId};const statement=words(await this.read(s.account,callData('intentStatement((address,uint256,bytes32,uint256,uint48,uint48,bytes32,bytes32,bytes32,bytes32,bytes32,uint32))',[INTENT_TYPES],[intentValues(intent)])),1)[0];return {schema:'idfbi/sovereign-evolution/1',chainId:this.chainId,collection:this.collection,account:s.account,tokenId:this.tokenId,intent,data,statement,proofRequired:true};}
 async verifiedEvolution(request,proof){await this.assertContext();if(request.chainId!==this.chainId||address(request.account)!==this.account||address(request.collection)!==this.collection)throw Error('Proof request belongs to a different deployment.');if(typeof proof!=='string'||bytesOf(proof).length===0||proof.length>262146)throw Error('A nonempty verifier proof is required (maximum 128 KiB).');return this.prepare('PROOF-AUTHORIZED EVOLUTION',this.account,encodeVerified(request.intent,request.data,proof),{note:'eth_call must accept this proof against the deployed verifier. This client does not generate ZK or TEE proofs.'});}
 async spawn(salt){await this.snapshot(this.tokenId);if(this.state.sovereign)throw Error('Sovereign descendants require a proof-authorized intent. Use the account SDK; the preview does not manufacture a proof.');const inner=callData('spawnFromAccount(uint256,address,bytes32)',['uint256','address','bytes32'],[this.tokenId,this.owner,salt]);return this.prepare('SPAWN DESCENDANT',this.account,callData('execute(address,uint256,bytes)',['address','uint256','bytes'],[this.collection,0,inner]),{note:'A real descendant NFT is minted to the connected wallet. No endowment is sent.'});}
 async seal(commitment){await this.snapshot(this.tokenId);if(this.state.sovereign)throw Error('Sovereign metadata writes require a valid proof. No owner bypass is available.');return this.prepare('SEAL MEMORY DIGEST',this.collection,callData('setMetadata(uint256,string,bytes)',['uint256','string','bytes'],[this.tokenId,'memory.commitment',commitment]),{note:'Stores a digest under memory.commitment; it does not change the account memory root or preserve the thought.'});}
 async ascend(policy){await this.snapshot(this.tokenId);return this.prepare('IRREVERSIBLE ASCENSION',this.collection,callData('ascend(uint256,bytes32,uint96,uint32)',['uint256','bytes32','uint96','uint32'],[this.tokenId,policy,0,0]),{note:'PERMANENT: direct owner execution and transfer stop. ETH action limit becomes zero. Without working proof infrastructure this NFT may become unusable. Testnet only.'});}
 close(){this.provider.removeListener?.('accountsChanged',this.handle);this.provider.removeListener?.('chainChanged',this.handle);this.provider.removeListener?.('disconnect',this.handle);this.connected=false;this.revision++;}
}


/** Deterministic, unsigned local rehearsal. Not an EVM emulator, price feed or wallet. */
const E18=10n**18n;
const HOOK_FLAGS=(1<<13)|(1<<9)|(1<<7)|(1<<6);
const MAX112=(1n<<112n)-1n;
function wei(text){if(!/^\d+(\.\d{1,18})?$/.test(String(text)))throw Error('Use a positive decimal with at most 18 places.');const [a,b='']=String(text).split('.');const n=BigInt(a)*E18+BigInt(b.padEnd(18,'0'));if(n>MAX112)throw Error('Amount is outside the supported range.');return n;}
function format(n,places=4){n=BigInt(n);const negative=n<0n;if(negative)n=-n;const whole=n/E18,tail=(n%E18).toString().padStart(18,'0').slice(0,places).replace(/0+$/,'');return(negative?'-':'')+whole.toLocaleString('en-US')+(tail?'.'+tail:'');}
function stable(v){if(Array.isArray(v))return '['+v.map(stable).join(',')+']';if(v&&typeof v==='object')return '{'+Object.keys(v).sort().map(k=>JSON.stringify(k)+':'+stable(v[k])).join(',')+'}';return JSON.stringify(v);}
function fingerprint(v){return keccak256(stable(v));}
function feeAt(born,now){if(now<=born)return 10000;if(now>=born+86400)return 3000;return 10000-Math.floor(7000*(now-born)/86400);}
function releasable(lock,now){if(now<lock.cliff)return 0n;const total=BigInt(lock.amount),vested=now>=lock.end?total:lock.linear?total*BigInt(now-lock.start)/BigInt(lock.end-lock.start):0n;return vested-BigInt(lock.released);}
function amount(n){n=BigInt(n);if(n<=0n||n>MAX112)throw Error('Amount must be positive and within the supported range.');return n;}
function text(s,max){s=String(s).trim();if(!s||new TextEncoder().encode(s).length>max)throw Error('Text must contain 1–'+max+' UTF-8 bytes.');return s;}
function take(balances,asset,n){n=amount(n);const b=BigInt(balances[asset]||0);if(b<n)throw Error('Insufficient rehearsal '+asset+' balance.');balances[asset]=(b-n).toString();}
function give(balances,asset,n){balances[asset]=(BigInt(balances[asset]||0)+BigInt(n)).toString();}
class KingdomModel {
 constructor(now=Math.floor(Date.now()/1000)){
  this.s={schema:'idfbi/kingdom-rehearsal/1.4',now,actor:'you',selected:1,next:4,seq:0,head:'0x'+'00'.repeat(32),wallets:{you:wei('25').toString(),guest:wei('12').toString(),archivist:wei('2').toString(),weaver:wei('2').toString()},identities:[
   {id:1,name:'The Phoenix Sanctum',owner:'you',epoch:1,nonce:0,seed:17,color:0,balances:{ETH:wei('8').toString(),AUR:wei('12000').toString()},lease:null},
   {id:204,name:'The Lumen Archive',owner:'archivist',epoch:1,nonce:0,seed:71,color:1,balances:{ETH:wei('1.25').toString(),AUR:wei('3600').toString()},lease:null},
   {id:317,name:'The Vesper Atelier',owner:'weaver',epoch:1,nonce:0,seed:123,color:2,balances:{ETH:wei('0.6').toString(),AUR:wei('1400').toString()},lease:null}],
   pools:[{asset:'AUR',name:'Aurora / rehearsal market',native:wei('100').toString(),tokens:wei('1000000').toString(),born:now-172800,creator:1}],sales:[],locks:[
    {id:1,identity:1,asset:'AUR',amount:wei('25000').toString(),released:'0',start:now-86400,cliff:now+29*86400,end:now+179*86400,linear:true},
    {id:2,identity:204,asset:'AUR',amount:wei('50000').toString(),released:'0',start:now,cliff:now+90*86400,end:now+90*86400,linear:false}],listings:[],
   rooms:[{id:1,name:'World',topic:'One shared sky. Public text; your own filters.',identity:0,gated:false,slow:3,color:'#a6e6e8',members:{},bans:[],moderators:{}},
    {id:2,name:'Sanctum Council',topic:'The room follows the artifact. The people do not.',identity:1,gated:true,slow:3,color:'#cfb9f6',members:{you:{accepted:true,epoch:1}},bans:[],moderators:{}}],messages:[],events:[],views:{you:{follow:['archivist'],block:[]},guest:{follow:[],block:[]}},settings:{motion:true,sound:false,accent:'#b5eeea'}};
  this._event('AWAKEN',1,{note:'Three fictional estates. All balances and people in this rehearsal are examples.'});
  this._post('archivist',1,0,'An artifact can carry its history without pretending the next owner wrote it.',0,0);
  this._post('weaver',1,0,'Open an estate. Move its time lens. The locked allocation becomes visible before it becomes spendable.',0,0);
  this._list(204,'archivist',wei('6.8'),now+7*86400);
  this._list(317,'weaver',wei('3.4'),now+7*86400);
 }
 identity(id=this.s.selected){const a=this.s.identities.find(x=>x.id===Number(id));if(!a)throw Error('Unknown artifact.');return a;}
 room(id){const r=this.s.rooms.find(x=>x.id===Number(id));if(!r)throw Error('Unknown room.');return r;}
 _owner(id){const a=this.identity(id);if(a.owner!==this.s.actor)throw Error('Only the current owner can spend or change this artifact.');return a;}
 _tx(fn){const before=structuredClone(this.s);try{return fn();}catch(e){this.s=before;throw e;}}
 _event(kind,id,data={},actor=this.s.actor){const a=id?this.identity(id):null;const e={seq:++this.s.seq,kind,identity:id,actor,custodian:a?.owner||actor,epoch:a?.epoch||0,time:this.s.now,data,provenance:'UNSIGNED LOCAL REHEARSAL',prior:this.s.head};e.digest=fingerprint(e);this.s.head=e.digest;this.s.events.push(e);return e;}
 setActor(actor){if(!['you','guest'].includes(actor))throw Error('Choose a rehearsal persona.');this.s.actor=actor;}
 select(id){this.identity(id);this.s.selected=Number(id);}
 advance(seconds){if(!Number.isSafeInteger(seconds)||seconds<=0||seconds>3650*86400)throw Error('Invalid rehearsal time step.');this.s.now+=seconds;this._event('TIME',0,{seconds});}
 createLaunch({name,symbol,supply=wei('1000000'),soft=wei('1'),hard=wei('20'),hours=24,founderBps=1000,liquidityBps=8000}){
  return this._tx(()=>{const a=this._owner(this.s.selected);name=text(name,48);symbol=text(symbol.toUpperCase(),12);if(!/^[A-Z][A-Z0-9]{1,11}$/.test(symbol)||symbol==='ETH'||this.s.pools.some(x=>x.asset===symbol)||this.s.sales.some(x=>x.symbol===symbol))throw Error('Choose a unique 2–12-character ticker.');
   supply=amount(supply);soft=amount(soft);hard=amount(hard);if(supply<E18||soft>hard||hours<1||hours>720||!Number.isInteger(hours)||founderBps<0||founderBps>2000||!Number.isInteger(founderBps)||liquidityBps<5000||liquidityBps>10000||!Number.isInteger(liquidityBps))throw Error('Invalid immutable launch terms.');
   const founder=supply*BigInt(founderBps)/10000n,pub=(supply-founder)*10000n/BigInt(10000+liquidityBps),lp=supply-founder-pub,id=this.s.sales.length+1;
   const sale={id,name,symbol,creator:a.id,supply:supply.toString(),public:pub.toString(),founder:founder.toString(),lp:lp.toString(),soft:soft.toString(),hard:hard.toString(),liquidityBps,opens:this.s.now,closes:this.s.now+hours*3600,raised:'0',contributions:{},claimed:[],status:'open',room:this.s.rooms.length+1};
   this.s.sales.push(sale);this.s.rooms.push({id:sale.room,name:symbol+' commons',topic:name+' · launch terms and conversation.',identity:a.id,gated:false,slow:3,color:'#e8c797',members:{},bans:[],moderators:{}});a.nonce++;this._event('LAUNCHED',a.id,{sale:id,symbol,room:sale.room});return sale;
  });
 }
 sale(id){const sale=this.s.sales.find(x=>x.id===Number(id));if(!sale)throw Error('Unknown launch.');return sale;}
 contribute(id,n){return this._tx(()=>{const a=this._owner(this.s.selected),s=this.sale(id);n=amount(n);if(s.status!=='open'||this.s.now>=s.closes)throw Error('Contribution window has closed.');if(BigInt(s.raised)+n>BigInt(s.hard))throw Error('Contribution exceeds the hard cap.');if((BigInt(s.contributions[a.id]||0)+n)*BigInt(s.public)/BigInt(s.hard)===0n)throw Error('Contribution would round to zero.');take(a.balances,'ETH',n);s.raised=(BigInt(s.raised)+n).toString();s.contributions[a.id]=(BigInt(s.contributions[a.id]||0)+n).toString();a.nonce++;this._event('CONTRIBUTED',a.id,{sale:s.id,symbol:s.symbol,amount:n.toString()});});}
 withdrawContribution(id,n){return this._tx(()=>{const a=this._owner(this.s.selected),s=this.sale(id);n=amount(n);const paid=BigInt(s.contributions[a.id]||0);if(s.status!=='open'||this.s.now>=s.closes||paid<n)throw Error('Contribution cannot be withdrawn.');if(paid-n>0n&&(paid-n)*BigInt(s.public)/BigInt(s.hard)===0n)throw Error('Remaining contribution would round to zero.');s.contributions[a.id]=(paid-n).toString();s.raised=(BigInt(s.raised)-n).toString();give(a.balances,'ETH',n);a.nonce++;this._event('WITHDREW',a.id,{sale:s.id,amount:n.toString()});});}
 settle(id){return this._tx(()=>{const s=this.sale(id);if(s.status!=='open'||this.s.now<s.closes)throw Error('Settlement starts after the contribution window.');const raised=BigInt(s.raised);s.status=raised>=BigInt(s.soft)?'settled':'refund';if(s.status==='settled'){
    const lp=raised*BigInt(s.liquidityBps)/10000n;this.s.pools.push({asset:s.symbol,name:s.name,native:lp.toString(),tokens:s.lp,born:this.s.now,creator:s.creator});
    if(BigInt(s.founder)>0n)this._lock(s.creator,s.symbol,BigInt(s.founder),180,true,30);
    if(raised>lp)this._lock(s.creator,'ETH',raised-lp,180,false,180);
   }this._event(s.status==='settled'?'SETTLED':'REFUNDABLE',s.creator,{sale:s.id,symbol:s.symbol,fee:10000});return s;});}
 claim(id){return this._tx(()=>{const a=this._owner(this.s.selected),s=this.sale(id),paid=BigInt(s.contributions[a.id]||0);if(!['settled','refund'].includes(s.status)||paid===0n||s.claimed.includes(a.id))throw Error('No allocation or refund can be claimed.');const n=s.status==='refund'?paid:paid*BigInt(s.public)/BigInt(s.raised);if(n===0n)throw Error('Allocation rounded to zero.');s.claimed.push(a.id);give(a.balances,s.status==='refund'?'ETH':s.symbol,n);a.nonce++;this._event('CLAIMED',a.id,{sale:s.id,symbol:s.symbol,amount:n.toString()});return n;});}
 _quoteHop(p,buy,n){const rin=BigInt(buy?p.native:p.tokens),rout=BigInt(buy?p.tokens:p.native),fee=feeAt(p.born,this.s.now),effective=n*BigInt(1000000-fee);return {out:effective*rout/(rin*1000000n+effective),fee};}
 quote(input,output,n){n=amount(n);if(input===output)throw Error('Choose two different assets.');const find=x=>{const p=this.s.pools.find(p=>p.asset===x);if(!p)throw Error('This token has not settled into a market.');return p;};let hops=[];if(input!=='ETH'){const p=find(input),q=this._quoteHop(p,false,n);hops.push({asset:input,buy:false,input:n.toString(),...q,out:q.out.toString()});n=q.out;}if(output!=='ETH'){const p=find(output),q=this._quoteHop(p,true,n);hops.push({asset:output,buy:true,input:n.toString(),...q,out:q.out.toString()});n=q.out;}if(n<=0n)throw Error('Output rounds to zero.');return {out:n,hops,model:'illustrative full-range reserve approximation; not v4 tick simulation'};}
 swap(input,output,n,minOut,lockDays=0,deadline=this.s.now+1200){return this._tx(()=>{const a=this._owner(this.s.selected);n=amount(n);minOut=amount(minOut);if(this.s.now>deadline)throw Error('Quote deadline expired.');const q=this.quote(input,output,n);if(q.out<minOut)throw Error('Minimum received is not met.');if(lockDays!==0&&(!Number.isInteger(lockDays)||lockDays<1||lockDays>3650))throw Error('Invalid lock duration.');take(a.balances,input,n);
   for(const h of q.hops){const p=this.s.pools.find(p=>p.asset===h.asset);p.native=(BigInt(p.native)+(h.buy?BigInt(h.input):-BigInt(h.out))).toString();p.tokens=(BigInt(p.tokens)+(h.buy?-BigInt(h.out):BigInt(h.input))).toString();}
   let lock=null;if(lockDays)lock=this._lock(a.id,output,q.out,lockDays,false,lockDays);else give(a.balances,output,q.out);a.nonce++;this._event('SWAPPED',a.id,{input,output,amount:n.toString(),received:q.out.toString(),lock:lock?.id||0});return{...q,lock};});}
 _lock(identity,asset,n,days,linear,cliffDays=0){const l={id:this.s.locks.length+1,identity,asset,amount:n.toString(),released:'0',start:this.s.now,cliff:this.s.now+(linear?cliffDays:days)*86400,end:this.s.now+days*86400,linear};this.s.locks.push(l);return l;}
 deposit(asset,n,days,linear=false){return this._tx(()=>{const a=this._owner(this.s.selected);n=amount(n);if(!Number.isInteger(days)||days<1||days>3650)throw Error('Choose 1–3,650 days.');take(a.balances,asset,n);const l=this._lock(a.id,asset,n,days,linear);a.nonce++;this._event('LOCKED',a.id,{lock:l.id,asset,amount:n.toString(),end:l.end});return l;});}
 release(id){return this._tx(()=>{const l=this.s.locks.find(x=>x.id===Number(id));if(!l)throw Error('Unknown lock.');const n=releasable(l,this.s.now);if(n===0n)throw Error('Nothing is unlocked at the current rehearsal time.');l.released=(BigInt(l.released)+n).toString();give(this.identity(l.identity).balances,l.asset,n);this._event('RELEASED',l.identity,{lock:l.id,asset:l.asset,amount:n.toString()});return n;});}
 _inventorySnapshot(id){const a=this.identity(id);return {id:a.id,epoch:a.epoch,nonce:a.nonce,seed:a.seed,lease:a.lease,balances:structuredClone(a.balances),locks:this.s.locks.filter(l=>l.identity===id).map(l=>structuredClone(l))};}
 _list(id,seller,price,expires){const a=this.identity(id);if(a.owner!==seller||a.lease&&a.lease.expires>=this.s.now)throw Error('Only an unleased, owner-controlled estate can be listed.');a.owner='escrow';a.epoch++;const inventory=this._inventorySnapshot(id),l={id:this.s.listings.length+1,identity:id,seller,price:amount(price).toString(),expires,status:'listed',inventory};l.manifest=fingerprint({domain:'LOCAL_ESTATE_COVENANT_1_4',...l});this.s.listings.push(l);this._event('LISTED',id,{listing:l.id,manifest:l.manifest},seller);return l;}
 listEstate(id,price,days=7){return this._tx(()=>{this._owner(id);if(!Number.isInteger(days)||days<1||days>30)throw Error('List for 1–30 days.');return this._list(id,this.s.actor,price,this.s.now+days*86400);});}
 checkListing(id){const l=this.s.listings.find(x=>x.id===Number(id));if(!l||l.status!=='listed'||this.s.now>l.expires)throw Error('Listing is unavailable or expired.');const a=this.identity(l.identity);if(a.owner!=='escrow'||fingerprint(this._inventorySnapshot(a.id))!==fingerprint(l.inventory))throw Error('Covenant rejected: contents or authority changed.');return l;}
 buy(id,manifest){return this._tx(()=>{const l=this.checkListing(id);if(l.seller===this.s.actor)throw Error('You cannot buy your own listing.');if(l.manifest!==manifest)throw Error('The reviewed manifest does not match.');const cash=BigInt(this.s.wallets[this.s.actor]||0),price=BigInt(l.price);if(cash<price)throw Error('Insufficient rehearsal wallet balance.');this.s.wallets[this.s.actor]=(cash-price).toString();this.s.wallets[l.seller]=(BigInt(this.s.wallets[l.seller]||0)+price).toString();l.status='sold';const a=this.identity(l.identity);a.owner=this.s.actor;a.epoch++;a.lease=null;this.s.selected=a.id;this._event('ACQUIRED',a.id,{listing:l.id,previousCustodian:l.seller,manifest:l.manifest});return a;});}
 cancel(id){return this._tx(()=>{const l=this.s.listings.find(x=>x.id===Number(id));if(!l||l.status!=='listed'||l.seller!==this.s.actor&&this.s.now<=l.expires)throw Error('Only the seller can cancel a live listing.');l.status='cancelled';const a=this.identity(l.identity);a.owner=l.seller;a.epoch++;this._event('RETURNED',a.id,{listing:l.id},l.seller);});}
 grantPresence(id,user,days){return this._tx(()=>{const a=this._owner(id);if(!['you','guest'].includes(user)||user===a.owner||!Number.isInteger(days)||days<1||days>30)throw Error('Select a different rehearsal user and 1–30 days.');a.lease={user,expires:this.s.now+days*86400};this._event('PRESENCE',a.id,{user,expires:a.lease.expires});});}
 userOf(id,now=this.s.now){const a=this.identity(id);return a.lease&&a.lease.expires>=now?a.lease.user:null;}
 _admin(r,actor=this.s.actor){return !!r.identity&&this.identity(r.identity).owner===actor;}
 _mod(r,actor=this.s.actor){return this._admin(r,actor)||(r.moderators[actor]===this.identity(r.identity||1).epoch&&r.members[actor]?.accepted);}
 _canPost(r,actor){if(r.bans.includes(actor))return false;return !r.gated||this._admin(r,actor)||r.members[actor]?.accepted;}
 _post(actor,room,identity,body,reply=0,receipt=0){const r=this.room(room);body=text(body,1024);if(!this._canPost(r,actor))throw Error('Accept a room invitation before posting; removed members cannot post.');if(reply&&!this.s.messages.some(m=>m.id===reply&&m.room===room))throw Error('Reply belongs to a different room.');if(receipt&&!this.s.events.some(e=>e.seq===receipt))throw Error('Unknown activity receipt.');const last=this.s.messages.findLast(m=>m.actor===actor&&m.room===room);if(last&&this.s.now<last.time+r.slow)throw Error('Room slow mode: wait '+r.slow+' seconds of rehearsal time.');const a=identity?this.identity(identity):null;
  const m={id:this.s.messages.length+1,actor,identity,custodian:a?.owner||actor,epoch:a?.epoch||0,room,time:this.s.now,body,reply,receipt,reactions:{},hidden:false};this.s.messages.push(m);return m;}
 post(room,body,reply=0,receipt=0){return this._tx(()=>{const a=this.identity(),identity=a.owner===this.s.actor?a.id:0;const m=this._post(this.s.actor,Number(room),identity,body,Number(reply),Number(receipt));this._event('SPOKE',identity,{message:m.id,room:Number(room)});return m;});}
 createRoom(name,gated=false){return this._tx(()=>{const a=this._owner(this.s.selected),r={id:this.s.rooms.length+1,name:text(name,48),topic:'A room held by '+a.name,identity:a.id,gated,slow:3,color:'#b3deda',members:{[this.s.actor]:{accepted:true,epoch:a.epoch}},bans:[],moderators:{}};this.s.rooms.push(r);this._event('ROOM',a.id,{room:r.id});return r;});}
 configureRoom(id,{name,topic,gated,slow,color}){return this._tx(()=>{const r=this.room(id);if(!this._admin(r))throw Error('Only the artifact owner can configure this room.');if(!Number.isInteger(slow)||slow<0||slow>3600||!/^#[0-9a-f]{6}$/i.test(color))throw Error('Invalid room controls.');r.name=text(name,48);r.topic=text(topic,256);r.gated=!!gated;r.slow=slow;r.color=color;this._event('ROOM_RULES',r.identity,{room:r.id});});}
 invite(id,user){const r=this.room(id);if(!this._mod(r)||!['you','guest','archivist','weaver'].includes(user)||this._admin(r,user))throw Error('Not permitted to invite this member.');r.members[user]={accepted:false,epoch:this.identity(r.identity).epoch};r.bans=r.bans.filter(x=>x!==user);this._event('INVITED',r.identity,{room:r.id,user});}
 accept(id){const r=this.room(id),m=r.members[this.s.actor];if(!m||r.bans.includes(this.s.actor)||m.epoch!==this.identity(r.identity).epoch)throw Error('Invitation expired, revoked or unavailable.');m.accepted=true;this._event('JOINED',0,{room:r.id});}
 remove(id,user){const r=this.room(id);if(!this._mod(r)||this._admin(r,user)||!this._admin(r)&&r.moderators[user])throw Error('Not permitted to remove this member.');delete r.members[user];delete r.moderators[user];if(!r.bans.includes(user))r.bans.push(user);this._event('REMOVED',r.identity,{room:r.id,user});}
 view(user,kind){if(!['follow','block'].includes(kind)||user===this.s.actor)throw Error('Invalid personal filter.');const v=this.s.views[this.s.actor]||={follow:[],block:[]};v[kind]=v[kind].includes(user)?v[kind].filter(x=>x!==user):[...v[kind],user];}
 react(id,emoji){const m=this.s.messages.find(m=>m.id===Number(id));if(!m||!['✧','♡','◎','↗'].includes(emoji)||!this._canPost(this.room(m.room),this.s.actor))throw Error('Reaction is not available.');const users=m.reactions[emoji]||[];m.reactions[emoji]=users.includes(this.s.actor)?users.filter(x=>x!==this.s.actor):[...users,this.s.actor];}
 export(){return {schema:this.s.schema,provenance:'unsigned local rehearsal; editable and not owner-authenticated',state:structuredClone(this.s),checksum:fingerprint(this.s)};}
 static restore(envelope){if(!envelope||envelope.schema!=='idfbi/kingdom-rehearsal/1.4'||envelope.provenance!=='unsigned local rehearsal; editable and not owner-authenticated'||fingerprint(envelope.state)!==envelope.checksum)throw Error('Rehearsal integrity check failed. Saved bytes were not overwritten.');const s=envelope.state;validateImportedState(s);if(!Number.isSafeInteger(s.now)||!Array.isArray(s.identities)||s.identities.length>64||s.identities.length===0||!Array.isArray(s.messages)||s.messages.length>4096||!Array.isArray(s.events)||s.events.length>10000||!['you','guest'].includes(s.actor))throw Error('Unsupported rehearsal data.');for(const a of s.identities)for(const n of Object.values(a.balances))if(!/^\d+$/.test(n)||BigInt(n)<0n)throw Error('Invalid stored balance.');let head='0x'+'00'.repeat(32);for(const e of s.events){const {digest,...body}=e;if(body.prior!==head||fingerprint(body)!==digest)throw Error('Receipt chain is inconsistent.');head=digest;}if(head!==s.head)throw Error('Receipt head mismatch.');const m=Object.create(KingdomModel.prototype);m.s=structuredClone(s);return m;}
}

// Imports are unsigned user data. Validate every structural value interpolated by the UI;
// an internally consistent checksum is not a schema or a security boundary by itself.
function validateImportedState(s){
 const fail=()=>{throw Error('Unsupported or unsafe rehearsal schema.');};
 const integer=(v,min=0,max=Number.MAX_SAFE_INTEGER)=>{if(!Number.isSafeInteger(v)||v<min||v>max)fail();};
 const str=(v,max=1024)=>{if(typeof v!=='string'||new TextEncoder().encode(v).length>max)fail();};
 const qty=v=>{if(typeof v!=='string'||!/^\d{1,78}$/.test(v)||BigInt(v)>=(1n<<256n))fail();};
 const symbol=v=>{if(typeof v!=='string'||!/^[A-Z][A-Z0-9]{1,11}$/.test(v))fail();};
 const array=(v,max)=>{if(!Array.isArray(v)||v.length>max)fail();};
 const person=v=>{if(!['you','guest','archivist','weaver','escrow'].includes(v))fail();};
 if(!s||typeof s!=='object')fail();integer(s.now,1,8640000000000);integer(s.seq);integer(s.selected,1);person(s.actor);
 array(s.identities,64);array(s.locks,4096);array(s.rooms,512);array(s.sales,512);array(s.pools,512);array(s.listings,1024);array(s.messages,4096);array(s.events,10000);
 const ids=new Set();for(const a of s.identities){integer(a.id,1);if(ids.has(a.id))fail();ids.add(a.id);str(a.name,128);integer(a.seed,0,0xffffffff);integer(a.color,0,2);integer(a.epoch);integer(a.nonce);person(a.owner);if(!a.balances||Object.keys(a.balances).length>512)fail();for(const [k,v]of Object.entries(a.balances)){symbol(k);qty(v);}if(a.lease){person(a.lease.user);integer(a.lease.expires,1);}}
 if(!ids.has(s.selected))fail();for(const [k,v]of Object.entries(s.wallets||{})){person(k);qty(v);}
 for(const l of s.locks){integer(l.id,1);if(!ids.has(l.identity))fail();symbol(l.asset);qty(l.amount);qty(l.released);integer(l.start,1);integer(l.cliff,l.start);integer(l.end,l.cliff);if(l.end<=l.start||BigInt(l.released)>BigInt(l.amount)||BigInt(l.amount)===0n||typeof l.linear!=='boolean')fail();}
 const rooms=new Set();for(const r of s.rooms){integer(r.id,1);if(rooms.has(r.id))fail();rooms.add(r.id);str(r.name,128);str(r.topic,1024);integer(r.slow,0,3600);if(r.identity!==0&&!ids.has(r.identity))fail();if(typeof r.gated!=='boolean'||!/^#[0-9a-f]{6}$/i.test(r.color))fail();array(r.bans,64);for(const u of r.bans)person(u);for(const [u,m]of Object.entries(r.members||{})){person(u);if(typeof m.accepted!=='boolean')fail();integer(m.epoch);}for(const [u,n]of Object.entries(r.moderators||{})){person(u);integer(n);}}
 if(!rooms.has(1))fail();for(const m of s.messages){integer(m.id,1);integer(m.reply);integer(m.receipt);integer(m.epoch);integer(m.time,1);person(m.actor);person(m.custodian);if(m.identity!==0&&!ids.has(m.identity)||!rooms.has(m.room))fail();str(m.body,1024);for(const [emoji,users]of Object.entries(m.reactions||{})){if(!['✧','♡','◎','↗'].includes(emoji))fail();array(users,64);users.forEach(person);}}
 for(const p of s.pools){symbol(p.asset);str(p.name,128);qty(p.native);qty(p.tokens);integer(p.born,1);if(!ids.has(p.creator))fail();}
 for(const x of s.sales){integer(x.id,1);str(x.name,128);symbol(x.symbol);for(const k of ['supply','public','founder','lp','soft','hard','raised'])qty(x[k]);integer(x.opens,1);integer(x.closes,x.opens);integer(x.liquidityBps,5000,10000);if(!ids.has(x.creator)||!rooms.has(x.room)||!['open','settled','refund'].includes(x.status))fail();array(x.claimed,64);for(const v of Object.values(x.contributions))qty(v);}
 for(const l of s.listings){integer(l.id,1);if(!ids.has(l.identity))fail();person(l.seller);qty(l.price);integer(l.expires,1);if(!['listed','sold','cancelled'].includes(l.status)||!/^0x[0-9a-f]{64}$/.test(l.manifest))fail();}
 for(const [who,v]of Object.entries(s.views||{})){person(who);array(v.follow,64);array(v.block,64);v.follow.forEach(person);v.block.forEach(person);}
 if(!s.settings||typeof s.settings.motion!=='boolean')fail();
}


/** Executable LOCAL specification. No chain, signatures, model inference, or price oracle.
 * Plans are content-bound reviews, NOT authenticated authorizations.
 * The financial model is integer reserve math, NOT Uniswap v4 tick execution.
 */
const DAY=86400;
const INTENT_DOMAIN='idfbi/instruments/1.5/unsigned-local';
const HEX=/^0x[0-9a-f]{64}$/;
const TOK=/^[A-Z][A-Z0-9]{1,11}$/;
const PEOPLE=['you','guest','archivist','weaver'];
const eq=(a,b)=>fingerprint(a)===fingerprint(b);
function keys(o,ks){if(!o||Object.getPrototypeOf(o)!==Object.prototype||Object.keys(o).sort().join('|')!==ks.sort().join('|'))throw Error('Unexpected plan fields.');}
function integer(n,lo,hi,label='Number'){if(!Number.isSafeInteger(n)||n<lo||n>hi)throw Error(label+' is outside its supported range.');return n;}
function intentAmount(n){if(typeof n!=='string'||!/^[0-9]{1,34}$/.test(n)||BigInt(n)<=0n||BigInt(n)>MAX112)throw Error('Invalid raw-unit amount.');return BigInt(n);}
function credit(a,k,n){a[k]=(BigInt(a[k]||0)+n).toString();}
function debit(a,k,n){if(BigInt(a[k]||0)<n)throw Error('Insufficient rehearsal '+k+' balance.');a[k]=(BigInt(a[k])-n).toString();}

class InstrumentEngine {
  constructor(now=Math.floor(Date.now()/1000)) {
    this.world=new KingdomModel(now);
    // Presentation names only; no replacement scene or replacement NFT identity.
    this.world.identity(1).name='Your artifact';
    this.world.identity(204).name='Artifact 0204';
    this.world.identity(317).name='Artifact 0317';
    this.world.room(1).topic='Public conversation. Your filters do not control other people.';
    this.world.room(2).name='Private posting circle';
    this.world.room(2).topic='Invitations control posting. These messages are not encrypted.';
    this.extra={schema:'idfbi/promises/1.5',gifts:[],personalBalances:{you:{},guest:{},archivist:{},weaver:{}}};
  }
  atomic(fn) {
    const state=structuredClone(this.world.s),extra=structuredClone(this.extra);
    try{return fn();}catch(e){this.world.s=state;this.extra=extra;throw e;}
  }
  context(anchor) {
    if(!HEX.test(anchor))throw Error('A full organism audit anchor is required.');
    const w=this.world,a=w.identity();
    return fingerprint({domain:INTENT_DOMAIN,anchor,actor:w.s.actor,id:a.id,owner:a.owner,
      epoch:a.epoch,nonce:a.nonce,balances:a.balances,pools:w.s.pools,
      gifts:this.extra.gifts.filter(g=>g.donor===a.id||g.recipient.kind==='artifact'&&g.recipient.id===a.id)});
  }
  normalize(raw) {
    keys(raw,['kind','input','output','amount','minOut','days','recipient']);
    if(!['SWAP','LOCK','GIFT'].includes(raw.kind))throw Error('Unknown instrument.');
    if(!TOK.test(raw.input)||!TOK.test(raw.output))throw Error('Invalid asset identifier.');
    intentAmount(raw.amount);intentAmount(raw.minOut);integer(raw.days,0,3650,'Days');
    if(raw.kind==='SWAP'&&raw.input===raw.output)throw Error('Choose two different assets.');
    if(raw.kind==='LOCK'&&(raw.days===0||raw.input!==raw.output))throw Error('A lock keeps the selected asset and needs a duration.');
    if(raw.kind!=='GIFT'&&raw.recipient!==null)throw Error('Only a gift has a separate recipient.');
    if(raw.kind==='GIFT'){
      integer(raw.days,2,3650,'Gift duration');keys(raw.recipient,['kind','id']);
      if(raw.recipient.kind==='person'){
        if(!PEOPLE.includes(raw.recipient.id))throw Error('Unknown rehearsal recipient.');
      }else if(raw.recipient.kind==='artifact'){
        integer(raw.recipient.id,1,Number.MAX_SAFE_INTEGER);this.world.identity(raw.recipient.id);
        if(raw.recipient.id===this.world.s.selected)throw Error('Use a normal lock for your own artifact.');
      }else throw Error('Choose a personal wallet or an artifact account.');
    }
    return structuredClone(raw);
  }
  prepare(raw,anchor) {
    const intent=this.normalize(raw),w=this.world;
    w._owner(w.s.selected);
    const context=this.context(anchor),deadline=w.s.now+600;
    const copy=this.constructor.restore(this.export());
    const preview=copy.apply(intent);
    const plan={domain:INTENT_DOMAIN,anchor,context,deadline,intent,preview};
    return {...plan,digest:fingerprint(plan)};
  }
  execute(plan,anchor) {
    return this.atomic(()=>{
      keys(plan,['domain','anchor','context','deadline','intent','preview','digest']);
      const {digest,...body}=plan;
      if(plan.domain!==INTENT_DOMAIN||!HEX.test(digest)||fingerprint(body)!==digest)throw Error('The reviewed plan was changed.');
      integer(plan.deadline,0,Number.MAX_SAFE_INTEGER);
      if(plan.anchor!==anchor||plan.context!==this.context(anchor))throw Error('The artifact, wallet, balances, or market changed. Review again.');
      if(this.world.s.now>plan.deadline)throw Error('This review expired. Prepare it again.');
      const intent=this.normalize(plan.intent);
      const prediction=this.constructor.restore(this.export()).apply(intent);
      if(!eq(prediction,plan.preview))throw Error('The result or schedule changed. Review again.');
      return this.apply(intent);
    });
  }
  apply(raw) {
    const p=this.normalize(raw),w=this.world,a=w._owner(w.s.selected),n=intentAmount(p.amount);
    return this.atomic(()=>{
      if(p.kind==='SWAP'){
        const result=w.swap(p.input,p.output,n,intentAmount(p.minOut),p.days);
        return {received:result.out.toString(),asset:p.output,lockId:result.lock?.id||0,
          releaseAt:result.lock?.end||0,steps:p.days?['SWAP','LOCK']:['SWAP']};
      }
      if(p.kind==='LOCK'){
        const l=w.deposit(p.input,n,p.days);
        return {received:n.toString(),asset:p.input,lockId:l.id,releaseAt:l.end,steps:['LOCK']};
      }
      let output=n,causes=[];
      if(p.input!==p.output){const result=w.swap(p.input,p.output,n,intentAmount(p.minOut));output=result.out;causes=[w.s.seq];}
      if(output<intentAmount(p.minOut))throw Error('Gift amount is below the reviewed minimum.');
      debit(a.balances,p.output,output);
      const g={id:this.extra.gifts.length+1,donor:a.id,donorActor:w.s.actor,donorEpoch:a.epoch,
        recipient:structuredClone(p.recipient),asset:p.output,amount:output.toString(),
        offeredAt:w.s.now,acceptBy:w.s.now+Math.min(3,p.days-1)*DAY,
        releaseAt:w.s.now+p.days*DAY,status:'offered',acceptedAt:0,released:'0',receipt:0};
      this.extra.gifts.push(g);a.nonce++;
      const e=w._event('OFFERED',a.id,{gift:g.id,asset:g.asset,amount:g.amount,
        recipient:g.recipient,acceptBy:g.acceptBy,releaseAt:g.releaseAt,causes});
      g.receipt=e.seq;
      return {received:g.amount,asset:g.asset,giftId:g.id,acceptBy:g.acceptBy,
        releaseAt:g.releaseAt,recipient:g.recipient,steps:causes.length?['SWAP','OFFER']:['OFFER']};
    });
  }
  gift(id){const g=this.extra.gifts.find(x=>x.id===Number(id));if(!g)throw Error('Unknown gift.');return g;}
  recipientActor(g){return g.recipient.kind==='person'?g.recipient.id:this.world.identity(g.recipient.id).owner;}
  accept(id) {return this.atomic(()=>{
    const g=this.gift(id),w=this.world;
    if(g.status!=='offered'||w.s.now>=g.acceptBy)throw Error('The offer expired or is no longer pending.');
    if(this.recipientActor(g)!==w.s.actor)throw Error('Only the designated recipient may accept.');
    g.status='accepted';g.acceptedAt=w.s.now;
    w._event('ACCEPTED',g.recipient.kind==='artifact'?g.recipient.id:0,{gift:g.id,releaseAt:g.releaseAt,causes:[g.receipt]});
    return structuredClone(g);
  });}
  cancel(id) {return this.atomic(()=>{
    const g=this.gift(id),w=this.world;
    if(g.status!=='offered')throw Error('An accepted gift cannot be recalled.');
    if(w.s.now<g.acceptBy)w._owner(g.donor);
    // Anyone may return an expired offer, but only to the original donor account.
    g.status='returned';credit(w.identity(g.donor).balances,g.asset,BigInt(g.amount));
    w._event('GIFT_RETURNED',g.donor,{gift:g.id,causes:[g.receipt]});
  });}
  releaseGift(id) {return this.atomic(()=>{
    const g=this.gift(id),w=this.world;
    if(g.status!=='accepted'||w.s.now<g.releaseAt)throw Error('This gift is not releasable at the actual rehearsal time.');
    g.status='released';g.released=g.amount;
    const balances=g.recipient.kind==='person'?this.extra.personalBalances[g.recipient.id]:w.identity(g.recipient.id).balances;
    credit(balances,g.asset,BigInt(g.amount));
    w._event('GIFT_RELEASED',g.recipient.kind==='artifact'?g.recipient.id:0,
      {gift:g.id,asset:g.asset,amount:g.amount,recipient:g.recipient,causes:[g.receipt]},w.s.actor);
    return BigInt(g.amount);
  });}
  // All permission-changing actor tests are checked at use time, not cached in the invitation.
  project(id,at) {
    integer(at,this.world.s.now,this.world.s.now+3650*DAY,'Projection date');
    const w=this.world,a=w.identity(id),balances=structuredClone(a.balances),claims=[];
    for(const l of w.s.locks.filter(l=>l.identity===id)){
      const n=releasable(l,at);credit(balances,l.asset,n);
      claims.push({type:'vault',id:l.id,asset:l.asset,available:n.toString(),end:l.end});
    }
    for(const g of this.extra.gifts.filter(g=>g.status==='accepted'&&g.recipient.kind==='artifact'&&g.recipient.id===id)){
      const n=at>=g.releaseAt?BigInt(g.amount):0n;credit(balances,g.asset,n);
      claims.push({type:'gift',id:g.id,asset:g.asset,available:n.toString(),end:g.releaseAt});
    }
    return {at,balances,claims,scope:'Scheduled claim projection only; no price, yield, auto-release, or future state prediction.'};
  }
  trace(seq) {
    const events=this.world.s.events,byId=new Map(events.map(e=>[e.seq,e])),seen=new Set(),result=[];
    const visit=n=>{if(seen.has(n))return;const e=byId.get(n);if(!e)return;seen.add(n);
      let refs=[...(e.data.causes||[])];
      if(e.data.sale)refs.push(...events.filter(x=>x.seq<e.seq&&x.data.sale===e.data.sale&&['LAUNCHED','CONTRIBUTED','SETTLED','REFUNDABLE'].includes(x.kind)).map(x=>x.seq));
      if(e.data.lock)refs.push(...events.filter(x=>x.seq<e.seq&&x.data.lock===e.data.lock&&['SWAPPED','LOCKED'].includes(x.kind)).map(x=>x.seq));
      refs=refs.filter(n=>Number.isSafeInteger(n)&&n>0&&n<e.seq);refs.forEach(visit);
      result.push({event:structuredClone(e),causes:[...new Set(refs)],discussions:this.world.s.messages.filter(m=>m.receipt===e.seq).map(m=>structuredClone(m))});
    };visit(Number(seq));return result;
  }
  export(){return {schema:'idfbi/instruments/1.5',world:this.world.export(),extra:structuredClone(this.extra),checksum:fingerprint({world:this.world.export(),extra:this.extra})};}
  static restore(data){
    keys(data,['schema','world','extra','checksum']);
    if(data.schema!=='idfbi/instruments/1.5'||data.checksum!==fingerprint({world:data.world,extra:data.extra}))throw Error('Instrument archive failed its integrity check.');
    const model=Object.create(InstrumentEngine.prototype);model.world=KingdomModel.restore(data.world);
    keys(data.extra,['schema','gifts','personalBalances']);
    if(data.extra.schema!=='idfbi/promises/1.5'||!Array.isArray(data.extra.gifts)||data.extra.gifts.length>2048)throw Error('Unsupported promise archive.');
    const ids=new Set();
    for(const g of data.extra.gifts){
      keys(g,['id','donor','donorActor','donorEpoch','recipient','asset','amount','offeredAt','acceptBy','releaseAt','status','acceptedAt','released','receipt']);
      integer(g.id,1,2048);if(g.id!==ids.size+1)throw Error('Gift IDs must be consecutive.');if(ids.has(g.id))throw Error('Duplicate gift.');ids.add(g.id);integer(g.donor,1,Number.MAX_SAFE_INTEGER);model.world.identity(g.donor);
      if(!PEOPLE.includes(g.donorActor)||!TOK.test(g.asset))throw Error('Invalid donor or asset.');
      integer(g.donorEpoch,0,Number.MAX_SAFE_INTEGER);intentAmount(g.amount);
      integer(g.offeredAt,1,Number.MAX_SAFE_INTEGER);integer(g.acceptBy,g.offeredAt+1,Number.MAX_SAFE_INTEGER);
      integer(g.releaseAt,g.acceptBy+1,g.offeredAt+3650*DAY);integer(g.acceptedAt,0,g.acceptBy-1);if(g.acceptedAt>0&&g.acceptedAt<g.offeredAt)throw Error('Acceptance predates its offer.');if(g.offeredAt>model.world.s.now||g.acceptedAt>model.world.s.now)throw Error('Future gift event.');
      integer(g.receipt,1,model.world.s.seq);
      if(!['offered','accepted','returned','released'].includes(g.status)||!['0',g.amount].includes(g.released))throw Error('Invalid gift state.');
      if((['accepted','released'].includes(g.status))!==(g.acceptedAt>0)||((g.status==='released')!==(g.released===g.amount)))throw Error('Inconsistent gift state.');
      keys(g.recipient,['kind','id']);if(g.recipient.kind==='person'){if(!PEOPLE.includes(g.recipient.id))throw Error('Invalid personal recipient.');}
      else if(g.recipient.kind==='artifact'){integer(g.recipient.id,1,Number.MAX_SAFE_INTEGER);model.world.identity(g.recipient.id);if(g.recipient.id===g.donor)throw Error('Self gift uses a normal lock.');}else throw Error('Invalid recipient type.');
      const e=model.world.s.events.find(x=>x.seq===g.receipt);
      if(e?.kind!=='OFFERED'||e.time!==g.offeredAt||e.actor!==g.donorActor||e.epoch!==g.donorEpoch||e.data.asset!==g.asset||e.data.gift!==g.id||e.data.amount!==g.amount||e.identity!==g.donor||!eq(e.data.recipient,g.recipient)||e.data.releaseAt!==g.releaseAt||e.data.acceptBy!==g.acceptBy)throw Error('Gift does not match its origin receipt.');
      const subsequent=model.world.s.events.filter(x=>x.seq>g.receipt&&x.data.gift===g.id);
      const accepted=subsequent.filter(x=>x.kind==='ACCEPTED'),returned=subsequent.filter(x=>x.kind==='GIFT_RETURNED'),released=subsequent.filter(x=>x.kind==='GIFT_RELEASED');
      if(accepted.length>1||returned.length>1||released.length>1||accepted.length!==Number(['accepted','released'].includes(g.status))||returned.length!==Number(g.status==='returned')||released.length!==Number(g.status==='released'))throw Error('Gift transitions do not match their receipts.');
      if(accepted.length&&(accepted[0].time!==g.acceptedAt||accepted[0].data.releaseAt!==g.releaseAt||!accepted[0].data.causes?.includes(g.receipt)))throw Error('Invalid acceptance receipt.');
      if(g.recipient.kind==='person'&&accepted.length&&accepted[0].actor!==g.recipient.id)throw Error('Wrong accepting person.');
      if(released.length&&(released[0].time<g.releaseAt||released[0].seq<accepted[0].seq||released[0].data.amount!==g.amount||released[0].data.asset!==g.asset||!eq(released[0].data.recipient,g.recipient)))throw Error('Invalid release receipt.');
    }
    keys(data.extra.personalBalances,PEOPLE.slice());
    for(const bs of Object.values(data.extra.personalBalances))for(const [asset,value]of Object.entries(bs)){
      if(!TOK.test(asset)||typeof value!=='string'||!/^[0-9]{1,78}$/.test(value)||BigInt(value)>=(1n<<256n))throw Error('Invalid personal balance.');
    }
    model.extra=structuredClone(data.extra);return model;
  }
}

/** Exact proportional allocation. Leftover indivisible units are explicit dust;
 * no first-come dust reward, random winner, or claim of wallet uniqueness. */
function proportionalAllocation(supply,bids) {
  supply=BigInt(supply);if(supply<0n||supply>MAX112||!Array.isArray(bids)||bids.length>10000)throw Error('Invalid allocation input.');
  const budgets=bids.map(x=>{x=BigInt(x);if(x<0n||x>MAX112)throw Error('Invalid budget.');return x;});
  const total=budgets.reduce((a,b)=>a+b,0n),allocations=budgets.map(b=>total?supply*b/total:0n);
  return {allocations,dust:supply-allocations.reduce((a,b)=>a+b,0n)};
}



/** Finite local execution model. Never evaluates user code or requests a wallet.
 * Published content is durable; grants are epoch-bound; obligations have their own lifecycle.
 * Laboratory capital is explicitly segregated, not a second allowance on the root account. */
const LAB_MODULES=['granary','issue','strips','counter','scrivener'];
function ceilRatio(a,b,d){a=BigInt(a);b=BigInt(b);d=BigInt(d);if(a<0n||b<0n||d<=0n)throw Error('Invalid ratio.');return (a*b+d-1n)/d;}
function matchedExit(assets,pairs,burn){assets=BigInt(assets);pairs=BigInt(pairs);burn=BigInt(burn);if(assets<0n||pairs<=0n||burn<=0n||burn>pairs)throw Error('Invalid matched exit.');return assets*burn/pairs;}
const opQ=v=>{if(!/^[0-9]{1,34}$/.test(String(v)))throw Error('Expected integer base units.');const n=BigInt(v);if(n<=0n||n>MAX112)throw Error('Amount out of range.');return n;};
const opN=v=>{if(!/^[0-9]{1,34}$/.test(String(v))||BigInt(v)>MAX112)throw Error('Invalid stored quantity.');return BigInt(v);};
const opText=(v,max=256)=>{if(typeof v!=='string'||!v.trim()||new TextEncoder().encode(v).length>max)throw Error('Text is empty or too long.');return v.trim();};
const opDays=(v,max=3650)=>{if(!Number.isInteger(v)||v<1||v>max)throw Error('Invalid duration.');return v;};
const opGet=(xs,id)=>{const v=xs.find(x=>x.id===Number(id));if(!v)throw Error('Unknown operating record.');return v;};
const opAdd=(b,k,n)=>{const v=BigInt(b[k]||0)+BigInt(n);if(v<0n||v>MAX112)throw Error('Balance out of range.');b[k]=String(v);};
const opTake=(b,k,n)=>{n=opQ(n);if(BigInt(b[k]||0)<n)throw Error('Insufficient '+k+' in this compartment.');opAdd(b,k,-n);};
function opFresh(){return {schema:'idfbi/operating-state/1.6',editions:[],installations:[],grants:[],commissions:[],collectibles:[],venues:[],cells:[],loans:[],options:[],covenants:{},applications:[],lossSink:{}};}
class OperatingEngine extends InstrumentEngine {
 constructor(now){super(now);this.op=opFresh();this._attach();
  if(!this.world.s.identities.some(a=>a.id===2)){this.world.s.identities.push({id:2,name:"Guest instrument account",owner:"guest",epoch:1,nonce:0,seed:29,color:0,balances:{ETH:String(wei("3")),AUR:String(wei("5000"))},lease:null});this.record("REHEARSAL_ACCOUNT",2,{note:"Fictional Guest allocation; not a mint or deposit."});}
  for(const l of this.world.s.listings.filter(l=>l.status==='listed'))this._bindListing(l);
 }
 _attach(){const self=this,w=this.world;
  w._owner=function(id){const a=this.identity(id);const d=self._delegation;if(a.owner===this.s.actor)return a;if(d&&d.identity===a.id&&d.epoch===a.epoch&&a.owner!=='escrow')return a;throw Error('Only the current owner may create new risk.');};
  const check=KingdomModel.prototype.checkListing;
  w.checkListing=function(id){const l=check.call(this,id),c=self.op.covenants[l.id];if(!c||c.moduleRoot!==fingerprint(self.moduleSnapshot(l.identity)))throw Error('Operating covenant changed or missing. Seller must refresh the listing.');return l;};
  w.listEstate=function(id,n,days=7){return self.atomic(()=>{const l=KingdomModel.prototype.listEstate.call(this,id,n,days);self._bindListing(l);return l;});};
 }
 atomic(fn){const saved=this.op?structuredClone(this.op):null;try{return super.atomic(fn);}catch(e){if(saved)this.op=saved;throw e;}}
 context(anchor){return fingerprint({base:super.context(anchor),operating:this.moduleSnapshot(this.world.s.selected)});}
 record(kind,id,data){const causes=new Set(data.causes||[]);if(data.cause)causes.add(data.cause);for(const key of ['commission','loan','option','venue','grant','edition'])if(data[key]){const prior=this.world.s.events.findLast(e=>e.data[key]===data[key]);if(prior)causes.add(prior.seq);}return this.world._event(kind,id,{...data,causes:[...causes].filter(n=>Number.isInteger(n)&&n>0&&n<=this.world.s.seq)});}
 edition(id){return opGet(this.op.editions,id);}
 publish(title,raw,parent=0){return this.atomic(()=>{const a=this.world._owner(this.world.s.selected);if(this.op.editions.length>=256)throw Error('Edition capacity reached.');const recipe=this.normalize(raw);if(parent)this.edition(parent);const e={id:this.op.editions.length+1,title:opText(title,96),author:this.world.s.actor,identity:a.id,epoch:a.epoch,time:this.world.s.now,parent:Number(parent),recipe};e.contentHash=fingerprint({schema:'idfbi/typed-edition/1.6',recipe});e.editionHash=fingerprint(e);e.receipt=this.record('EDITION_PUBLISHED',a.id,{edition:e.id,contentHash:e.contentHash,parent:e.parent}).seq;this.op.editions.push(e);return e;});}
 install(id){return this.atomic(()=>{const a=this.world._owner(this.world.s.selected),e=this.edition(id);if(this.op.installations.some(x=>x.identity===a.id&&x.edition===e.id))throw Error('Edition already in this library.');const i={id:this.op.installations.length+1,identity:a.id,edition:e.id,contentHash:e.contentHash,readyAt:this.world.s.now+7*DAY};this.op.installations.push(i);this.record('EDITION_ADOPTED',a.id,{edition:e.id,readyAt:i.readyAt,cause:e.receipt});return i;});}
 grant(edition,{caller,perCall,total,calls=1,days=7}){return this.atomic(()=>{const a=this.world._owner(this.world.s.selected),e=this.edition(edition),i=this.op.installations.find(x=>x.identity===a.id&&x.edition===e.id);if(!i||this.world.s.now<i.readyAt)throw Error('Installation is still in its seven-day review period.');if(!['you','guest'].includes(caller)||!Number.isInteger(calls)||calls<1||calls>100)throw Error('Invalid caller or call limit.');const p=opQ(perCall),t=opQ(total);if(p>t||BigInt(e.recipe.amount)>p)throw Error('Recipe exceeds the authorized budget.');opDays(days,30);const g={id:this.op.grants.length+1,identity:a.id,edition:e.id,editionHash:e.editionHash,epoch:a.epoch,caller,perCall:String(p),total:String(t),spent:'0',calls,used:0,expires:this.world.s.now+days*DAY,revoked:false};this.op.grants.push(g);this.record('GRANT_CREATED',a.id,{grant:g.id,edition:e.id,epoch:a.epoch,caller,budget:g.total,cause:e.receipt});return g;});}
 grantStatus(g){const a=this.world.identity(g.identity);return g.revoked?'revoked':a.owner==='escrow'?'escrowed':a.epoch!==g.epoch?'custody changed':this.world.s.now>=g.expires?'expired':g.used>=g.calls?'exhausted':'active';}
 revoke(id){const g=opGet(this.op.grants,id);this.world._owner(g.identity);g.revoked=true;this.record('GRANT_REVOKED',g.identity,{grant:g.id});}
 run(id){return this.atomic(()=>{const g=opGet(this.op.grants,id),e=this.edition(g.edition),n=BigInt(e.recipe.amount);if(this.grantStatus(g)!=='active'||g.caller!==this.world.s.actor||e.editionHash!==g.editionHash||n>BigInt(g.perCall)||BigInt(g.spent)+n>BigInt(g.total))throw Error('Grant rejects this caller, custody epoch, edition or budget.');const selection=this.world.s.selected;this._delegation={identity:g.identity,epoch:g.epoch};this.world.s.selected=g.identity;try{const result=this.apply(e.recipe);g.used++;g.spent=String(BigInt(g.spent)+n);this.record('EDITION_EXECUTED',g.identity,{grant:g.id,edition:e.id,amount:String(n),asset:e.recipe.input,cause:e.receipt});return result;}finally{this._delegation=null;this.world.s.selected=selection;}});}
 commission({title,worker,asset,amount,days=14,room=1}){return this.atomic(()=>{const a=this.world._owner(this.world.s.selected);if(!['you','guest'].includes(worker)||worker===this.world.s.actor)throw Error('Choose the other participant.');this.world.room(room);opDays(days,90);const n=opQ(amount);opTake(a.balances,asset,n);const c={id:this.op.commissions.length+1,identity:a.id,sponsor:this.world.s.actor,reviewer:this.world.s.actor,worker,asset,amount:String(n),title:opText(title,256),deadline:this.world.s.now+days*DAY,reviewEnds:this.world.s.now+(days+3)*DAY,room:Number(room),status:'offered',edition:0};c.receipt=this.record('COMMISSION_FUNDED',a.id,{commission:c.id,room:c.room,asset,amount:c.amount}).seq;this.op.commissions.push(c);return c;});}
 acceptCommission(id){const c=opGet(this.op.commissions,id);if(this.world.s.actor!==c.worker||c.status!=='offered'||this.world.s.now>=c.deadline)throw Error('Commission cannot be accepted.');c.status='accepted';this.record('COMMISSION_ACCEPTED',c.identity,{commission:c.id,cause:c.receipt});}
 submitCommission(id,edition){const c=opGet(this.op.commissions,id),e=this.edition(edition);if(this.world.s.actor!==c.worker||e.author!==c.worker||c.status!=='accepted'||this.world.s.now>=c.deadline)throw Error('Deliver your own edition before the deadline.');c.edition=e.id;c.status='submitted';this.record('WORK_SUBMITTED',c.identity,{commission:c.id,edition:e.id,cause:c.receipt});}
 decideCommission(id,approve){return this.atomic(()=>{const c=opGet(this.op.commissions,id);if(c.status!=='submitted'||this.world.s.actor!==c.reviewer||this.world.s.now>=c.reviewEnds)throw Error('Only the named reviewer can decide within the review window.');if(approve){opAdd(this.extra.personalBalances[c.worker],c.asset,c.amount);c.status='paid';}else{opAdd(this.world.identity(c.identity).balances,c.asset,c.amount);c.status='refunded';}this.record(approve?'WORK_PAID':'WORK_REJECTED',c.identity,{commission:c.id,edition:c.edition,cause:c.receipt});});}
 refundCommission(id){return this.atomic(()=>{const c=opGet(this.op.commissions,id);const timeout=this.world.s.now>=(c.status==='submitted'?c.reviewEnds:c.deadline);if(!['offered','accepted','submitted'].includes(c.status)||!timeout)throw Error('The fixed refund deadline has not arrived.');c.status='refunded';opAdd(this.world.identity(c.identity).balances,c.asset,c.amount);this.record('COMMISSION_REFUNDED',c.identity,{commission:c.id,cause:c.receipt});});}
 mintCollectible(edition){const a=this.world._owner(this.world.s.selected),e=this.edition(edition);const t={id:this.op.collectibles.length+1,edition:e.id,holder:a.id,venue:0};this.op.collectibles.push(t);this.record('EDITION_COLLECTED',a.id,{collectible:t.id,edition:e.id,cause:e.receipt});return t;}
 openShelf(collectible,price,days=7){return this.atomic(()=>{const a=this.world._owner(this.world.s.selected),t=opGet(this.op.collectibles,collectible);if(t.holder!==a.id||t.venue)throw Error('Collectible is not free inventory.');opDays(days,365);const v={id:this.op.venues.length+1,identity:a.id,collectible:t.id,price:String(opQ(price)),bondUntil:this.world.s.now+days*DAY,reserve:'0',status:'open'};t.venue=v.id;this.op.venues.push(v);this.record('VENUE_OPENED',a.id,{venue:v.id,collectible:t.id,bondUntil:v.bondUntil});return v;});}
 buyShelf(id){return this.atomic(()=>{const a=this.world._owner(this.world.s.selected),v=opGet(this.op.venues,id),t=opGet(this.op.collectibles,v.collectible);if(v.status!=='open'||a.id===v.identity)throw Error('Shelf unavailable or self-trade.');opTake(a.balances,'ETH',v.price);v.reserve=v.price;v.status='sold';t.holder=a.id;t.venue=0;this.record('SHELF_PURCHASED',a.id,{venue:v.id,collectible:t.id,amount:v.price,asset:'ETH'});});}
 closeShelf(id){return this.atomic(()=>{const v=opGet(this.op.venues,id),a=this.world._owner(v.identity);if(v.status==='closed'||this.world.s.now<v.bondUntil)throw Error('Venue bond has not ended.');const t=opGet(this.op.collectibles,v.collectible);if(v.status==='open'){t.venue=0;t.holder=a.id;}opAdd(a.balances,'ETH',v.reserve);v.reserve='0';v.status='closed';this.record('VENUE_CLOSED',a.id,{venue:v.id});});}
 cell(id=this.world.s.selected,create=false){let c=this.op.cells.find(x=>x.identity===Number(id));if(!c&&create){c={id:this.op.cells.length+1,identity:Number(id),enabled:[],enabledEpoch:this.world.identity(id).epoch,balances:{},strategy:{asset:'AUR',assets:'0',shares:'0'},basket:{supply:'0',ETH:'0',AUR:'0'},strip:null};this.op.cells.push(c);}if(!c)throw Error('Allocate a separate laboratory balance first.');return c;}
 allocate(asset,amount){return this.atomic(()=>{const a=this.world._owner(this.world.s.selected),n=opQ(amount),c=this.cell(a.id,true);opTake(a.balances,asset,n);opAdd(c.balances,asset,n);this.record('LAB_ALLOCATED',a.id,{cell:c.id,asset,amount:String(n)});return c;});}
 enableLab(module){const a=this.world._owner(this.world.s.selected),c=this.cell(a.id);if(!LAB_MODULES.includes(module))throw Error('This module has no runnable adapter.');if(c.enabledEpoch!==a.epoch){c.enabled=[];c.enabledEpoch=a.epoch;}if(!c.enabled.includes(module))c.enabled.push(module);this.record('LAB_ENABLED',a.id,{cell:c.id,module});}
 labOwner(module,id=this.world.s.selected,newRisk=true){const a=this.world._owner(id),c=this.cell(a.id);if(newRisk&&(c.enabledEpoch!==a.epoch||!c.enabled.includes(module)))throw Error('This experiment is locked in the engine. Explicitly enable it for this cell and custody epoch.');return c;}
 withdrawLab(asset,n){return this.atomic(()=>{const a=this.world._owner(this.world.s.selected),c=this.cell(a.id);n=opQ(n);opTake(c.balances,asset,n);opAdd(a.balances,asset,n);this.record('LAB_RETURNED',a.id,{asset,amount:String(n)});});}
 sow(n,minShares=1n){return this.atomic(()=>{const c=this.labOwner('granary'),s=c.strategy;n=opQ(n);const shares=BigInt(s.shares)===0n?n:n*BigInt(s.shares)/BigInt(s.assets);if(shares<opQ(minShares)||shares===0n)throw Error('Minimum shares not met; zero-asset strategies cannot accept a deposit.');opTake(c.balances,s.asset,n);opAdd(s,'assets',n);opAdd(s,'shares',shares);this.record('STRATEGY_DEPOSIT',c.identity,{asset:s.asset,amount:String(n),shares:String(shares),cell:c.id});return shares;});}
 reap(shares,minAssets=1n){return this.atomic(()=>{const c=this.labOwner('granary',this.world.s.selected,false),s=c.strategy;shares=opQ(shares);if(shares>BigInt(s.shares))throw Error('Insufficient strategy shares.');const n=BigInt(s.assets)*shares/BigInt(s.shares);if(n<opQ(minAssets))throw Error('Minimum assets not met.');opAdd(s,'shares',-shares);opAdd(s,'assets',-n);opAdd(c.balances,s.asset,n);this.record('STRATEGY_WITHDRAW',c.identity,{asset:s.asset,amount:String(n),shares:String(shares)});return n;});}
 mintBasket(shares){return this.atomic(()=>{const c=this.labOwner('issue'),b=c.basket;shares=opQ(shares);const eth=ceilRatio(shares,wei('0.0001'),E18),aur=ceilRatio(shares,wei('10'),E18);opTake(c.balances,'ETH',eth);opTake(c.balances,'AUR',aur);opAdd(b,'ETH',eth);opAdd(b,'AUR',aur);opAdd(b,'supply',shares);this.record('BASKET_MINTED',c.identity,{shares:String(shares),ETH:String(eth),AUR:String(aur)});return {eth,aur};});}
 redeemBasket(shares){return this.atomic(()=>{const c=this.labOwner('issue',this.world.s.selected,false),b=c.basket;shares=opQ(shares);if(shares>BigInt(b.supply))throw Error('Insufficient basket shares.');const eth=shares*wei('0.0001')/E18,aur=shares*wei('10')/E18;opAdd(b,'supply',-shares);opAdd(b,'ETH',-eth);opAdd(b,'AUR',-aur);opAdd(c.balances,'ETH',eth);opAdd(c.balances,'AUR',aur);if(b.supply==='0'){opAdd(c.balances,'ETH',b.ETH);opAdd(c.balances,'AUR',b.AUR);b.ETH=b.AUR='0';}this.record('BASKET_REDEEMED',c.identity,{shares:String(shares)});return {eth,aur};});}
 strip(n,days=30){return this.atomic(()=>{const c=this.labOwner('strips');if(c.strip&&BigInt(c.strip.pairs)>0n)throw Error('Close this matched series before starting another.');n=opQ(n);opDays(days);opTake(c.balances,'AUR',n);c.strip={asset:'AUR',assets:String(n),pairs:String(n),initial:String(n),maturity:this.world.s.now+days*DAY};this.record('PAIRS_CREATED',c.identity,{amount:String(n),asset:'AUR',maturity:c.strip.maturity});});}
 unstrip(n){return this.atomic(()=>{const c=this.labOwner('strips',this.world.s.selected,false),s=c.strip;if(!s)throw Error('No matched series.');n=opQ(n);const out=matchedExit(s.assets,s.pairs,n);opAdd(s,'pairs',-n);opAdd(s,'assets',-out);opAdd(c.balances,s.asset,out);this.record('PAIRS_RECOMBINED',c.identity,{amount:String(n),received:String(out),asset:s.asset});return out;});}
 stress(module,bps){return this.atomic(()=>{const c=this.labOwner(module);if(!['granary','strips'].includes(module)||!Number.isInteger(bps)||bps<1||bps>10000)throw Error('Loss scenario must be 1–10,000 basis points.');const p=module==='granary'?c.strategy:c.strip;if(!p)throw Error('No funded position.');const n=BigInt(p.assets)*BigInt(bps)/10000n;opAdd(p,'assets',-n);opAdd(this.op.lossSink,p.asset,n);this.record('LAB_LOSS_SCENARIO',c.identity,{module,asset:p.asset,amount:String(n),note:'Explicit hypothetical loss; not market data.'});return n;});}
 requestLoan(collateral,principal,repayment,days=14){return this.atomic(()=>{const c=this.labOwner('counter');collateral=opQ(collateral);principal=opQ(principal);repayment=opQ(repayment);if(repayment<principal)throw Error('Repayment below principal.');opDays(days,365);opTake(c.balances,'AUR',collateral);const l={id:this.op.loans.length+1,borrower:c.identity,lender:0,collateral:String(collateral),principal:String(principal),repayment:String(repayment),days,offerUntil:this.world.s.now+7*DAY,due:0,status:'offered'};this.op.loans.push(l);this.record('CREDIT_REQUESTED',c.identity,{loan:l.id,amount:l.principal,asset:'ETH'});return l;});}
 fundLoan(id){return this.atomic(()=>{const c=this.labOwner('counter'),l=opGet(this.op.loans,id);if(l.status!=='offered'||c.identity===l.borrower||this.world.s.now>=l.offerUntil)throw Error('Credit offer unavailable.');opTake(c.balances,'ETH',l.principal);opAdd(this.cell(l.borrower).balances,'ETH',l.principal);l.lender=c.identity;l.due=this.world.s.now+l.days*DAY;l.status='active';this.record('CREDIT_FUNDED',c.identity,{loan:l.id,borrower:l.borrower,due:l.due});});}
 repayLoan(id){return this.atomic(()=>{const l=opGet(this.op.loans,id),c=this.labOwner('counter',l.borrower,false);if(l.status!=='active'||this.world.s.now>=l.due)throw Error('Repayment window has closed.');opTake(c.balances,'ETH',l.repayment);opAdd(this.cell(l.lender).balances,'ETH',l.repayment);opAdd(c.balances,'AUR',l.collateral);l.status='repaid';this.record('CREDIT_REPAID',c.identity,{loan:l.id});});}
 closeLoan(id){return this.atomic(()=>{const l=opGet(this.op.loans,id);if(l.status==='offered'){if(this.world.s.now<l.offerUntil)this.world._owner(l.borrower);opAdd(this.cell(l.borrower).balances,'AUR',l.collateral);l.status='cancelled';}else if(l.status==='active'&&this.world.s.now>=l.due){opAdd(this.cell(l.lender).balances,'AUR',l.collateral);l.status='defaulted';}else throw Error('No collateral release is available.');this.record('CREDIT_CLOSED',l.borrower,{loan:l.id,status:l.status});});}
 writeOption(underlying,strike,premium,days=14){return this.atomic(()=>{const c=this.labOwner('scrivener');opDays(days,365);underlying=opQ(underlying);strike=opQ(strike);premium=opQ(premium);opTake(c.balances,'AUR',underlying);const o={id:this.op.options.length+1,writer:c.identity,buyer:0,underlying:String(underlying),strike:String(strike),premium:String(premium),expiry:this.world.s.now+days*DAY,status:'offered'};this.op.options.push(o);this.record('OPTION_WRITTEN',c.identity,{option:o.id,expiry:o.expiry,asset:'AUR',amount:o.underlying});return o;});}
 takeOption(id){return this.atomic(()=>{const c=this.labOwner('scrivener'),o=opGet(this.op.options,id);if(o.status!=='offered'||c.identity===o.writer||this.world.s.now>=o.expiry)throw Error('Option not available.');opTake(c.balances,'ETH',o.premium);opAdd(this.cell(o.writer).balances,'ETH',o.premium);o.buyer=c.identity;o.status='active';this.record('OPTION_BOUGHT',c.identity,{option:o.id});});}
 exercise(id){return this.atomic(()=>{const o=opGet(this.op.options,id),c=this.labOwner('scrivener',o.buyer,false);if(o.status!=='active'||this.world.s.now>=o.expiry)throw Error('Exercise window closed.');opTake(c.balances,'ETH',o.strike);opAdd(this.cell(o.writer).balances,'ETH',o.strike);opAdd(c.balances,'AUR',o.underlying);o.status='exercised';this.record('OPTION_EXERCISED',c.identity,{option:o.id});});}
 closeOption(id){return this.atomic(()=>{const o=opGet(this.op.options,id);if(!['offered','active'].includes(o.status))throw Error('Option already closed.');if(this.world.s.now<o.expiry){if(o.status!=='offered')throw Error('Accepted cover remains locked until expiry.');this.world._owner(o.writer);}opAdd(this.cell(o.writer).balances,'AUR',o.underlying);o.status='closed';this.record('OPTION_CLOSED',o.writer,{option:o.id});});}
 registerApplication(title,hash,bytes){const a=this.world._owner(this.world.s.selected);if(!/^0x[0-9a-f]{64}$/.test(hash)||!Number.isInteger(bytes)||bytes<1||bytes>4000000)throw Error('Invalid application manifest.');const x={id:this.op.applications.length+1,identity:a.id,title:opText(title,96),hash,bytes,time:this.world.s.now};this.op.applications.push(x);this.record('APPLICATION_COMMITTED',a.id,{application:x.id,hash,bytes});return x;}
 moduleSnapshot(id){id=Number(id);return {version:'1.6',gifts:this.extra.gifts.filter(g=>g.donor===id||g.recipient.kind==='artifact'&&g.recipient.id===id),installations:this.op.installations.filter(x=>x.identity===id),grants:this.op.grants.filter(x=>x.identity===id),commissions:this.op.commissions.filter(x=>x.identity===id),collectibles:this.op.collectibles.filter(x=>x.holder===id),venues:this.op.venues.filter(x=>x.identity===id),cells:this.op.cells.filter(x=>x.identity===id),loans:this.op.loans.filter(x=>x.borrower===id||x.lender===id),options:this.op.options.filter(x=>x.writer===id||x.buyer===id),applications:this.op.applications.filter(x=>x.identity===id)};}
 _bindListing(l){const moduleRoot=fingerprint(this.moduleSnapshot(l.identity));const base=fingerprint({domain:'LOCAL_ESTATE_COVENANT_1_4',id:l.id,identity:l.identity,seller:l.seller,price:l.price,expires:l.expires,status:'listed',inventory:l.inventory});const manifest=fingerprint({domain:'IDFBI_OPERATING_COVENANT_1_6',base,moduleRoot});this.op.covenants[l.id]={moduleRoot,manifest,base};l.manifest=manifest;this.record('MANIFEST_BOUND',l.identity,{listing:l.id,moduleRoot,manifest},l.seller);}
 refreshListing(id){return this.atomic(()=>{const l=opGet(this.world.s.listings,id);if(l.status!=='listed'||l.seller!==this.world.s.actor||this.world.identity(l.identity).owner!=='escrow')throw Error('Only the listing seller may refresh declared contents.');l.inventory=this.world._inventorySnapshot(l.identity);this._bindListing(l);});}
 ledger(id=this.world.s.selected){const a=this.world.identity(id);return {identity:a.id,owner:a.owner,epoch:a.epoch,free:structuredClone(a.balances),projected:this.project(a.id,this.world.s.now),modules:structuredClone(this.moduleSnapshot(a.id)),activeGrants:this.op.grants.filter(g=>g.identity===a.id).map(g=>({id:g.id,status:this.grantStatus(g),remaining:String(BigInt(g.total)-BigInt(g.spent))})),note:'No aggregate valuation. Each asset has its own units. Laboratory collateral and committed funds are not free balances.'};}
 export(){const b={schema:'idfbi/operating/1.6',instruments:super.export(),operating:structuredClone(this.op)};return {...b,checksum:fingerprint(b)};}
 static restore(x){if(x?.schema==='idfbi/instruments/1.5'){const e=InstrumentEngine.restore(x);Object.setPrototypeOf(e,OperatingEngine.prototype);e.op=opFresh();e._attach();return e;}if(!x||x.schema!=='idfbi/operating/1.6')throw Error('Unsupported operating archive.');const {checksum,...b}=x;if(Object.keys(x).sort().join(',')!=='checksum,instruments,operating,schema'||fingerprint(b)!==checksum)throw Error('Operating archive checksum failed.');const e=InstrumentEngine.restore(x.instruments);Object.setPrototypeOf(e,OperatingEngine.prototype);e.op=structuredClone(x.operating);validateOperating(e);e._attach();return e;}
}
function validateOperating(e){const o=e.op,w=e.world,fail=()=>{throw Error('Unsafe or inconsistent operating archive.');};if(!o||o.schema!=='idfbi/operating-state/1.6')fail();const expected=Object.keys(opFresh()).sort().join(',');if(Object.keys(o).sort().join(',')!==expected)fail();
 const arrs=['editions','installations','grants','commissions','collectibles','venues','cells','loans','options','applications'];for(const k of arrs){if(!Array.isArray(o[k])||o[k].length>512)fail();for(let i=0;i<o[k].length;i++)if(o[k][i].id!==i+1)fail();}
 const person=v=>{if(!['you','guest','archivist','weaver'].includes(v))fail();};const identity=v=>{w.identity(v);};const time=v=>{if(!Number.isSafeInteger(v)||v<0)fail();};const asset=v=>{if(!/^[A-Z][A-Z0-9]{1,11}$/.test(v))fail();};
 const quantities=b=>{if(!b||typeof b!=='object'||Array.isArray(b)||Object.keys(b).length>512)fail();for(const[k,v]of Object.entries(b)){asset(k);opN(v);}};
 for(const x of o.editions){identity(x.identity);person(x.author);time(x.time);time(x.epoch);opText(x.title,96);if(x.parent>=x.id||x.parent<0)fail();const recipe=e.normalize(x.recipe);if(fingerprint(recipe)!==fingerprint(x.recipe)||x.contentHash!==fingerprint({schema:'idfbi/typed-edition/1.6',recipe}))fail();const{editionHash,receipt,contentHash,...body}=x;if(editionHash!==fingerprint({...body,contentHash}))fail();const ev=w.s.events.find(z=>z.seq===receipt);if(ev?.kind!=='EDITION_PUBLISHED'||ev.data.edition!==x.id||ev.data.contentHash!==contentHash)fail();}
 for(const x of o.installations){identity(x.identity);if(e.edition(x.edition).contentHash!==x.contentHash)fail();time(x.readyAt);}
 for(const x of o.grants){identity(x.identity);person(x.caller);time(x.epoch);time(x.expires);if(e.edition(x.edition).editionHash!==x.editionHash||typeof x.revoked!=='boolean')fail();opQ(x.perCall);opQ(x.total);opN(x.spent);if(BigInt(x.spent)>BigInt(x.total)||BigInt(x.perCall)>BigInt(x.total)||!Number.isInteger(x.calls)||x.calls<1||x.calls>100||!Number.isInteger(x.used)||x.used<0||x.used>x.calls)fail();if(BigInt(x.spent)!==BigInt(e.edition(x.edition).recipe.amount)*BigInt(x.used))fail();}
 for(const x of o.commissions){identity(x.identity);person(x.sponsor);person(x.worker);person(x.reviewer);asset(x.asset);opQ(x.amount);opText(x.title);time(x.deadline);time(x.reviewEnds);if(x.reviewEnds!==x.deadline+3*DAY||!['offered','accepted','submitted','paid','refunded'].includes(x.status))fail();w.room(x.room);if(['submitted','paid'].includes(x.status)&&e.edition(x.edition).author!==x.worker)fail();}
 for(const c of o.cells){identity(c.identity);time(c.enabledEpoch);quantities(c.balances);if(!Array.isArray(c.enabled)||c.enabled.some(k=>!LAB_MODULES.includes(k))||new Set(c.enabled).size!==c.enabled.length)fail();if(c.strategy.asset!=='AUR')fail();opN(c.strategy.assets);opN(c.strategy.shares);for(const k of ['supply','ETH','AUR'])opN(c.basket[k]);if(BigInt(c.basket.ETH)<ceilRatio(c.basket.supply,wei('0.0001'),E18)||BigInt(c.basket.AUR)<ceilRatio(c.basket.supply,wei('10'),E18))fail();if(c.strip){for(const k of ['assets','pairs','initial'])opN(c.strip[k]);if(c.strip.asset!=='AUR'||BigInt(c.strip.assets)>BigInt(c.strip.initial)||BigInt(c.strip.pairs)>BigInt(c.strip.initial))fail();time(c.strip.maturity);}}
 if(new Set(o.cells.map(c=>c.identity)).size!==o.cells.length)fail();
 for(const x of o.collectibles){identity(x.holder);e.edition(x.edition);if(!Number.isInteger(x.venue)||x.venue<0||x.venue>o.venues.length)fail();}
 for(const x of o.venues){identity(x.identity);opQ(x.price);opN(x.reserve);time(x.bondUntil);opGet(o.collectibles,x.collectible);if(!['open','sold','closed'].includes(x.status))fail();}
 for(const x of o.loans){identity(x.borrower);if(x.lender)identity(x.lender);for(const k of ['collateral','principal','repayment'])opQ(x[k]);if(BigInt(x.repayment)<BigInt(x.principal)||!['offered','active','cancelled','defaulted','repaid'].includes(x.status))fail();opDays(x.days,365);time(x.offerUntil);time(x.due);if(['active','defaulted','repaid'].includes(x.status)&&(!x.lender||x.lender===x.borrower||x.due===0))fail();e.cell(x.borrower);if(x.lender)e.cell(x.lender);}
 for(const x of o.options){identity(x.writer);if(x.buyer)identity(x.buyer);for(const k of ['underlying','strike','premium'])opQ(x[k]);time(x.expiry);if(!['offered','active','exercised','closed'].includes(x.status)||['active','exercised'].includes(x.status)&&(!x.buyer||x.buyer===x.writer))fail();e.cell(x.writer);if(x.buyer)e.cell(x.buyer);}
 for(const x of o.applications){identity(x.identity);opText(x.title,96);if(!/^0x[0-9a-f]{64}$/.test(x.hash)||!Number.isInteger(x.bytes)||x.bytes<1||x.bytes>4000000)fail();time(x.time);}
 for(const [id,c]of Object.entries(o.covenants)){const l=opGet(w.s.listings,id);if(!/^0x[0-9a-f]{64}$/.test(c.moduleRoot)||c.manifest!==fingerprint({domain:'IDFBI_OPERATING_COVENANT_1_6',base:c.base,moduleRoot:c.moduleRoot})||c.manifest!==l.manifest)fail();}quantities(o.lossSink);
}


/** Native authenticated encryption only. No homemade cipher or plaintext fallback.
 * Passphrases and decrypted bodies never belong in a MemoryEngine export. */
const MEMORY_KDF_ROUNDS=600000;
const memUTF8=new TextEncoder();
const memHex=b=>Array.from(b,x=>x.toString(16).padStart(2,'0')).join('');
const memBytes=h=>{if(typeof h!=='string'||!/^([0-9a-f]{2})*$/.test(h))throw Error('Malformed encoded memory.');return Uint8Array.from(h.match(/../g)||[],x=>parseInt(x,16));};
function memNative(){if(!globalThis.crypto?.subtle||!crypto.getRandomValues)throw Error('Encrypted memory needs native Web Crypto in a secure browser context. Nothing was saved as plaintext.');return crypto.subtle;}
function memPass(pass){if(typeof pass!=='string'||pass.length<12||memUTF8.encode(pass).length>1024)throw Error('Use a unique memory passphrase of at least 12 characters (maximum 1024 UTF-8 bytes). It is not your wallet seed phrase.');}
function memoryBody(raw){
 const limits={title:160,thesis:3000,setup:512,invalidation:800,exitPlan:800,horizon:256,risk:256,tags:256,feeling:256,lesson:1500};
 if(!raw||typeof raw!=='object'||Array.isArray(raw)||Object.keys(raw).some(k=>!(k in limits)))throw Error('Unsupported journal field.');
 const body={};for(const[k,max]of Object.entries(limits)){const v=raw[k]??'';if(typeof v!=='string'||memUTF8.encode(v).length>max)throw Error(k+' exceeds its UTF-8 limit.');body[k]=v.trim();}
 if(!body.thesis&&!body.lesson)throw Error('Write a reason, a memory, or a reflection first.');
 return body;
}
function memPack(body){const bytes=memUTF8.encode(stable(memoryBody(body)));if(bytes.length>12000)throw Error('Memory body too large.');const padded=new Uint8Array(Math.ceil((bytes.length+4)/1024)*1024);new DataView(padded.buffer).setUint32(0,bytes.length);padded.set(bytes,4);return padded;}
function memUnpack(bytes){if(bytes.length<4)throw Error('Invalid private memory.');const len=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength).getUint32(0);if(len>12000||len+4>bytes.length||bytes.slice(len+4).some(x=>x!==0))throw Error('Invalid private memory padding.');return memoryBody(JSON.parse(new TextDecoder('utf-8',{fatal:true}).decode(bytes.slice(4,4+len))));}
async function memKey(pass,salt,usage){memPass(pass);const s=memNative(),key=await s.importKey('raw',memUTF8.encode(pass),'PBKDF2',false,['deriveKey']);return s.deriveKey({name:'PBKDF2',salt,iterations:MEMORY_KDF_ROUNDS,hash:'SHA-256'},key,{name:'AES-GCM',length:256},false,usage);}
async function encryptMemory(header,body,pass){memPass(pass);memNative();const salt=crypto.getRandomValues(new Uint8Array(16)),iv=crypto.getRandomValues(new Uint8Array(12));const key=await memKey(pass,salt,['encrypt']);const bytes=await crypto.subtle.encrypt({name:'AES-GCM',iv,additionalData:memUTF8.encode(stable(header)),tagLength:128},key,memPack(body));return {mode:'encrypted',cipher:'AES-256-GCM',kdf:'PBKDF2-SHA256',rounds:MEMORY_KDF_ROUNDS,salt:memHex(salt),iv:memHex(iv),data:memHex(new Uint8Array(bytes))};}
function validateMemoryPayload(payload){
 if(payload?.mode==='public'){if(Object.keys(payload).sort().join(',')!=='body,mode')throw Error('Invalid public memory.');if(stable(memoryBody(payload.body))!==stable(payload.body))throw Error('Noncanonical public memory.');return;}
 if(!payload||Object.keys(payload).sort().join(',')!=='cipher,data,iv,kdf,mode,rounds,salt'||payload.mode!=='encrypted'||payload.cipher!=='AES-256-GCM'||payload.kdf!=='PBKDF2-SHA256'||payload.rounds!==MEMORY_KDF_ROUNDS||memBytes(payload.salt).length!==16||memBytes(payload.iv).length!==12)throw Error('Unsupported encrypted memory.');
 if(typeof payload.data!=='string'||payload.data.length>26656)throw Error('Ciphertext exceeds the memory limit.');const n=memBytes(payload.data).length;if(n<1040||n>13328||(n-16)%1024)throw Error('Invalid ciphertext length.');
}
async function decryptMemory(header,payload,pass){validateMemoryPayload(payload);if(payload.mode==='public')return structuredClone(payload.body);try{const key=await memKey(pass,memBytes(payload.salt),['decrypt']);const data=await crypto.subtle.decrypt({name:'AES-GCM',iv:memBytes(payload.iv),additionalData:memUTF8.encode(stable(header)),tagLength:128},key,memBytes(payload.data));return memUnpack(new Uint8Array(data));}catch{throw Error('Cannot open this memory: wrong passphrase, changed header, or damaged ciphertext. Nothing was modified.');}}
function memorySeal(header,payload){validateMemoryPayload(payload);return {header:structuredClone(header),payload:structuredClone(payload),digest:fingerprint({domain:'IDFBI_MEMORY_ENVELOPE_1_7',header,payload})};}




const MEM_ZERO='0x'+'00'.repeat(32);
const memHash=v=>typeof v==='string'&&/^0x[0-9a-f]{64}$/.test(v);
const memInt=n=>Number.isSafeInteger(n)&&n>=0;
const memFresh=(seq,origin)=>({schema:'idfbi/memory-state/1.7',origin,start:seq,entries:[],bindings:[]});
/** Append-only, unsigned LOCAL journal. Public metadata and ciphertext are not signatures.
 * Historical custody stays attributed. The NFT never acquires an author's decryption key. */
class MemoryEngine extends OperatingEngine {
 constructor(now,origin=MEM_ZERO){super(now);if(!memHash(origin))throw Error('Invalid origin.');this.mem=memFresh(this.world.s.seq,origin);}
 atomic(fn){const saved=this.mem?structuredClone(this.mem):null;try{return super.atomic(fn);}catch(e){if(saved)this.mem=saved;throw e;}}
 memoryHeader({phase='memory',parent=0,plan=MEM_ZERO,receipt=0,form=true}={}){
  const w=this.world,a=w.identity();if(!['memory','before','reflection'].includes(phase)||!memInt(parent)||!memInt(receipt)||!memHash(plan)||typeof form!=='boolean')throw Error('Invalid memory context.');
  if(phase==='reflection'){const p=this.entry(parent);if(p.header.author!==w.s.actor)throw Error('Only the original author can add a reflection.');if(p.header.identity!==a.id)throw Error('Select the original artifact for this reflection.');}
  else{w._owner(a.id);if(parent)throw Error('Only reflections have parents.');}
  if(phase==='before'&&plan===MEM_ZERO)throw Error('A before-trade inscription must bind a reviewed plan.');
  if(phase!=='before'&&plan!==MEM_ZERO)throw Error('Only a before-trade entry may bind a plan.');
  if(receipt&&!w.s.events.some(e=>e.seq===receipt&&e.identity===a.id))throw Error('Receipt belongs to another artifact or does not exist.');
  return {schema:'idfbi/memory-header/1.7',origin:this.mem.origin,id:this.mem.entries.length+1,identity:a.id,author:w.s.actor,custodian:a.owner,epoch:a.epoch,time:w.s.now,phase,parent,plan,receipt,form:form&&a.owner===w.s.actor,previous:this.mem.entries.at(-1)?.digest||MEM_ZERO};
 }
 entry(id){const e=this.mem.entries.find(x=>x.header.id===Number(id));if(!e)throw Error('Unknown memory.');return e;}
 appendMemory(sealed){return this.atomic(()=>{if(this.mem.entries.length>=1000)throw Error('This rehearsal holds at most 1000 memories; export before continuing.');
  const h=sealed?.header;if(h?.phase==='before'&&!this._journalTransaction)throw Error('Before entries require atomic swap execution.');if(!h)throw Error('Memory header required.');const expected=this.memoryHeader({phase:h.phase,parent:h.parent,plan:h.plan,receipt:h.receipt,form:h.form});
  if(stable(expected)!==stable(h)||memorySeal(h,sealed.payload).digest!==sealed.digest)throw Error('The memory context changed. Reopen and review the inscription.');
  const ev=this.world._event('MEMORY_INSCRIBED',h.identity,{memory:h.id,commitment:sealed.digest,phase:h.phase,form:h.form,parent:h.parent,causes:h.receipt?[h.receipt]:[]});
  const record={...structuredClone(sealed),event:ev.seq};this.mem.entries.push(record);return record;
 });}
 executeNoted(plan,anchor,sealed){return this.atomic(()=>{
  if(sealed.header.phase!=='before'||sealed.header.plan!==plan.digest)throw Error('The inscription is bound to another plan.');
  const checked=this.constructor.restore(this.export()).execute(plan,anchor);
  let note;this._journalTransaction=true;try{note=this.appendMemory(sealed);}finally{this._journalTransaction=false;}const since=this.world.s.seq;const result=this.apply(plan.intent);
  if(stable(result)!==stable(checked))throw Error('The journaled outcome changed.');
  const ev=this.world.s.events.slice().reverse().find(e=>e.seq>since&&e.kind==='SWAPPED');if(!ev)throw Error('No successful swap receipt to bind.');
  const binding={entry:note.header.id,plan:plan.digest,receipt:ev.seq,receiptDigest:ev.digest,result:structuredClone(result),recordedAt:this.world.s.now};
  const linked=this.world._event('MEMORY_BOUND',note.header.identity,{memory:note.header.id,plan:plan.digest,causes:[note.event,ev.seq]});binding.event=linked.seq;this.mem.bindings.push(binding);return result;
 });}
 form(id=this.world.s.selected,through=this.world.s.seq){return memoryForm(this.world.s.events,this.mem,id,through);}
 export(){const b={schema:'idfbi/remembering/1.7',operating:super.export(),memory:structuredClone(this.mem)};return {...b,checksum:fingerprint(b)};}
 static restore(x,origin=MEM_ZERO){
  let e;if(x?.schema!=='idfbi/remembering/1.7'){e=OperatingEngine.restore(x);Object.setPrototypeOf(e,MemoryEngine.prototype);e.mem=memFresh(e.world.s.seq,origin);return e;}
  const{checksum,...b}=x;if(Object.keys(x).sort().join(',')!=='checksum,memory,operating,schema'||fingerprint(b)!==checksum)throw Error('Memory archive checksum failed.');
  e=OperatingEngine.restore(x.operating);Object.setPrototypeOf(e,MemoryEngine.prototype);e.mem=structuredClone(x.memory);validateMemState(e);return e;
 }
}
function validateMemState(e){
 const m=e.mem,w=e.world;if(!m||Object.keys(m).sort().join(',')!=='bindings,entries,origin,schema,start'||m.schema!=='idfbi/memory-state/1.7'||!memHash(m.origin)||!memInt(m.start)||m.start>w.s.seq||!Array.isArray(m.entries)||m.entries.length>1000||!Array.isArray(m.bindings)||m.bindings.length>1000)throw Error('Invalid memory archive.');
 let prev=MEM_ZERO,evt=0;
 for(let i=0;i<m.entries.length;i++){
  const n=m.entries[i],h=n.header;validateMemoryPayload(n.payload);
  if(Object.keys(n).sort().join(',')!=='digest,event,header,payload'||!h||Object.keys(h).sort().join(',')!=='author,custodian,epoch,form,id,identity,origin,parent,phase,plan,previous,receipt,schema,time'||h.schema!=='idfbi/memory-header/1.7'||h.origin!==m.origin||h.id!==i+1||h.previous!==prev||!memInt(h.time)||!memInt(h.epoch)||!['you','guest'].includes(h.author)||typeof h.form!=='boolean'||!['memory','before','reflection'].includes(h.phase)||!memHash(h.plan)||!memInt(h.parent)||h.parent>=h.id||!memInt(h.receipt)||!memInt(n.event)||n.event<=evt||h.time>w.s.now||memorySeal(h,n.payload).digest!==n.digest)throw Error('Inconsistent memory record.');
  w.identity(h.identity);if(!['you','guest','archivist','weaver','escrow'].includes(h.custodian)||h.form&&h.author!==h.custodian)throw Error('Invalid memory custody.');
  const ev=w.s.events.find(x=>x.seq===n.event);if(!ev||ev.kind!=='MEMORY_INSCRIBED'||ev.identity!==h.identity||ev.actor!==h.author||ev.epoch!==h.epoch||ev.custodian!==h.custodian||ev.time!==h.time||ev.data.commitment!==n.digest||ev.data.memory!==h.id||ev.data.phase!==h.phase||ev.data.form!==h.form||ev.data.parent!==h.parent)throw Error('Memory receipt mismatch.');
  if(h.phase==='reflection'){const p=m.entries[h.parent-1];if(!p||p.header.author!==h.author||p.header.identity!==h.identity)throw Error('Reflection authorship mismatch.');}else if(h.parent)throw Error('Unexpected parent.');
  if((h.phase==='before')!==(h.plan!==MEM_ZERO))throw Error('Plan phase mismatch.');
  if(h.receipt){const r=w.s.events.find(x=>x.seq===h.receipt);if(!r||r.seq>=n.event||r.identity!==h.identity)throw Error('Memory references an invalid receipt.');}
  prev=n.digest;evt=n.event;
 }
 const seen=new Set();for(const b of m.bindings){const n=m.entries[b.entry-1],r=w.s.events.find(x=>x.seq===b.receipt),end=w.s.events.find(x=>x.seq===b.event);if(Object.keys(b).sort().join(',')!=='entry,event,plan,receipt,receiptDigest,recordedAt,result'||!n||seen.has(b.entry)||n.header.phase!=='before'||n.header.plan!==b.plan||!r||r.kind!=='SWAPPED'||r.identity!==n.header.identity||r.actor!==n.header.author||r.seq<=n.event||r.digest!==b.receiptDigest||!end||end.kind!=='MEMORY_BOUND'||end.seq<=r.seq||end.data.memory!==b.entry||end.data.plan!==b.plan||b.recordedAt!==end.time||!end.data.causes.includes(n.event)||!end.data.causes.includes(r.seq)||b.result?.received!==r.data.received||b.result?.asset!==r.data.output||b.result?.lockId!==(r.data.lock||0))throw Error('Invalid trade/memory binding.');seen.add(b.entry);}
 for(const n of m.entries)if(n.header.phase==='before'&&!seen.has(n.header.id))throw Error('Missing successful swap binding.');
}
// These categories do NOT score virtue, profit, volume, truthfulness or humanity.
const FORM_CHANNELS=['exchange','commitment','creation','connection','exploration','memory'];
function memoryCategory(kind){
 if(/^(SWAPPED|CONTRIBUTED|WITHDRAWN|WITHDREW|CLAIMED|ACQUIRED|LISTED|BOUGHT|SHELF_BOUGHT|SHELF_PURCHASED)/.test(kind))return 0;
 if(/(EXIT_|LOCK|GIFT|OFFERED|ACCEPTED|RELEASED|RETURNED|CREDIT|OPTION|COMMISSION|WORK_)/.test(kind))return 1;
 if(/(LAUNCH|SETTLED|EDITION|APPLICATION|SHELF_OPEN|VENUE_)/.test(kind))return 2;
 if(/(POST|SPOKE|JOINED|REMOVED|MESSAGE|ROOM|INVIT|MEMBER|REACT|FOLLOW|BLOCK|PRESENCE)/.test(kind))return 3;
 if(/(LAB_|STRATEGY|BASKET|MATCHED|PAIRS_|GRANT|INSTRUMENT|ADOPT|INSTALL)/.test(kind))return 4;
 if(kind==='MEMORY_INSCRIBED')return 5;
 return -1;
}
function memoryForm(events,memory,id,through=Number.MAX_SAFE_INTEGER){
 let root=fingerprint({domain:'IDFBI_LIVED_FORM_1_7',origin:memory.origin,identity:id}),count=0;const counts=Array(6).fill(0),sums=Array(8).fill(0),marks=[];
 for(const e of events){if(e.seq<=memory.start||e.seq>through||e.identity!==Number(id))continue;const k=memoryCategory(e.kind);if(k<0||e.kind==='MEMORY_INSCRIBED'&&!e.data.form)continue;
  // Only typed event identity and content commitments; never decrypt or analyze prose.
  const data=Object.fromEntries(Object.entries(e.data).filter(([name])=>!['cause','causes','receipt'].includes(name)));
  const projection=fingerprint({kind:e.kind,identity:e.identity,actor:e.actor,custodian:e.custodian,epoch:e.epoch,time:e.time,data});
  root=fingerprint({domain:'IDFBI_FORM_EVENT_1_7',previous:root,projection});count++;counts[k]++;
  const weight=1/(1+counts[k]*.32);for(let i=0;i<8;i++){const b=parseInt(root.slice(2+i*6,8+i*6),16)/0xffffff;sums[i]=sums[i]*.86+(b*2-1)*weight;}
  const diversity=counts.filter(Boolean).length;const strength=.30*(1-Math.exp(-count/5))+.10*diversity/6;
  const traits=sums.map((v,i)=>Math.round(Math.tanh(v+(i===0?(counts[1]-counts[0])*.025:0))*strength*100000)/100000);
  marks.push({seq:e.seq,kind:e.kind,time:e.time,root,count,counts:[...counts],traits});
 }
 const last=marks.at(-1);return {version:'lived-field/1.7',identity:Number(id),root,count,counts,traits:last?.traits||Array(8).fill(0),marks};
}




const exitKeys=(v,names)=>{if(!v||Object.getPrototypeOf(v)!==Object.prototype||Object.keys(v).sort().join(',')!==names.split(',').sort().join(','))throw Error('Unexpected exit fields.');};
const exitInt=(n,min,max)=>{if(!Number.isSafeInteger(n)||n<min||n>max)throw Error('Exit schedule number is outside its supported range.');return n;};
const exitAmount=n=>{if(typeof n!=='string'||!/^[0-9]{1,34}$/.test(n)||BigInt(n)<1n||BigInt(n)>MAX112)throw Error('Enter a positive minimum in raw asset units.');return BigInt(n);};
const exitAdd=(a,asset,n)=>{const v=BigInt(a[asset]||0)+n;if(v<0n||v>MAX112)throw Error('Insufficient or excessive exit balance.');a[asset]=String(v);};
function normalizeExit(spec,cliffDays){
 exitKeys(spec,'asset,rows');if(!/^[A-Z][A-Z0-9]{1,11}$/.test(spec.asset)||!Array.isArray(spec.rows)||!spec.rows.length||spec.rows.length>64)throw Error('Choose an exit asset and 1–64 installments.');
 exitInt(cliffDays,1,3650);let last=-1,total=0;
 for(const row of spec.rows){exitKeys(row,'offsetDays,bps,minOut,graceDays');exitInt(row.offsetDays,0,3650-cliffDays);if(row.offsetDays<=last)throw Error('Installment dates must increase.');last=row.offsetDays;exitInt(row.bps,1,10000);total+=row.bps;exitAmount(row.minOut);exitInt(row.graceDays,1,365);}
 if(total!==10000)throw Error('Installment percentages must add up to exactly 100%.');return structuredClone(spec);
}
function exitSlices(total,spec,first){
 total=exitAmount(String(total));let assigned=0n;
 return spec.rows.map((r,i)=>{const n=i===spec.rows.length-1?total-assigned:total*BigInt(r.bps)/10000n;exitAmount(String(n));assigned+=n;return {index:i,amount:String(n),minOut:r.minOut,due:first+r.offsetDays*DAY,expires:first+(r.offsetDays+r.graceDays)*DAY,status:'pending',received:'0',receipt:0};});
}
/** Funded local specification. Time makes a slice eligible; only an execution creates a fill. */
class ExitEngine extends MemoryEngine {
 constructor(now,origin){super(now,origin);this.exits=[];}
 atomic(fn){const before=this.exits?structuredClone(this.exits):null;try{return super.atomic(fn);}catch(e){if(before)this.exits=before;throw e;}}
 moduleSnapshot(id){const base=super.moduleSnapshot(id),plans=(this.exits||[]).filter(p=>p.identity===Number(id));return plans.length?{...base,vestedExits:structuredClone(plans)}:base;}
 normalize(raw){if(!Object.hasOwn(raw||{},'exit'))return super.normalize(raw);exitKeys(raw,'kind,input,output,amount,minOut,days,recipient,exit');const {exit,...base}=raw,p=super.normalize(base);if(!['SWAP','LOCK'].includes(p.kind))throw Error('Only a swap or funded lock can create an exit.');const terms=normalizeExit(exit,p.days);if(terms.asset===p.output)throw Error('Choose a different asset to receive when selling.');for(const asset of [p.output,terms.asset])if(asset!=='ETH'&&!this.world.s.pools.some(pool=>pool.asset===asset))throw Error('This exit asset has no market.');return {...p,exit:terms};}
 publish(title,raw,parent=0){if(raw?.exit)throw Error('Exit schedules are funded individually; publish a plain instrument edition.');return super.publish(title,raw,parent);}
 apply(raw){if(!raw?.exit)return super.apply(raw);return this.atomic(()=>{
  const p=this.normalize(raw),{exit,...base}=p,w=this.world,a=w._owner(w.s.selected);if(this.exits.length>=256)throw Error('Export before creating more exit schedules.');
  let result,originReceipt=0;if(p.kind==='SWAP'){result=super.apply({...base,days:0});originReceipt=w.s.seq;}else result={received:p.amount,asset:p.output,lockId:0,releaseAt:0,steps:[]};
  const n=BigInt(result.received);exitAdd(a.balances,p.output,-n);
  const plan={id:this.exits.length+1,identity:a.id,epoch:a.epoch,owner:a.owner,input:p.output,output:exit.asset,total:String(n),created:w.s.now,cliffDays:p.days,terms:exit,originReceipt,paused:false,cancelled:false,slices:exitSlices(n,exit,w.s.now+p.days*DAY),receipt:0};
  const ev=this.record('EXIT_FUNDED',a.id,{exit:plan.id,plan:structuredClone(plan),causes:originReceipt?[originReceipt]:[]});plan.receipt=ev.seq;this.exits.push(plan);a.nonce++;
  return {...result,lockId:0,releaseAt:plan.slices[0].due,exitPlanId:plan.id,steps:p.kind==='SWAP'?['SWAP','VEST','SCHEDULE EXIT']:['VEST','SCHEDULE EXIT']};
 });}
 exitPlan(id){const p=this.exits.find(p=>p.id===Number(id));if(!p)throw Error('Unknown exit schedule.');return p;}
 exitStatus(p,s){const a=this.world.identity(p.identity),now=this.world.s.now;if(s.status!=='pending')return s.status;if(now<s.due)return 'vesting';if(p.cancelled)return 'recoverable';if(now>=s.expires)return 'expired';if(a.owner==='escrow'||a.epoch!==p.epoch||a.owner!==p.owner)return 'custody changed';return p.paused?'paused':'eligible';}
 changeExit(id,action){return this.atomic(()=>{const p=this.exitPlan(id),a=this.world._owner(p.identity);if(!['pause','resume','cancel','reauthorize'].includes(action))throw Error('Unknown exit instruction.');if(p.cancelled)throw Error('A cancelled schedule cannot restart.');if(action==='resume'&&(a.epoch!==p.epoch||a.owner!==p.owner))throw Error('Reauthorize the new custody before resuming.');if(action==='reauthorize'){if(a.owner==='escrow')throw Error('Exit authority cannot be granted in escrow.');p.epoch=a.epoch;p.owner=a.owner;p.paused=true;}else if(action==='cancel')p.cancelled=true;else p.paused=action==='pause';a.nonce++;this.record('EXIT_CONTROL',p.identity,{exit:p.id,action,epoch:p.epoch,owner:p.owner,causes:[p.receipt]});});}
 executeExit(id,index){return this.atomic(()=>{
  const p=this.exitPlan(id),s=p.slices[exitInt(Number(index),0,p.slices.length-1)],w=this.world;
  if(this.exitStatus(p,s)!=='eligible')throw Error('This installment is not eligible for sale.');
  const selection=w.s.selected,delegation=this._delegation;w.s.selected=p.identity;this._delegation={identity:p.identity,epoch:p.epoch};
  try{exitAdd(w.identity(p.identity).balances,p.input,BigInt(s.amount));const fill=w.swap(p.input,p.output,BigInt(s.amount),BigInt(s.minOut),0,s.expires-1),swap=w.s.seq;
   s.status='sold';s.received=String(fill.out);s.receipt=this.record('EXIT_SOLD',p.identity,{exit:p.id,index:s.index,amount:s.amount,received:s.received,swap,causes:[p.receipt,swap,...this.mem.bindings.filter(b=>b.receipt===p.originReceipt).map(b=>b.event)]}).seq;return structuredClone(s);
  }finally{w.s.selected=selection;this._delegation=delegation;}
 });}
 recoverExit(id,index){return this.atomic(()=>{const p=this.exitPlan(id),s=p.slices[exitInt(Number(index),0,p.slices.length-1)],w=this.world;if(s.status!=='pending'||w.s.now<s.due||(!p.cancelled&&w.s.now<s.expires))throw Error('Recovery waits until vesting and cancellation or expiry.');exitAdd(w.identity(p.identity).balances,p.input,BigInt(s.amount));s.status='recovered';s.receipt=this.record('EXIT_RECOVERED',p.identity,{exit:p.id,index:s.index,amount:s.amount,causes:[p.receipt]}).seq;return structuredClone(s);});}
 project(id,at){const result=super.project(id,at);for(const p of (this.exits||[]).filter(p=>p.identity===Number(id)))for(const s of p.slices.filter(s=>s.status==='pending')){const available=at>=s.due&&(p.cancelled||at>=s.expires)?s.amount:'0';result.claims.push({type:'vesting-exit',id:p.id,index:s.index,asset:p.input,escrow:s.amount,available,end:s.due});if(available!=='0')exitAdd(result.balances,p.input,BigInt(available));}result.scope='Scheduled claims and segregated exit tokens only; future sale proceeds are unknown and never projected.';return result;}
 export(){const body={schema:'idfbi/vested-exits/1',remembering:super.export(),exits:structuredClone(this.exits||[])};return {...body,checksum:fingerprint(body)};}
 static restore(data,origin){
  if(data?.schema!=='idfbi/vested-exits/1'){const e=MemoryEngine.restore(data,origin);Object.setPrototypeOf(e,ExitEngine.prototype);e.exits=[];if(e.world.s.events.some(x=>x.kind==='EXIT_FUNDED'))throw Error('Missing funded exit state.');return e;}
  exitKeys(data,'schema,remembering,exits,checksum');const {checksum,...body}=data;if(checksum!==fingerprint(body))throw Error('Exit archive checksum failed.');
  const e=MemoryEngine.restore(data.remembering,origin);Object.setPrototypeOf(e,ExitEngine.prototype);e.exits=structuredClone(data.exits);validateExits(e);return e;
 }
}
function validateExits(e){
 if(!Array.isArray(e.exits)||e.exits.length>256)throw Error('Invalid exit archive.');const usedSwaps=new Set(),events=e.world.s.events,funds=events.filter(x=>x.kind==='EXIT_FUNDED');if(funds.length!==e.exits.length)throw Error('Missing exit funding records.');
 for(let i=0;i<e.exits.length;i++){
  const p=e.exits[i],fund=funds[i];exitKeys(p,'id,identity,epoch,owner,input,output,total,created,cliffDays,terms,originReceipt,paused,cancelled,slices,receipt');
  if(p.id!==i+1||p.receipt!==fund.seq||fund.identity!==p.identity||fund.time!==p.created||fund.data.exit!==p.id)throw Error('Exit funding mismatch.');
  const initial=structuredClone(fund.data.plan);exitKeys(initial,'id,identity,epoch,owner,input,output,total,created,cliffDays,terms,originReceipt,paused,cancelled,slices,receipt');
  for(const asset of [initial.input,initial.output])if(typeof asset!=='string'||!/^[A-Z][A-Z0-9]{1,11}$/.test(asset)||asset!=='ETH'&&!e.world.s.pools.some(p=>p.asset===asset))throw Error('Invalid exit asset.');exitInt(initial.created,1,e.world.s.now);exitInt(initial.epoch,0,Number.MAX_SAFE_INTEGER);exitInt(initial.originReceipt,0,fund.seq-1);if(fund.actor!==fund.custodian)throw Error('Invalid exit funder.');normalizeExit(initial.terms,initial.cliffDays);exitAmount(initial.total);e.world.identity(initial.identity);if(initial.epoch!==fund.epoch||initial.owner!==fund.custodian||initial.owner==='escrow'||initial.input===initial.output||initial.terms.asset!==initial.output||initial.paused!==false||initial.cancelled!==false||initial.receipt!==0)throw Error('Invalid funded exit terms.');
  if(fingerprint(initial.slices)!==fingerprint(exitSlices(initial.total,initial.terms,initial.created+initial.cliffDays*DAY)))throw Error('Exit allocation mismatch.');
  if(initial.originReceipt){if(usedSwaps.has(initial.originReceipt))throw Error('Reused exit swap receipt.');usedSwaps.add(initial.originReceipt);const swap=events.find(x=>x.seq===initial.originReceipt);if(swap?.kind!=='SWAPPED'||swap.seq>=fund.seq||swap.identity!==p.identity||swap.actor!==fund.actor||swap.epoch!==fund.epoch||swap.custodian!==fund.custodian||swap.data.output!==initial.input||swap.data.received!==initial.total||swap.data.lock)throw Error('Exit source swap mismatch.');}
  initial.receipt=fund.seq;
  for(const event of events.filter(x=>x.seq>fund.seq&&x.data.exit===p.id&&x.kind.startsWith('EXIT_'))){
   if(event.identity!==p.identity||!event.data.causes?.includes(fund.seq))throw Error('Exit transition context mismatch.');
   if(event.kind==='EXIT_CONTROL'){const d=event.data;if(initial.cancelled||event.actor!==event.custodian||event.custodian==='escrow')throw Error('Invalid exit controller.');if(d.action==='reauthorize'){if(d.epoch!==event.epoch||d.owner!==event.custodian)throw Error('Invalid exit reauthorization.');initial.epoch=d.epoch;initial.owner=d.owner;initial.paused=true;}else if(d.action==='cancel')initial.cancelled=true;else if(['pause','resume'].includes(d.action))initial.paused=d.action==='pause';else throw Error('Invalid exit control');if(initial.epoch!==d.epoch||initial.owner!==d.owner)throw Error('Changed exit authority.');continue;}
   const s=initial.slices[event.data.index];if(!s||s.status!=='pending'||event.time<s.due||event.data.amount!==s.amount)throw Error('Duplicate, early or changed exit installment.');
   if(event.kind==='EXIT_SOLD'){
    if(usedSwaps.has(event.data.swap)||!event.data.causes.includes(event.data.swap))throw Error('Reused or unlinked exit fill.');usedSwaps.add(event.data.swap);const swap=events.find(x=>x.seq===event.data.swap);if(initial.cancelled||initial.paused||event.time>=s.expires||event.epoch!==initial.epoch||event.custodian!==initial.owner||swap?.kind!=='SWAPPED'||swap.identity!==p.identity||swap.actor!==event.actor||swap.epoch!==event.epoch||swap.custodian!==event.custodian||swap.seq>=event.seq||swap.time!==event.time||swap.data.input!==initial.input||swap.data.output!==initial.output||swap.data.amount!==s.amount||swap.data.received!==event.data.received||BigInt(event.data.received)<BigInt(s.minOut)||swap.data.lock)throw Error('Invalid exit fill.');s.status='sold';s.received=event.data.received;
   }else if(event.kind==='EXIT_RECOVERED'){if(!initial.cancelled&&event.time<s.expires)throw Error('Early exit recovery.');s.status='recovered';}else throw Error('Unknown exit transition.');s.receipt=event.seq;
  }
  if(fingerprint(initial)!==fingerprint(p))throw Error('Stored exit differs from its funding and execution receipts.');
 }
}

const LIVED_ASSETS={"shader":"#version 300 es\nprecision highp float;precision highp int;\n#define V3 vec3\n#define V4 vec4\n#define v3 vec3\n#define TOU(x) uint(x)\n#define TOF(x) float(x)\n#define TOI(x) int(x)\n#define atan2(y,x) atan(y,x)\nuniform vec2 resolution;uniform float center;uniform vec4 seed,genome,root,life0,life1;uniform float t,sovereign,pulse,fold,cameraX,cameraY,zoom,pointerX,pointerY,eventKind,lens;out vec4 outColor;\n// Shared optical field. Compiled to GLSL ES 3 and WebAssembly from the same source.\n// All fields are procedural; no photographs, baked textures, external media or mesh.\nfloat saturate(float x){return min(1.0,max(0.0,x));}\nfloat fract1(float x){return x-floor(x);}\nfloat mix1(float a,float b,float t0){return a+(b-a)*t0;}\nfloat ease(float a,float b,float x){float q=saturate((x-a)/(b-a));return q*q*(3.0-2.0*q);}\nV3 add3(V3 a,V3 b){return v3(a.x+b.x,a.y+b.y,a.z+b.z);}\nV3 mul3(V3 a,float b){return v3(a.x*b,a.y*b,a.z*b);}\nV3 mix3(V3 a,V3 b,float f){return add3(mul3(a,1.0-f),mul3(b,f));}\nfloat dot3(V3 a,V3 b){return a.x*b.x+a.y*b.y+a.z*b.z;}\nfloat length3(V3 a){return sqrt(dot3(a,a));}\nV3 rotate3(V3 p,float x,float y){float cx=cos(x),sx=sin(x),cy=cos(y),sy=sin(y);V3 q=v3(p.x,p.y*cx-p.z*sx,p.y*sx+p.z*cx);return v3(q.x*cy+q.z*sy,q.y,-q.x*sy+q.z*cy);}\nfloat lattice(int x,int y,int z){uint h=TOU(x)*1597334677u ^ TOU(y)*3812015801u ^ TOU(z)*2798796415u;h^=h>>16;h*=2246822519u;h^=h>>13;h*=3266489917u;h^=h>>16;return TOF(h&16777215u)/16777215.0;}\nfloat noise3(V3 p){int x=TOI(floor(p.x)),y=TOI(floor(p.y)),z=TOI(floor(p.z));float a=fract1(p.x),b=fract1(p.y),c=fract1(p.z);a=a*a*(3.0-2.0*a);b=b*b*(3.0-2.0*b);c=c*c*(3.0-2.0*c);return mix1(mix1(mix1(lattice(x,y,z),lattice(x+1,y,z),a),mix1(lattice(x,y+1,z),lattice(x+1,y+1,z),a),b),mix1(mix1(lattice(x,y,z+1),lattice(x+1,y,z+1),a),mix1(lattice(x,y+1,z+1),lattice(x+1,y+1,z+1),a),b),c);}\nfloat fbm(V3 p){float a=0.56,f=0.0;for(int i=0;i\u003c4;i++){f+=noise3(p)*a;p=add3(mul3(p,2.07),v3(13.1,7.7,3.8));a*=0.48;}return f;}\n// The original's repeated abs / inversion fold, kept as an optical microstructure.\n// The archived GLSL in original.frag is additionally selectable unmodified.\nfloat originalTrap(V3 p){float trap=10.0;float ax=.36+genome.x*.8+sin(t*.07)*.04,ay=.22+root.y*.55;float cx=cos(ax),sx=sin(ax),cy=cos(ay),sy=sin(ay);for(int i=0;i\u003c8;i++){float a=p.x*cx+p.y*sx,b=p.y*cx-p.x*sx;p.x=a;p.y=b;a=p.y*cy+p.z*sy;b=p.z*cy-p.y*sy;p.y=a;p.z=b;p=v3(abs(p.x)-(.46+.12*seed.x),abs(p.y)-(.38+.08*genome.z),abs(p.z)-(.34+.1*root.w));float k=max(.18,min(1.15,dot3(p,p)));p=add3(mul3(p,1.0/k),v3(-.76-.17*seed.z,-.55-.14*root.x,-.43-.1*genome.y));trap=min(trap,sqrt(p.x*p.x+p.y*p.y));}return trap;}\nV3 spectrum(float u){V3 cold=v3(.11,.65,.83),violet=v3(.42,.16,.72),warm=v3(1.0,.39,.17);float blend=saturate(sin(u*5.4+genome.z*3.0)*.5+.5);V3 col=mix3(cold,violet,blend*.58);col=mix3(col,warm,ease(.75,1.0,blend)*.65);return mix3(col,v3(.96,.17,.33),sovereign*.68);}\nV3 radiance(float x,float y){\n float z0=max(.48,zoom);float u=x*z0, v=y*z0;u+=(pointerX-.5)*.045;v+=(pointerY-.5)*.035;\n\n // A bounded, versioned life deformation. Zero traits preserve the approved field.\n float lived=abs(life0.x)+abs(life0.y)+abs(life0.z)+abs(life0.w)+abs(life1.x)+abs(life1.y)+abs(life1.z)+abs(life1.w);\n if(lived>0.000001){\n  float aa=atan2(v,u),rr=sqrt(u*u+v*v);\n  float lobes=1.0+life0.y*.42*sin(aa*3.0+life1.z*3.0)+life0.z*.35*cos(aa*5.0+life1.w*2.0);\n  float turn=life0.w*.85*(1.0-exp(-rr*rr*1.7));\n  float cu=cos(turn),su=sin(turn),oldU=u;\n  u=(u*cu-v*su)/((1.0+life0.x*.6)*lobes);\n  v=(oldU*su+v*cu)/((1.0-life0.x*.45)*lobes);\n }\n float radial=sqrt(u*u+v*v),angle=atan2(v,u);\n float clock=t*.08, breath=1.0+sin(t*.7)*.025;\n float effect=pulse, rupture=eventKind==4.0?effect:0.0;\n float radius=.91*breath*(1.0-rupture*.42)+.035*sin(angle*3.0+clock);\n float foldAngle=fold*.7+cameraY*.7;\n V3 p=rotate3(v3(u,v,.32),cameraX+fold*.3,cameraY+t*.035);\n // Advected domain warping: veils, not a polygonal shell.\n V3 q=add3(mul3(p,2.6),v3(seed.x*13.0+genome.x*3.7,seed.y*17.0+genome.y*3.1,seed.z*11.0+genome.z*4.3+clock));\n float warp=fbm(q);q=add3(q,v3(warp*1.7,warp*.8,-warp*1.3));\n float cloud=fbm(q);float cloud2=noise3(add3(mul3(q,3.2),v3(clock,genome.w*8.0,-clock)));\n float edge=radius+(.5-cloud)*.26+fold*.045*sin(angle*5.0+t*.1);\n float delta=radial-edge;\n float halo=exp(-abs(delta)*5.0);\n float mist=halo*pow(max(.0,cloud),2.6)*1.1;\n V3 col=v3(.002,.0025,.008);\n col=add3(col,mul3(v3(.2,.09,.41),exp(-radial*radial*.65)*.27));\n col=add3(col,mul3(spectrum(cloud+angle*.05),mist));\n // Front-to-back absorption / emission through a bounded participating medium.\n float disk=1.24*1.24-u*u-v*v;\n if(disk>0.0){\n  float far=sqrt(disk),stepLength=2.0*far/18.0,transmit=1.0;\n  for(int j=0;j\u003c18;j++){\n   float z=-far+(TOF(j)+.5)*stepLength;\n   V3 sampleP=rotate3(v3(u,v,z),cameraX,cameraY+t*.028);\n   if(lived>0.000001){\n    float bend=life1.x*.7*sampleP.z+life1.y*.28*sin(sampleP.y*3.0);\n    float xx=sampleP.x;\n    sampleP.x=xx*cos(bend)-sampleP.y*sin(bend);\n    sampleP.y=xx*sin(bend)+sampleP.y*cos(bend);\n    sampleP.z+=life1.y*.3*sin(xx*3.0+sampleP.y*2.0);\n   }\n   // A fourth-coordinate slice rotates into the existing three-dimensional field.\n   float w=.35*sin(sampleP.y*2.0+clock);float xp=sampleP.x*cos(foldAngle)-w*sin(foldAngle);\n   sampleP.x=xp;\n   sampleP=mul3(sampleP,1.0+rupture*1.2);\n   // Genome changes the density domain and anisotropy, not just the light palette.\n   float r=length3(v3(sampleP.x/(.88+genome.x*.24),sampleP.y/(.88+genome.y*.24),sampleP.z/(.9+genome.z*.2)));\n   V3 np=add3(mul3(sampleP,2.7+genome.w*1.2),v3(seed.x*9.0+genome.y*4.0,seed.y*9.0+genome.z*3.0+clock,seed.w*9.0+genome.x*4.5));\n   float n=fbm(np);\n   float veil=abs(r-(.74+.31*n));\n   float density=exp(-veil*35.0)*(.23+n*.9);\n   density*=.48+.52*pow(saturate(noise3(mul3(np,3.8))*1.5),3.0);\n   density*=ease(1.3,1.05,r);\n   float opacity=1.0-exp(-density*stepLength*2.7);\n   V3 light=spectrum(n+sampleP.y*.18+genome.y*.3);\n   float hot=pow(saturate(n*1.35),4.0);\n   light=add3(mul3(light,.8+hot*2.2),mul3(v3(.49,.94,1.0),hot*.55*(1.0-sovereign*.45)));\n   col=add3(col,mul3(light,transmit*opacity));transmit*=1.0-opacity;\n  }\n }\n // Orbit-trap detail from the approved renderer lights the corona.\n float trap=originalTrap(v3(u*.75,v*.75,.43+genome.w));\n float fine=1.0/(1.0+120.0*trap*trap);\n float rim=exp(-abs(delta)*48.0)*(cloud*.75+cloud2*.4+.14);\n col=add3(col,mul3(spectrum(angle*.05+cloud),rim*(1.4+fine*1.7)));\n // A hot nucleus with wavelength-separated diffraction. Not an opaque white disk.\n float core=exp(-radial*radial*37.0)*(.35+.2*sin(t*.65));\n float nucleus=.0026/(.009+radial*radial);\n col=add3(col,mul3(mix3(v3(.35,.9,1.0),v3(1.0,.25,.42),sovereign),core+nucleus));\n float ray=exp(-abs(v+sin(u*4.0+t*.2)*.008)*125.0)*exp(-abs(u)*2.2);\n col=add3(col,mul3(v3(.35,.62,.88),ray*.13));\n // No uniform flash: events travel through the body and settle into a new state.\n float front=(1.0-effect)*1.7;\n float wave=exp(-abs(radial-front)*34.0)*effect;\n V3 eventColor=eventKind==2.0?v3(.67,.3,1.0):eventKind==3.0?v3(1.0,.56,.2):eventKind==4.0?v3(1.0,.18,.38):v3(.2,.95,1.0);\n col=add3(col,mul3(eventColor,wave*.75));\n if(eventKind==3.0){float jet=exp(-abs(v-u*.38)*90.0)*ease(.0,.3,u)*ease(1.5,.1,u);col=add3(col,mul3(eventColor,jet*effect*.55));}\n if(lens==2.0)col=mix3(col,v3(col.z*.8,col.x*.45,col.y*1.4),.55);\n // Filaments beyond the shell leave the edges dark, retaining the original atmosphere.\n float filament=pow(saturate(1.0-abs(cloud-.49)*22.0),7.0)*exp(-abs(delta-.14)*6.0);\n col=add3(col,mul3(spectrum(cloud+angle*.1),filament*.22));\n float star=lattice(TOI(floor(x*570.0)),TOI(floor(y*570.0)),47);\n if(star>.9992){float flicker=.55+.45*sin(t*.8+star*70.0);col=add3(col,mul3(v3(.51,.58,.82),flicker*.44));}\n float falloff=exp(-pow(radial/2.5,4.0));col=mul3(col,falloff);\n // Filmic-like exposure: preserve colored light rather than clamp to pure white.\n col=v3(1.0-exp(-col.x*1.55),1.0-exp(-col.y*1.55),1.0-exp(-col.z*1.55));\n return v3(pow(max(0.0,col.x),.88),pow(max(0.0,col.y),.88),pow(max(0.0,col.z),.88));\n}\n\nvoid main(){vec2 uv=(gl_FragCoord.xy-vec2(resolution.x*.5,resolution.y*(1.-center)))/resolution.y*3.2;outColor=vec4(radiance(uv.x,uv.y),1.);}","wasm":""};
const IX_CATALOG={"schema":"anima/capability-catalog/5.9","deployment":null,"mainnetEnabled":false,"warning":"Local EVM and service tests verify this source edition. Extension deployments and named external services must be configured before use; no public-chain deployment is claimed.","modules":[{"id":"launch","label":"Launch + token commons","state":"LOCAL_AND_COMPILED_SOURCE","dependencies":["collection","account","ledger","vault","v4-market","hook"],"authority":"account owner in Bound mode; no new sovereign bypass","privacy":"public","exit":"withdraw before close; failure refund","economics":"batch/pro-rata; not CCA","explanation":"Design a token sale in the local rehearsal. The separate v4 launch desk can create a real token and pool after its contracts are deployed.","sources":["contracts/src/protocol/GenesisLaunchpad.sol","web/instruments/engine.mjs"]},{"id":"swap","label":"Swap + optional output lock","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","dependencies":["market","vault"],"authority":"exact reviewed input and minimum output","privacy":"public","exit":"atomic revert; no post-swap undo","economics":"Typed NativeMarket quote and execution; the separate v4 desk uses its configured v4 router.","explanation":"Trade one kind of token for another. You choose what to spend and the smallest amount you will accept back.","sources":["web/genesis/live-protocol.mjs","contracts/src/protocol/NativeMarket.sol"]},{"id":"gift","label":"Consent-based future gift","state":"LOCAL_AND_COMPILED_SOURCE","dependencies":["sealed ledger","market","vault"],"authority":"donor funds; recipient accepts; fixed beneficiary","privacy":"public","exit":"donor recall until acceptance; permissionless expiry return; no early exit after acceptance","explanation":"Put aside a gift for someone. They choose whether to accept it, and you choose when it becomes available.","sources":["contracts/src/instruments/ConsentGiftRouter.sol"]},{"id":"marketplace","label":"Declared-content artifact sale","state":"LOCAL_AND_COMPILED_SOURCE","dependencies":["account","exchange","vault","immutable commitment index","edition and account revision","known root and cell obligations"],"authority":"escrow and two inventory checks","privacy":"public","exit":"cancel listing; no reversal of completed purchase","limitations":"Bounded declared module universe, not discovery of all external liabilities. Local exact hashes differ from contract predicates.","explanation":"Sell the main NFT together with the belongings and promises that its configured inventory knows about.","sources":["contracts/src/kingdom/EstateExchange.sol"]},{"id":"world","label":"Rooms, invitations, historical authorship","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","dependencies":["ledger"],"authority":"personal filters separate from room administration","privacy":"public text, including member-gated rooms","exit":"leave/mute without deleting others' history","explanation":"Read public messages, make rooms, invite members, accept an invitation, leave, and manage your room through reviewed NFT transactions.","sources":["contracts/src/protocol/WorldLedger.sol","web/genesis/live-protocol.mjs","web/genesis/live-desk.mjs"],"limitations":"Pinned public room/message reads and reviewed room operations. Invitations control posting membership, not secrecy. No encrypted group protocol."},{"id":"cca","label":"Continuous clearing auction","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Keep a standing bid while new sale inventory is released. Claim purchased tokens and recover unused bids.","sources":["contracts/src/extensions/markets/ContinuousClearingAuction.sol","web/extensions/markets.mjs"],"dependencies":["reviewed extension deployment","connected signer"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Independent ANIMA uniform-price streaming mechanism with 64 standing orders. No Uniswap CCA compliance or automatic liquidity migration.","entrance":"Atlas → New instruments"},{"id":"private-agent","label":"Encrypted memory handover","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Encrypt an agent memory for its recipient. They decrypt and verify it before accepting the NFT in one transaction.","sources":["contracts/src/extensions/privacy/PrivateMemoryHandover.sol","web/extensions/privacy.mjs"],"dependencies":["reviewed extension deployment","connected signer"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Recipient-confirmed content verification, not ERC-7857 or zero-knowledge handover. Old content may remain with the seller.","entrance":"Atlas → New instruments"},{"id":"agent-execution","label":"Continuous scoped agent","state":"LOCAL_SERVICE_AND_CONTRACTS","explanation":"Let a running operator repeat an exact owner-approved action with a spending limit, waiting time and expiry.","sources":["contracts/src/extensions/agents/AgentPolicyGuard.sol","web/extensions/agents.mjs"],"dependencies":["reviewed extension deployment","configured local or hosted service"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Bound-account scoped operator. Arbitrary autonomous decision-making and Sovereign-mode proof generation are not supplied.","entrance":"Atlas → New instruments"},{"id":"omnichain","label":"Authenticated cross-chain witnesses","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Carry an NFT state observation through a sealed authenticated route without granting remote spending power.","sources":["contracts/src/extensions/privacy/AuthenticatedStatePortal.sol","web/extensions/privacy.mjs"],"dependencies":["reviewed extension deployment","connected signer"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Requires real LayerZero endpoints, explicit DVNs/libraries/executor configuration and peer deployments. Tests use local endpoint doubles.","entrance":"Atlas → New instruments"},{"id":"private-chat","label":"Encrypted group chat","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Invite people, rotate private group keys and send messages only current key holders can decrypt.","sources":["contracts/src/extensions/privacy/EpochGroupChat.sol","web/extensions/privacy.mjs"],"dependencies":["reviewed extension deployment","connected signer"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Custom WebCrypto epoch protocol, not MLS. Membership and timing are public; removed members retain old messages.","entrance":"Atlas → New instruments"},{"id":"public-goods","label":"Funded public-goods matching","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Add a funded matching pot to capped contributions and pay each project its calculated share.","sources":["contracts/src/extensions/markets/PublicGoodsMatching.sol","web/extensions/markets.mjs"],"dependencies":["reviewed extension deployment","connected signer"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"A disclosed immutable registrar attests unique people; this does not cryptographically eliminate Sybil identities or collusion.","entrance":"Atlas → New instruments"},{"id":"sponsored-actions","label":"Sponsored account actions","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Have a relayer pay transaction gas using an exact owner-and-sponsor signed request and the sponsor’s funded budget.","sources":["contracts/src/extensions/access/SessionSponsor.sol","web/extensions/access.mjs"],"dependencies":["reviewed extension deployment","connected signer"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Native account-session relay, not an ERC-4337 paymaster. Capped reimbursement with fixed overhead may not cover the relayer’s whole cost.","entrance":"Atlas → New instruments"},{"id":"library","label":"Publish, adopt and grant an instrument","tier":"core","state":"LOCAL_AND_COMPILED_SOURCE","dependencies":["edition registry","custody epoch","finite router"],"authority":"Public code is not permission. Seven-day adoption; exact caller, calldata, target, per-call/lifetime ceiling and expiry.","exit":"Immediate revocation; custody change invalidates grant.","limitations":"Local recipes include the existing action vocabulary. Dedicated onchain router implements swap/direct lock; no arbitrary bytecode or AI executor.","explanation":"Publish an instrument recipe, adopt an edition, and separately give an exact, limited permission to use funds.","sources":["contracts/src/operating/EditionRegistry.sol","contracts/src/operating/InstrumentRouter.sol"]},{"id":"commission","label":"Prefunded work and acceptance","tier":"core","state":"LOCAL_AND_COMPILED_SOURCE","dependencies":["commission escrow","fixed reviewer","deliverable hash"],"authority":"Worker accepts a fixed personal reviewer and deadline/refund policy. Selling the NFT does not change either person.","exit":"Payment on acceptance or refund after rejection/timeout.","limitations":"Human judgment is not verified task correctness. No appeals or automatic compensation. Source stores a deliverable digest; local model additionally binds its edition author.","explanation":"Set aside a fixed payment for a job, choose the worker and reviewer, and pay or refund after the decision or deadline.","sources":["contracts/src/operating/CommissionEscrow.sol"]},{"id":"shelves","label":"Artifact-owned bonded sell shelves","tier":"core","state":"LOCAL_AND_COMPILED_SOURCE","dependencies":["item custody","fixed ask","fixed bond"],"authority":"Item and proceeds held separately; market operating rights follow the artifact.","exit":"After fixed bond, return unsold item or withdraw proceeds.","limitations":"One fixed-price sell shelf; not a buy wall, two-sided AMM or a whole-artifact exchange. Rehearsal collectibles are fictional.","explanation":"Put one item on a fixed-price shelf. Later collect the sale money or take back the unsold item after its waiting period.","sources":["contracts/src/operating/BondedShelf.sol"]},{"id":"commitments","label":"Commitment-aware artifact exchange","tier":"core","state":"LOCAL_AND_COMPILED_SOURCE","dependencies":["module adapters","cell factory","pre/post transfer checks"],"authority":"Listing commits to known gifts, grants, work, cells, venues and schedules.","exit":"Seller refreshes a changed manifest; old buyer review cannot execute.","limitations":"New deployments and explicit configuration required. Old immutable accounts and exchanges are unchanged.","explanation":"Inspect the promises attached to an NFT before selling it. The sale checks the configured promises again.","sources":["contracts/src/operating/CommitmentIndex.sol"]},{"id":"granary","label":"Strategy-share conversion","tier":"experimental","state":"LOCAL_AND_COMPILED_SOURCE","dependencies":["isolated cell","reviewed ERC-4626 vault","minimum outputs"],"authority":"Only explicitly funded cell assets; recipient pinned to that cell.","exit":"Redeem subject to the underlying strategy; no promise of liquidity.","limitations":"Local model is not a live strategy and promises no yield. External vault solvency remains separate.","explanation":"An experimental adapter exchanges assets for shares in a chosen vault. It cannot promise profits or instant withdrawal.","sources":["contracts/src/operating/ExperimentalAdapters.sol"]},{"id":"issue","label":"Fixed basket issuance","tier":"experimental","state":"LOCAL_AND_COMPILED_SOURCE","dependencies":["isolated cell","two supported assets","ceil/floor accounting"],"authority":"Round deposits up and redemptions down; measure exact receipts.","exit":"Redeem shares against specified backing.","limitations":"Not the original underfunded Issue implementation. Simplified two-asset basket; source dust has a fixed beneficiary.","explanation":"An experimental two-asset basket lets people deposit the required ingredients and redeem shares for backing.","sources":["contracts/src/operating/ExperimentalAdapters.sol"]},{"id":"strips","label":"Matched principal / income study","tier":"experimental","state":"LOCAL_AND_COMPILED_SOURCE","dependencies":["isolated cell","matched share accounting"],"authority":"Proportional recombination of matched pairs including losses.","exit":"Return proportional remaining backing; no nominal-principal priority.","limitations":"Not separately tradable principal/yield tokens or a live yield market.","explanation":"An experimental accounting study separates matched claims and recombines them against the remaining backing.","sources":["contracts/src/operating/ExperimentalAdapters.sol"]},{"id":"counter","label":"Negotiated collateralized credit","tier":"experimental","state":"LOCAL_AND_COMPILED_SOURCE","dependencies":["isolated cell","escrow collateral","fixed dates"],"authority":"Named parties, collateral and exact repayment; no oracle or root-NFT pawn authority.","exit":"Repay before due; collateral to lender at/after default deadline.","limitations":"Not flash liquidity, dynamic liquidation, unrestricted lending or a safety endorsement.","explanation":"An experimental loan agreement records the lender, borrower, collateral, repayment amount, and due date.","sources":["contracts/src/operating/NegotiatedMarkets.sol"]},{"id":"scrivener","label":"Physically covered calls","tier":"experimental","state":"LOCAL_AND_COMPILED_SOURCE","dependencies":["isolated cell","escrow cover","fixed strike/premium"],"authority":"Full cover at creation; only named holder exercises before expiry.","exit":"Writer cancellation before sale; unsatisfied expiry returns cover.","limitations":"No naked options, oracle pricing or promised fair premium.","explanation":"An experimental covered option reserves the asset first, then gives a named holder the choice to buy before expiry.","sources":["contracts/src/operating/NegotiatedMarkets.sol"]},{"id":"site-editions","label":"Application-edition commitments","tier":"core","state":"LOCAL_ONLY","dependencies":["content hash","byte count","prepared immutable archive"],"authority":"Records identify a version; no uploaded program is evaluated.","exit":"Read-only version history.","limitations":"The local Applications panel stores commitments. Full immutable application storage is the separate onchain-runtime capability. No public-chain deployment is claimed.","explanation":"The local Applications panel records a version fingerprint. The separate immutable runtime archive stores the actual application bytes.","sources":["web/operating/engine.mjs"]},{"id":"house","label":"House leveraged spot positions","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Borrow into an isolated real spot position, repay or sell it, and divide losses by an explicit order.","sources":["contracts/src/extensions/experimental/House.sol","web/extensions/experimental.mjs"],"dependencies":["reviewed extension deployment","connected signer"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Test-network gate; lender-funded isolated spot leverage, not the original pooled synthetic book. Oracle, venue and liquidity are external.","entrance":"Atlas → New instruments"},{"id":"wager","label":"Wager outcome markets","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Fund and trade binary outcomes, challenge an answer and reclaim funds if the named authority misses its deadline.","sources":["contracts/src/extensions/experimental/Wager.sol","web/extensions/experimental.mjs"],"dependencies":["reviewed extension deployment","connected signer"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Test-network gate. Question, source, resolver, arbiter and all cutoffs are fixed per market; those authorities can be wrong.","entrance":"Atlas → New instruments"},{"id":"wake","label":"Wake keeper windows","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Prepay a keeper seat, compensate a replaced keeper and allow public help when the holder stalls.","sources":["contracts/src/extensions/experimental/Wake.sol","web/extensions/experimental.mjs"],"dependencies":["reviewed extension deployment","connected signer"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Test-network gate. Exclusivity covers the adapter route; underlying permissionless exits remain callable.","entrance":"Atlas → New instruments"},{"id":"memory","label":"Decision journal and general memories","tier":"core","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","dependencies":["append-only envelope","exact-plan binding","actual successful fill","original authorship"],"authority":"Writing does not authorize a trade. Reflections belong to the original author; NFT sale does not transfer a decryption key.","privacy":"Explicit public body or client-encrypted AES-GCM body. Headers and timing are not confidential.","exit":"Export a complete archive; keep the passphrase separately. No recovery from the discarded original Seal Memory.","limitations":"Local encrypted journal and configurable public onchain memory desk. Keys do not transfer with the NFT. No funded public deployment is claimed.","explanation":"Keep a decision or memory, add later reflections, or deliberately publish a public memory to the NFT ledger.","sources":["web/memory/engine.mjs","contracts/src/memory/MemoryLedger.sol","web/genesis/live-protocol.mjs"]},{"id":"lived-form","label":"Activity-shaped optical form","tier":"presentation","state":"LOCAL_ONLY","dependencies":["admitted local receipts","versioned bounded reducer","shared GLSL/Wasm field"],"authority":"A view projection, not account authority, rarity, reputation or financial value.","privacy":"No interpretation or display of private words. Optional memory imprint can be omitted; action metadata remain visible.","exit":"Compare original or inspect an earlier activity layer without undoing actions.","limitations":"History uses the current base genome. Finite visual parameters are not collision-resistant identity. Full protocol-wide onchain event admission, reorg handling and metadata linkage are not implemented.","explanation":"Local activity adds a bounded visual layer to the blue object. This decoration grants no extra spending rights.","sources":["web/memory/renderer-bridge.js","web/memory/engine.mjs"]},{"id":"journal-swap-source","label":"Atomic onchain journaled swap","tier":"protocol","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","dependencies":["MemoryLedger","JournalSwapRouter","installed NativeMarket","NFT account execution","explicit public-text consent"],"authority":"Exact nonce, input, minimum output, deadline, temporary allowance and caller-account recipient.","privacy":"Contract stores full public bytes or supplied ciphertext; it receives no decryption key and cannot certify encryption.","limitations":"Typed NativeMarket wallet path and local EVM tests exist. The live desk publishes a consented public note; its optional local output-lock model is not an atomic journaled-swap-and-lock route.","explanation":"Make the trade and write its note in one transaction, so the note cannot claim a trade that failed.","sources":["contracts/src/memory/JournalSwapRouter.sol","web/genesis/live-protocol.mjs"]},{"id":"vesting-exits","label":"Vesting exits + decision receipts","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","dependencies":["funded exit escrow","compatible market","separate live executor"],"authority":"fixed input, per-slice minimum, expiry and NFT recipient; owner epoch bound","exit":"cancel sales; recover only matured cancelled/expired slices","limitations":"local clock does not execute; live deployment and keeper not configured","explanation":"Fund a schedule that sells only eligible matured slices, with your minimum prices and deadline. A running keeper must trigger automatic sales.","sources":["contracts/src/protocol/VestedExitVault.sol","web/exit/live.mjs","agent/exit-keeper.mjs"]},{"id":"mint-sanctuary","label":"Mint sanctuary","state":"RESTRICTED_WALLET","dependencies":["encrypted backup","trusted Ethereum-model RPC","supported direct collection mint"],"authority":"independent EOA, exact reviews, narrow calldata grammar and application budget","limitations":"cannot observe other browser tabs; exported keys or compromised browser can bypass app caps","explanation":"Use a separate small wallet for a project mint, with encrypted backup and a review of each supported action.","sources":["web/burners/desk.mjs","web/burners/policy.mjs","web/burners/vault.mjs"]},{"id":"original-nft","label":"Original NFT identity and evolution","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Mint an identity, evolve it, commit a memory, add entropy, create a descendant, or review an irreversible authority change. The original local preview also rehearses these actions.","sources":["contracts/src/core/IDontFuckingBelieveIt.sol","web/model.mjs"],"dependencies":["compatible collection","owner wallet or required verifier"],"authority":"Ownership and proof rules are enforced by the deployed contracts.","limitations":"A visually similar projection is possible. The identity digest is authoritative; shared seeds and downloaded app code are public."},{"id":"nft-account","label":"NFT-controlled account and permissions","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Your NFT has an account that can hold assets. Its current owner can review calls and give or revoke narrowly scoped permissions.","sources":["contracts/src/core/SovereignAccount.sol","web/confluence/wallet.mjs"],"dependencies":["canonical collection account","current owner"],"authority":"Content, a game, and a model suggestion grant no spending authority.","limitations":"Same-collection master NFT nesting is forbidden. Private wallet secrets and burner keys are separate from NFT custody."},{"id":"vault-live","label":"Cliff and linear token locks","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Put coins in a time box. Open it at one chosen time, or let the available amount grow gradually.","sources":["contracts/src/protocol/TimeVault.sol","web/genesis/live-protocol.mjs"],"dependencies":["sealed TimeVault","owner wallet"],"authority":"The lock beneficiary and vesting math control releases.","exit":"Release the matured amount; the interface cannot make an immature amount available."},{"id":"interior","label":"Blue-object interior and controls","state":"IMPLEMENTED_BROWSER","explanation":"Travel inside the blue object, look around, move in and out, and return to your place after using a function.","sources":["web/genesis/interior.mjs","web/genesis/interior-space.mjs","web/genesis/interior-quality.mjs"],"dependencies":["compatible browser"],"limitations":"Pinch, double-tap, two-finger pan, keyboard and adaptive rendering are implemented. Supported physical phone and wallet combinations still need testing."},{"id":"v4-exchange","label":"v4 swaps, launch and liquidity redemption","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Create a fixed-supply token and a pool, trade through it, and redeem liquidity shares for their available assets.","sources":["web/v4/desk.mjs","web/v4/client.mjs","web/v4/public-session.mjs"],"dependencies":["deployed compatible v4 stack","PoolManager","quote services","wallet"],"authority":"Owner-reviewed public session or separately controlled shielded wallet.","privacy":"Public pools and tokens; shielded execution requires the private stack.","limitations":"Source integration exists. Public-chain deployment and funded shielded lifecycle validation remain outstanding."},{"id":"private-wallet","label":"Encrypted private wallet and screen lock","state":"IMPLEMENTED_BROWSER","explanation":"Keep recovery information, notes and launch drafts encrypted. Lock the screen or restore an encrypted backup.","sources":["web/privacy/vault.mjs","web/privacy/bridge.mjs","web/v4/desk.mjs"],"dependencies":["browser cryptography","separately saved recovery backup"],"privacy":"Secrets stay offchain. NFT transfer does not transfer the private wallet secret.","limitations":"Locking does not cancel a transaction already broadcast or erase data retained by another application."},{"id":"shielded-execution","label":"Shielded execution integration","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Choose the private route so supported transactions use your shielded funds. If the route cannot work, it stops instead of silently sending publicly.","sources":["web/privacy/bridge.mjs","web/v4/client.mjs"],"dependencies":["RAILGUN runtime","funded shielded wallet","compatible deployments","circuit artifacts","RPC and proof services","broadcaster"],"privacy":"Public launch token and pool remain observable. No guarantee of anonymous timing, funding or device activity.","limitations":"No funded end-to-end shielded transaction has been verified in this project."},{"id":"owner-fees","label":"Owner-chosen fee distribution","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Choose fee recipients, shares and supported payout assets, then review the contract configuration.","sources":["contracts/src/confluence/fees/OwnerFeeRouter.sol","contracts/src/confluence/fees/OwnerLaunchFactory.sol","web/confluence/app.js"],"dependencies":["deployed compatible owner hook","factory","supported asset converter when needed"],"authority":"No mandatory project recipient; exact routing is owner-controlled.","limitations":"A saved routing definition alone deploys nothing. This hook/factory path is separate from the v4 exchange stack and requires its own setup."},{"id":"rehearsal","label":"Pinned-block transaction rehearsal","state":"LOCAL_SERVICE_AND_CONTRACTS","explanation":"Try a supported public action on a disposable copy of the blockchain and inspect what changed before signing.","sources":["agent/rehearsal/engine.mjs","web/rehearsal/client.mjs"],"dependencies":["local authenticated rehearsal service","Anvil","historical source RPC"],"privacy":"Private shielded payloads never enter this public-intent service.","limitations":"A measured local simulation is not a cryptographic proof or a guarantee about a later block."},{"id":"workshop","label":"Commissioned calendar instruments","state":"LOCAL_SERVICE_AND_CONTRACTS","explanation":"Ask a chosen provider to make a release calendar, check its exact output, and acquire it as a frozen cartridge NFT held by your main NFT.","sources":["web/workshop/client.mjs","web/workshop/calendar.mjs","contracts/src/confluence/cartridges/CommissionedCartridges.sol"],"dependencies":["CommissionEscrow","CommissionedCartridges","CartridgeRegistry","chosen worker and evaluator"],"authority":"Acquiring an instrument grants no spending permission.","limitations":"This original recipe remains deterministic. New instruments separately supports arbitrary source review, provider discovery and paid-service adapters."},{"id":"cartridges","label":"Owned executable cartridges","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Hold a supported game or instrument NFT, verify its code, and open it in a separate play area.","sources":["contracts/src/confluence/cartridges/CartridgeRegistry.sol","web/confluence/app.js"],"dependencies":["owned compatible cartridge","content hash","sandboxed browser frame"],"authority":"Sandboxed opaque-origin frame; no wallet bridge by default. Browser isolation does not certify arbitrary programs safe.","limitations":"Compatible HTML cartridges and bundled local games are supported. Owning an arbitrary NFT does not make its game executable."},{"id":"onchain-runtime","label":"Immutable application and wallet thumbnail","state":"IMMUTABLE_ARCHIVE_SOURCE","explanation":"The NFT carries a recoverable copy of its application and a seed-shaped wallet picture in immutable contract storage.","sources":["contracts/src/confluence/GenesisSVG.sol","contracts/src/confluence/ConfluenceRenderer.sol","contracts/src/protocol/OnchainApp.sol","web/confluence/chain-loader.mjs"],"dependencies":["new renderer deployment","immutable application chunks","read provider"],"authority":"Everyone can read public application bytes; the owner controls protected transactions.","limitations":"Browser rendering and chain reads are still required. Updating a website does not replace bytes in an older immutable mint."},{"id":"immutable-resources","label":"Sealed modules and privacy worker shards","state":"IMMUTABLE_ARCHIVE_SOURCE","explanation":"A sealed directory tells the NFT where its tools are. Its large privacy worker is stored in checked fragments and rebuilt when needed.","sources":["contracts/src/confluence/GenesisManifest.sol","contracts/src/protocol/ShardedResource.sol","web/privacy/recover-resource.mjs"],"dependencies":["sealed module manifest","worker shards","read provider"],"limitations":"Worker bytes are archived; proving circuits, RPC, synchronization, proof services and broadcasters remain external."},{"id":"agent-commerce","label":"Paid services and job escrow","state":"LOCAL_SERVICE_AND_CONTRACTS","explanation":"Review and pay for a supported x402 service, or fund a separately reviewed provider job and its delivery rules.","sources":["contracts/src/extensions/agents/AgentCommerce.sol","web/extensions/agents.mjs"],"dependencies":["reviewed extension deployment","configured local or hosted service"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"x402 v2 exact EVM EIP-3009 through official SDK; separate ERC-8183 draft non-hooked kernel. Requires supported token and configured service.","entrance":"Atlas → New instruments"},{"id":"provider-directory","label":"Open provider discovery","state":"LOCAL_SERVICE_AND_CONTRACTS","explanation":"Browse signed provider entries and compare attributable feedback from funded jobs.","sources":["contracts/src/extensions/agents/ProviderDirectory.sol","web/extensions/agents.mjs"],"dependencies":["reviewed extension deployment","configured local or hosted service"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"ANIMA-native directory, not an ERC-8004 implementation. Signatures authenticate wallets, not quality, personhood or claims.","entrance":"Atlas → New instruments"},{"id":"generated-instruments","label":"Commission arbitrary programs","state":"LOCAL_SERVICE_AND_CONTRACTS","explanation":"Commission a new HTML and JavaScript tool, inspect its exact source, approve it and acquire a frozen cartridge.","sources":["contracts/src/confluence/cartridges/CommissionedCartridges.sol","worlds/instruments.mjs","web/extensions/worlds.mjs"],"dependencies":["reviewed extension deployment","configured local or hosted service"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Human approval and browser isolation do not prove arbitrary code safe. No default wallet bridge; exact proposals need separate review.","entrance":"Atlas → New instruments"},{"id":"quote-proof","label":"Cryptographically proved quote","state":"LOCAL_SERVICE_AND_CONTRACTS","explanation":"Prove the exact integer math of a supported swap and bind it to the current NFT and market state before signing.","sources":["contracts/src/extensions/proof/NativeQuoteRehearsal.sol","web/extensions/proof.mjs"],"dependencies":["reviewed extension deployment","configured local or hosted service"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Groth16 development setup; one-hop NativeMarket arithmetic only. Full EVM execution, gas, token behavior and future state are not proved.","entrance":"Atlas → New instruments"},{"id":"shared-world","label":"Persistent shared world","state":"LOCAL_SERVICE_AND_CONTRACTS","explanation":"Meet other players, gather, craft, fight, found towns and trade in a persistent hosted world.","sources":["agent/worlds/server.mjs","worlds/model.mjs","web/extensions/worlds.mjs"],"dependencies":["reviewed extension deployment","configured local or hosted service"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Working multiplayer civilization slice, not a complete mature MMO. Operator-authoritative game state and game coins have no onchain withdrawal.","entrance":"Atlas → New instruments"},{"id":"named-mint","label":"ENS named mint entry","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Mint an NFT and its deterministic subname together, then resolve that name back to the NFT’s real account.","sources":["contracts/src/extensions/access/GenesisNames.sol","web/extensions/access.mjs"],"dependencies":["reviewed extension deployment","connected signer"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Requires an explicitly delegated ENS parent. Does not purchase or renew a .eth domain; ancestor control and expiry remain applicable.","entrance":"Atlas → New instruments"},{"id":"whole-nft-shares","label":"Whole-NFT custody shares","state":"CONFIGURABLE_WALLET_AND_CONTRACTS","explanation":"Wrap an unused NFT, share its economic custody, vote on a funded buyout or reunite every share to recover it.","sources":["contracts/src/extensions/markets/WholeNFTShares.sol","web/extensions/markets.mjs"],"dependencies":["reviewed extension deployment","connected signer"],"authority":"Exact contract permissions and explicit wallet reviews; NFT custody invalidates applicable account sessions.","limitations":"Only unused Bound NFTs qualify. Account execution freezes while fractionalized; shares do not transfer private keys or every possible external right.","entrance":"Atlas → New instruments"},{"id":"full-evm-proof","label":"Arbitrary EVM effect proofs","state":"RESEARCH_NOT_IMPLEMENTED","explanation":"Prove every relevant effect of an arbitrary contract transaction instead of trusting an execution service.","limitations":"Research remains. The delivered quote circuit proves only bounded NativeMarket arithmetic and current-state binding."},{"id":"adaptive-agent-reasoning","label":"General autonomous agent decisions","state":"RESEARCH_NOT_IMPLEMENTED","explanation":"Have an agent independently choose new multi-step actions while proving they follow the owner’s intent.","limitations":"Research remains. The delivered operator runs exact owner-approved calls with onchain limits; it does not supply unrestricted adaptive reasoning or Sovereign-mode proofs."},{"id":"complete-mmo","label":"Complete mature MMO civilization","state":"RESEARCH_NOT_IMPLEMENTED","explanation":"Build a production-scale multiplayer civilization with a complete content, progression and economic system.","limitations":"Broader project remains. This release supplies a persistent playable multiplayer slice, not a finished MMORPG."}],"sourceVersion":"5.9.0","stateLabels":{"LOCAL_AND_COMPILED_SOURCE":"LOCAL REHEARSAL + CONTRACT SOURCE","LOCAL_ONLY":"LOCAL INTERACTION ONLY","RESEARCH_NOT_IMPLEMENTED":"RESEARCH · NOT IMPLEMENTED","SPECIFICATION_ONLY":"SPECIFICATION · NO EXECUTOR","RESTRICTED_WALLET":"RESTRICTED REAL WALLET","CONFIGURABLE_WALLET_AND_CONTRACTS":"WALLET + CONTRACTS · DEPLOYMENT REQUIRED","LOCAL_SERVICE_AND_CONTRACTS":"LOCAL SERVICE + CONTRACTS","IMPLEMENTED_BROWSER":"BROWSER FUNCTION","IMMUTABLE_ARCHIVE_SOURCE":"IMMUTABLE ARCHIVE · DEPLOYMENT REQUIRED"}};
/* This controller is an additive, isolated layer. It never edits ASSETS, OpticalField,
 * the original receipt book, original identity or wallet integration. A separate versioned bridge projects activity into optics. */
const IX_TABS=[['trade','Trade'],['journal','Memory'],['launch','Launch'],['vault','Lock'],['exits','Vesting exits'],['give','Give'],['market','Market'],['world','World'],['library','Library'],['work','Work'],['ledger','Commitments'],['lab','Lab']];
const ix$=s=>document.querySelector(s);
const esc=v=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const when=t=>new Date(t*1000).toLocaleString('en-US',{month:'short',day:'numeric',year:'numeric',hour:'2-digit',minute:'2-digit'});
const money=(n,asset)=>format(n,6)+' '+esc(asset);
const short=v=>String(v).slice(0,12)+'…'+String(v).slice(-8);
const B=(act,label,id='',cls='',disabled=false)=>`<button type="button" class="ix-button ${cls}" data-act="${act}" data-id="${esc(id)}" ${disabled?'disabled':''}>${label}</button>`;
const fact=(k,v)=>`<div class="ix-fact"><span>${k}</span><strong>${v}</strong></div>`;
const field=(label,name,value,type='text')=>`<div class="ix-field"><label for="ix-${name}">${label}</label><input id="ix-${name}" name="${name}" type="${type}" value="${esc(value)}" ${type==='number'?'step="1"':''}></div>`;
const personLabel=p=>p==='you'?'You':p==='guest'?'Guest':p==='archivist'?'Example collector A':p==='weaver'?'Example collector B':'Listing escrow';
let ixEngine,ixView='trade',ixRoom=1,ixReceipt=0,ixReply=0,ixInspect=0,ixFuture=0,ixFilter='all',ixReview=null,ixStored=null,ixStore,ixSeed,ixBusy=false,ixCorrupt=false;
const W=()=>ixEngine.world;
const anchor=()=>window.__idfbi.world().state.audit;
const input=id=>ix$('#ix-'+id)?.value;
function sourceCheck(){const s=window.__idfbi;if(s.chain())throw Error('The instruments have no live-chain adapter. Disconnect the original testnet client to rehearse.');if(s.state().root!==s.world().state.root)throw Error('History and descendants are read-only. Return the organism to its present first.');if(s.state().sovereign)throw Error('Sovereign utility execution requires a valid proof adapter. This rehearsal does not bypass that policy.');if(s.world().state.seed!==ixSeed)throw Error('The original identity changed. Reload to open its separate instruments.');}
function notice(text,error=false){const n=ix$('#ix-notice');n.textContent=text;n.dataset.error=String(error);}
function savedCheck(){if(ixStored===null)return;let current;try{current=localStorage.getItem(ixStore);}catch{return;}if(current!==ixStored)throw Error('Another tab changed this instrument archive. Export your open state before reloading.');}
function persist(){const data=JSON.stringify({schema:'idfbi/instrument-file/1.7',originSeed:ixSeed,originAudit:anchor(),engine:ixEngine.export()});try{localStorage.setItem(ixStore,data);ixStored=data;}catch{ixStored=null;notice('Browser storage is unavailable. Export this rehearsal to retain it.');}}
function saveFile(data,name){const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),10000);}
async function perform(fn,message='Applied in the local rehearsal.',replaceCorrupt=false){
 if(ixBusy)return;ixBusy=true;
 try{const job=()=>{sourceCheck();savedCheck();if(ixCorrupt&&!replaceCorrupt)throw Error('The saved archive is corrupt and has been preserved. Import a valid export to replace it explicitly.');const old=ixEngine;old.atomic(()=>{fn();savedCheck();});ixCorrupt=false;persist();};
  if(navigator.locks?.request)await navigator.locks.request(ixStore,job);else job();
  ixReview=null;draw();notice(message); // No visual success is converted into an onchain receipt.
 }catch(e){notice(e.message,true);}finally{ixBusy=false;}
}
function assets(selected='ETH'){return ['ETH',...W().s.pools.map(p=>p.asset)].map(a=>`<option value="${esc(a)}" ${a===selected?'selected':''}>${esc(a)}</option>`).join('');}
function amountBox(asset='ETH',amount='0.1'){return `<label for="ix-amount">You provide</label><div class="ix-asset"><input id="ix-amount" name="amount" value="${amount}" inputmode="decimal" aria-label="Input amount"><select id="ix-input" name="input" aria-label="Input asset">${assets(asset)}</select></div>`;}
function recipient(g){return g.recipient.kind==='person'?personLabel(g.recipient.id)+' · personal wallet':'Artifact #'+g.recipient.id+' · current owner '+personLabel(W().identity(g.recipient.id).owner);}
function nav(view){ixView=view;ixReview=null;ixInspect=0;notice('');draw();}
function draw(){
 const w=W(),a=w.identity();
 ix$('#ix-actor').value=w.s.actor;
 ix$('#ix-identity').innerHTML=w.s.identities.map(x=>`<option value="${x.id}" ${x.id===a.id?'selected':''}>#${String(x.id).padStart(4,'0')} · ${personLabel(x.owner)}</option>`).join('');
 ix$('.ix-tabs').innerHTML=IX_TABS.map(([id,label])=>`<button type="button" role="tab" aria-selected="${ixView===id}" data-act="tab" data-id="${id}">${label}</button>`).join('');
 const pages={exits:exPage,journal:memPage,'memory-write':memWritePage,form:memFormPage,trade:tradePage,launch:launchPage,vault:vaultPage,give:givePage,market:marketPage,world:worldPage,receipts:receiptPage,capabilities:capabilityPage,clock:clockPage,library:opLibraryPage,work:opWorkPage,ledger:opLedgerPage,lab:opLabPage,venues:opVenuesPage,applications:opApplicationPage};
 ix$('#ix-content').innerHTML=ixReview?reviewPage():(pages[ixView]||tradePage)();
 if(!ixReview&&ixView==='market')ix$('#ix-content').insertAdjacentHTML('afterbegin',`<div class="ix-actions" style="margin-bottom:20px">${B('op-venues','Operating shelves →')}${B('tab','Commitment ledger →','ledger')}${W().s.listings.filter(l=>l.status==='listed'&&l.seller===W().s.actor).map(l=>B('op-refresh-listing','Refresh manifest #'+l.id,l.id)).join('')}</div>`);
 if(!ixReview&&['trade','give'].includes(ixView))updateEstimate();
 memAfterDraw();
}
function balanceFacts(){return Object.entries(W().identity().balances).map(([k,v])=>fact(esc(k),format(v,6))).join('');}
function tradePage(){return `<div class="ix-grid"><div class="ix-main"><h3>A swap, or a future.</h3><p>Receive an asset now—or route the output straight into a time lock.</p><form id="ix-trade-form">${amountBox()}<div class="ix-row"><div><label for="ix-output">Receive</label><select id="ix-output" name="output">${assets('AUR')}</select></div><div><label for="ix-slip">Maximum slippage</label><select id="ix-slip" name="slip"><option value="50">0.5%</option><option value="100">1%</option><option value="300">3%</option></select></div></div><div id="ix-estimate" class="ix-estimate"></div><label class="ix-check"><input id="ix-autolock" type="checkbox" name="autolock"> Lock the received asset in the same operation</label><div id="ix-lock-duration" hidden>${field('Lock for days','days',30,'number')}${exEditor()}</div><button class="ix-button primary" type="submit">Review the outcome →</button></form></div><aside class="ix-side"><h4>Inside this artifact</h4>${balanceFacts()}<p class="ix-small" style="margin-top:16px">Fictional balances. Quote math is an integer reserve approximation, not a v4 tick simulation or live market price.</p><details><summary>What the contract source does</summary><p>Exact-input v4 execution, deadline and minimum output, then optional vault deposit. Every PoolManager delta must settle. The hook publishes its fee ramp and locks only the seed adapter’s principal.</p><p class="ix-small">Source is compiled. This rehearsal is separate from the configured live exit desk and its actual market adapter.</p></details></aside></div>`;}
function givePage(){const gs=ixEngine.extra.gifts;return `<div class="ix-grid"><div><h3>Give someone a future.</h3><p>Offer a chosen asset with a fixed release date. They accept the terms; then the gift cannot be recalled.</p><form id="ix-give-form">${amountBox()}<div class="ix-row"><div><label for="ix-output">They receive</label><select id="ix-output">${assets('AUR')}</select></div><div><label for="ix-slip">Maximum slippage</label><select id="ix-slip"><option value="50">0.5%</option><option value="100">1%</option></select></div></div><div class="ix-field"><label for="ix-recipient">Who receives it?</label><select id="ix-recipient"><option value="person:guest">Guest’s personal wallet</option><option value="person:you">Your personal wallet</option>${W().s.identities.filter(x=>x.id!==W().s.selected).map(x=>`<option value="artifact:${x.id}">Artifact #${x.id} account (follows its ownership)</option>`).join('')}</select></div>${field('Release in days · at least 2','days',30,'number')}<div id="ix-estimate" class="ix-estimate"></div><button class="ix-button primary" type="submit">Review this gift →</button></form></div><aside class="ix-side"><h4>Two decisions. One promise.</h4><div class="ix-flow"><span>OFFER</span><b>→</b><span>ACCEPT</span><b>→</b><span>RELEASE</span></div>${fact('Before acceptance','You can recall it')}${fact('After acceptance','No early release or recall')}${fact('No response','Return to donor after expiry')}${fact('Messages and membership','Never attached automatically')}<p class="ix-small" style="margin-top:16px">A personal-wallet gift stays with that wallet. An artifact-account gift follows the artifact if it is sold. These are different rights.</p></aside></div><hr class="ix-divider"><div class="ix-inline"><h4>Offers & accepted gifts</h4><span class="ix-small">Change “Act as” above to test recipient consent.</span></div><div class="ix-stack">${gs.length?gs.map(giftCard).join(''):'<p>No gifts yet. Nothing has been sent to a real wallet.</p>'}</div><details><summary>Personal rehearsal balances</summary>${Object.entries(ixEngine.extra.personalBalances).map(([p,bs])=>`<h4>${personLabel(p)}</h4>${Object.entries(bs).map(([k,v])=>fact(esc(k),format(v,6))).join('')||'<p class="ix-small">No released gifts.</p>'}`).join('')}</details>`;}
function giftCard(g){const pending=g.status==='offered',accepted=g.status==='accepted',actor=W().s.actor;return `<article class="ix-card"><h4>Gift ${g.id} <span class="ix-tag">${g.status.toUpperCase()}</span></h4>${fact('Amount',money(g.amount,g.asset))}${fact('Recipient',esc(recipient(g)))}${fact('Fixed release',when(g.releaseAt))}${pending?fact('Accept before',when(g.acceptBy)):''}<div class="ix-actions">${pending?B('accept-gift','Accept',g.id,'',ixEngine.recipientActor(g)!==actor||W().s.now>=g.acceptBy)+B('return-gift','Return to donor',g.id,'',W().identity(g.donor).owner!==actor&&W().s.now<g.acceptBy):''}${accepted?B('release-gift','Release to beneficiary',g.id,'',W().s.now<g.releaseAt):''}${B('trace','Follow its receipts',g.receipt)}</div></article>`;}
function updateEstimate(){try{const n=wei(input('amount')),a=input('input'),b=input('output'),q=a===b?{out:n,hops:[]}:W().quote(a,b,n);if(n<=0n)throw Error('Enter an amount.');const min=q.out*BigInt(10000-Number(input('slip')))/10000n;ix$('#ix-estimate').innerHTML=`≈ ${money(q.out,b)}<small>Minimum ${money(min,b)} · ${q.hops.length?q.hops.length+' model route hop'+(q.hops.length>1?'s':''):'no swap'}<br>Illustrative rehearsal only. No live quote.</small>`;}catch(e){ix$('#ix-estimate').textContent=e.message;}}
function vaultPage(){const w=W(),id=w.s.selected,ls=w.s.locks.filter(l=>l.identity===id),gs=ixEngine.extra.gifts.filter(g=>g.recipient.kind==='artifact'&&g.recipient.id===id&&['accepted','released'].includes(g.status));return `<div class="ix-grid"><div><h3>Make time part of the promise.</h3><p>No yield claim. Just an explicit beneficiary and a release schedule.</p><form id="ix-lock-form">${amountBox('ETH','0.1')}${field('Lock for days','days',30,'number')}<button class="ix-button primary" type="submit">Review lock →</button></form></div><aside class="ix-side"><h4>A lens, not a time machine.</h4><label for="ix-future">Inspect scheduled availability: <b id="ix-future-label">+${ixFuture} days</b></label><input id="ix-future" type="range" min="0" max="365" value="${ixFuture}"><div id="ix-future-balances">${futureFacts(id)}</div><p class="ix-small" style="margin-top:13px">Projection never moves the real rehearsal clock, unlocks funds, or predicts a token’s price.</p></aside></div><hr class="ix-divider"><h4>Existing schedules</h4><div class="ix-stack">${ls.map(l=>lockCard(l)).join('')}${gs.map(giftCard).join('')}${!ls.length&&!gs.length?'<p>No schedules for this artifact.</p>':''}</div>`;}
function futureFacts(id){const p=ixEngine.project(id,W().s.now+ixFuture*DAY);return Object.entries(p.balances).map(([k,v])=>fact(esc(k),format(v,5))).join('');}
function lockCard(l){const available=releasable(l,W().s.now),progress=Math.max(0,Math.min(100,100*(W().s.now-l.start)/(l.end-l.start)));return `<article class="ix-card"><h4>Lock ${l.id} <span class="ix-tag">${l.linear?'LINEAR VESTING':'TIME LOCK'}</span></h4>${fact('Committed',money(l.amount,l.asset))}${fact('Release schedule',when(l.end))}<div class="ix-progress"><span style="width:${progress.toFixed(2)}%"></span></div>${fact('Releasable now',money(available,l.asset))}<div class="ix-actions">${B('release-lock','Release to this account',l.id,'',available===0n)}</div></article>`;}
function launchPage(){return `<div class="ix-grid"><div><h3>A launch with terms that stay put.</h3><p>Token, allocations, sale, future pool, and conversation—declared before contributions.</p><form id="ix-launch-form"><div class="ix-row">${field('Token name','name','A new beginning')}${field('Ticker','symbol','BEGIN')}</div><div class="ix-row">${field('Fixed supply','supply','1000000')}${field('Sale window · hours','hours',24,'number')}</div><div class="ix-row">${field('Minimum raise · ETH','soft','1')}${field('Maximum raise · ETH','hard','20')}</div><details><summary>Allocations & pool rules</summary><div class="ix-row" style="margin-top:14px">${field('Founder share · basis points','founder',1000,'number')}${field('Raised ETH to seed · basis points','liquidity',8000,'number')}</div><p>Founder tokens vest over 180 days with a 30-day cliff. Unseeded treasury ETH locks for 180 days. Seed principal is non-withdrawable through the supplied v4 adapter. LP fees may be harvested to the creator account.</p><p class="ix-small">The hook starts at a 1.00% LP fee and reaches 0.30% after 24 hours. Other LPs retain their withdrawal rights. This is not bot immunity.</p></details><button class="ix-button primary" type="submit">Review immutable terms →</button></form></div><aside class="ix-side"><h4>Not every launch is the same game.</h4>${fact('This rehearsal','Time-boxed, pro-rata batch')}${fact('Before closing','Contribution withdrawal')}${fact('Below minimum','Refund path')}${fact('After successful settlement','Market + locked allocations')}${fact('CCA integration','Researched, not implemented')}<p class="ix-small" style="margin-top:15px">The hard cap can favor earlier contributors. A v4 hook alone does not turn a batch sale into a continuous clearing auction.</p></aside></div><hr class="ix-divider"><h4>Launches</h4><div class="ix-stack">${W().s.sales.map(saleCard).join('')||'<p>Your first launch will appear here.</p>'}</div>`;}
function saleCard(s){return `<article class="ix-card"><h4>${esc(s.name)} · ${esc(s.symbol)} <span class="ix-tag">${s.status.toUpperCase()}</span></h4>${fact('Raised',money(s.raised,'ETH'))}${fact('Closes',when(s.closes))}<div class="ix-actions">${s.status==='open'?(W().s.now<s.closes?B('contribute','Contribute',s.id)+B('withdraw-contribution','Withdraw',s.id)+B('close-sale-clock','Advance test clock to close',s.id):B('settle','Settle',s.id)):B('claim',s.status==='refund'?'Claim refund':'Claim allocation',s.id)}${B('open-room','Open commons',s.room)}</div></article>`;}
function marketPage(){if(ixInspect){const a=W().identity(ixInspect),l=W().s.listings.find(l=>l.identity===a.id&&l.status==='listed');return `<h3>What moves with #${String(a.id).padStart(4,'0')}?</h3><p>You acquire control of declared possessions and account-held rights—not authorship of old messages or ownership of the people in its rooms.</p><div class="ix-grid"><div>${Object.entries(a.balances).map(([k,v])=>fact('Spendable '+esc(k),format(v,6))).join('')}${W().s.locks.filter(x=>x.identity===a.id).map(x=>fact('Lock '+x.id,money(x.amount,x.asset)+'<br>'+when(x.end))).join('')}${fact('Current custody',personLabel(a.owner))}${fact('Authority epoch',a.epoch)}${opMarketFacts(a.id)}<div class="ix-actions">${l?B('buy','Review acquisition',l.id):''}${B('market-back','Back to market')}</div></div><aside class="ix-side"><label for="ix-future">Scheduled future · <b id="ix-future-label">+${ixFuture} days</b></label><input id="ix-future" type="range" min="0" max="365" value="${ixFuture}"><div id="ix-future-balances">${futureFacts(a.id)}</div><p class="ix-small" style="margin-top:14px">Declared inventory is not a universal balance-sheet proof. Known gifts, work, instruments, shelves and laboratory records are committed here. Unknown external approvals or liabilities are not automatically discovered.</p>${l?`<span class="ix-code">Manifest ${esc(l.manifest)}</span>`:''}</aside></div>`;}
 return `<h3>Buy the artifact. Understand the contents.</h3><p>Inspect present balances, future claims, custody, and the exact manifest before a purchase.</p><div class="ix-cards">${W().s.listings.filter(l=>l.status==='listed').map(l=>{const a=W().identity(l.identity);return `<article class="ix-card"><span class="ix-eyebrow">DECLARED-CONTENT OFFER</span><h3 style="margin-top:15px">#${String(a.id).padStart(4,'0')}</h3>${fact('Ask',money(l.price,'ETH'))}${fact('Contents',Object.keys(a.balances).length+' asset types')}${fact('Time commitments',W().s.locks.filter(x=>x.identity===a.id).length)}<div class="ix-actions">${B('inspect-estate','Unfold the contents →',a.id)}${l.seller===W().s.actor||W().s.now>l.expires?B('cancel-listing','Cancel listing',l.id):''}</div></article>`;}).join('')||'<p>No open rehearsal listings.</p>'}</div><hr class="ix-divider"><h4>Selected artifact #${W().s.selected}</h4><p class="ix-small">Only its current owner can list it. Escrow revokes their spending. The buyer receives control, not an early-unlock exception.</p>${B('list-estate','Prepare a listing',W().s.selected,'',W().identity().owner!==W().s.actor)}<details><summary>Ownership vs temporary use</summary><p>A presence grant allows use without treasury control. Active grants block this marketplace’s listing path.</p>${B('presence','Grant temporary presence',W().s.selected,'',W().identity().owner!==W().s.actor)}</details>`;}
function worldPage(){const w=W(),r=w.room(ixRoom),v=w.s.views[w.s.actor]||{block:[],follow:[]};let posts=w.s.messages.filter(m=>m.room===r.id&&!v.block.includes(m.actor));if(ixFilter==='follow')posts=posts.filter(m=>m.actor===w.s.actor||v.follow.includes(m.actor));const activity=w.s.events.filter(e=>['LAUNCHED', 'SETTLED', 'SWAPPED', 'LOCKED', 'ACQUIRED', 'OFFERED', 'ACCEPTED', 'GIFT_RELEASED', 'EDITION_PUBLISHED', 'EDITION_ADOPTED', 'GRANT_CREATED', 'GRANT_REVOKED', 'EDITION_EXECUTED', 'COMMISSION_FUNDED', 'COMMISSION_ACCEPTED', 'WORK_SUBMITTED', 'WORK_PAID', 'WORK_REJECTED', 'COMMISSION_REFUNDED', 'VENUE_OPENED', 'SHELF_PURCHASED', 'VENUE_CLOSED', 'EDITION_COLLECTED', 'LAB_ALLOCATED', 'LAB_WITHDRAWN', 'CREDIT_REQUESTED', 'CREDIT_FUNDED', 'CREDIT_REPAID', 'CREDIT_CLOSED', 'OPTION_WRITTEN', 'OPTION_BOUGHT', 'OPTION_EXERCISED', 'OPTION_CLOSED', 'APPLICATION_COMMITTED'].includes(e.kind)&&!v.block.includes(e.actor)&&(ixFilter!=='follow'||e.actor===w.s.actor||v.follow.includes(e.actor))).filter(e=>r.id===1||e.data.room===r.id||w.s.sales.find(s=>s.room===r.id)?.id===e.data.sale);const feed=[...posts.map(m=>({t:m.time,order:m.id,html:postCard(m)})),...activity.map(e=>({t:e.time,order:e.seq,html:eventCard(e)}))].sort((a,b)=>b.t-a.t||b.order-a.order).slice(0,60);return `<div class="ix-inline" style="margin-top:0"><div><h3>The conversation continues.</h3><p style="margin-bottom:0">Conversations and actions. Each keeps its own author.</p></div>${B('new-room','Create a room')}</div><div class="ix-row"><div><label for="ix-room">Conversation</label><select id="ix-room">${w.s.rooms.map(x=>`<option value="${x.id}" ${x.id===r.id?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div><label for="ix-filter">Your view</label><select id="ix-filter"><option value="all" ${ixFilter==='all'?'selected':''}>Everyone</option><option value="follow" ${ixFilter==='follow'?'selected':''}>Following + you</option></select></div></div><p class="ix-small">${esc(r.topic)} · ${r.gated?'Members may post. Content remains public.':'Public posting.'}</p>${r.members[w.s.actor]&&!r.members[w.s.actor].accepted?B('accept-room','Accept room invitation',r.id):''}<div class="ix-feed">${feed.map(x=>x.html).join('')||'<p>This view has no messages yet.</p>'}</div>${ixReceipt?`<div class="ix-inline"><span class="ix-small">Discussing receipt ${ixReceipt}. This text cannot manufacture an action.</span>${B('clear-reply','Clear reference')}</div>`:''}${ixReply?`<p class="ix-small">Replying to message ${ixReply}.</p>`:''}<form id="ix-message-form" class="ix-composer"><textarea id="ix-message" rows="2" maxlength="1024" placeholder="Say something worth keeping…" aria-label="Public message"></textarea><button type="submit" class="ix-button" ${w._canPost(r,w.s.actor)?'':'disabled'}>${w._canPost(r,w.s.actor)?'Post locally':'Posting not permitted'}</button></form><details><summary>People & room controls</summary>${['guest','archivist','weaver','you'].filter(p=>p!==w.s.actor).map(p=>`<div class="ix-person"><span>${personLabel(p)}</span>${B('follow',v.follow.includes(p)?'Unfollow':'Follow',p)}${B('block',v.block.includes(p)?'Unmute':'Mute in my view',p)}${r.identity&&w._mod(r)?B('invite','Invite to room',p)+B('remove-member','Remove from room',p):''}</div>`).join('')}<p class="ix-small" style="margin-top:14px">Muted people retain their World access. Removing a room member revokes future posting, not their old messages. Invitations require acceptance.</p>${w._admin(r)?B('room-settings','Room settings',r.id):''}</details>`;}
function postCard(m){return `<article class="ix-post"><header><span>${personLabel(m.actor)}${m.identity?' · artifact #'+m.identity:''}</span><span>MESSAGE ${m.id}</span></header><p>${esc(m.body)}</p><small>Authored as ${personLabel(m.custodian)} · epoch ${m.epoch}${m.receipt?' · receipt '+m.receipt:''}</small><div class="ix-actions">${B('reply','Reply',m.id)}${B('react','♡ '+(m.reactions['♡']?.length||0),m.id)}${m.receipt?B('trace','Inspect referenced action',m.receipt):''}</div></article>`;}
function eventCard(e){const labels={EDITION_PUBLISHED:'published an instrument',EDITION_ADOPTED:'adopted an edition',EDITION_EXECUTED:'executed a bounded instrument',COMMISSION_FUNDED:'funded a commission',WORK_SUBMITTED:'delivered an edition',WORK_PAID:'paid for accepted work',VENUE_OPENED:'opened a collectible shelf',SHELF_PURCHASED:'bought a collectible',LAB_ALLOCATED:'allocated laboratory capital',CREDIT_FUNDED:'funded fixed-term credit',OPTION_BOUGHT:'accepted a covered option',LAUNCHED:'launched a token',SETTLED:'settled a launch',SWAPPED:'swapped assets',LOCKED:'created a time lock',ACQUIRED:'acquired an artifact',OFFERED:'offered a gift',ACCEPTED:'accepted a gift',GIFT_RELEASED:'released a gift'};let description=e.data.symbol?esc(e.data.symbol):e.data.gift?'Gift '+esc(e.data.gift):e.data.amount?money(e.data.amount,e.data.asset||e.data.input||'ETH'):'Artifact #'+e.identity;if(e.kind==='SWAPPED')description=money(e.data.amount,e.data.input)+' → '+money(e.data.received,e.data.output)+(e.data.lock?' · output locked':'');if(['OFFERED','GIFT_RELEASED'].includes(e.kind))description=money(e.data.amount,e.data.asset)+' → '+esc(e.data.recipient.kind==='person'?personLabel(e.data.recipient.id)+'’s personal wallet':'artifact #'+e.data.recipient.id);return `<article class="ix-post activity"><header><span>${personLabel(e.actor)} ${esc(labels[e.kind]||e.kind)}</span><span>LOCAL RECEIPT ${e.seq}</span></header><p>${description}</p><small>Protocol-model activity, not a user-written claim or a blockchain receipt.</small><div class="ix-actions">${B('trace','Trace this action',e.seq)}${B('discuss','Discuss',e.seq)}</div></article>`;}
function receiptPage(){const trace=ixInspect?ixEngine.trace(ixInspect):null;return `<h3>${trace?'A receipt is a beginning, not a dead end.':'Follow what actually led here.'}</h3><p>Inspect causes, then the discussions attached to them. These local records check consistency—not authorship or blockchain execution.</p>${trace?`<div class="ix-timeline">${trace.map(({event:e,causes,discussions})=>`<article><h4>${e.seq} / ${esc(e.kind)}</h4><p>${personLabel(e.actor)} acting with artifact ${e.identity||'none'} · custody ${personLabel(e.custodian)} · epoch ${e.epoch}</p><span class="ix-code">${esc(e.digest)}</span><p class="ix-small">${causes.length?'Caused by '+causes.join(', '):'No earlier causal reference in this local record.'}</p><details><summary>Exact record</summary><pre>${esc(JSON.stringify(e,null,2))}</pre></details>${discussions.map(m=>`<p>“${esc(m.body)}” — ${personLabel(m.actor)}</p>`).join('')}${B('discuss','Discuss this receipt',e.seq)}</article>`).join('')}</div>${B('receipts','All receipts')}`:`<div class="ix-stack">${W().s.events.slice().reverse().map(e=>`<article class="ix-card"><h4>${e.seq} / ${esc(e.kind)} <span class="ix-small">${personLabel(e.actor)}</span></h4><span class="ix-code">${esc(short(e.digest))}</span>${B('trace','Inspect causes & conversation',e.seq)}</article>`).join('')}</div>`}`;}
function capabilityPage(){return `<h3>What your NFT can do</h3><p>Each card explains a function and what it needs to work. Contract-backed tools require a compatible deployment and your reviewed transaction.</p><div class="ix-stack">${IX_CATALOG.modules.map(m=>`<article class="ix-card"><h4>${esc(m.label)} <span class="ix-tag">${esc(IX_CATALOG.stateLabels[m.state]||'STATUS UNKNOWN')}</span></h4><p>${esc(m.explanation||'An explanation has not been supplied.')}</p><details><summary>How it works & what it needs</summary><p>${esc(m.dependencies.join(' · '))}</p>${m.authority?`<p>Control: ${esc(m.authority)}</p>`:''}${m.privacy?`<p>Privacy: ${esc(m.privacy)}</p>`:''}${m.exit?`<p>Exit: ${esc(m.exit)}</p>`:''}${m.limitations?`<p>Scope: ${esc(m.limitations)}</p>`:''}</details></article>`).join('')}</div><p class="ix-small" style="margin-top:20px">${esc(IX_CATALOG.warning)} Research entries have no working executor in this release.</p>`;}
function clockPage(){return `<h3>The test clock is not the future lens.</h3><p>This changes the actual local model time and can expire offers, finish a sale, or make locks releasable. It never changes a blockchain’s timestamp.</p>${fact('Current rehearsal time',when(W().s.now))}<div class="ix-actions">${B('advance','+1 minute',60)}${B('advance','+1 day',DAY)}${B('advance','+7 days',7*DAY)}${B('advance','+30 days',30*DAY)}${B('advance','+180 days',180*DAY)}</div><hr class="ix-divider"><p class="ix-small">Export before advancing to keep a separate copy. The ordinary time lens remains read-only.</p>`;}
function reviewPage(){const r=ixReview;return `<h3>${esc(r.title)}</h3><p>${esc(r.description)}</p>${r.html}<span class="ix-code">Original audit anchor ${esc(short(r.anchor))}</span><p class="ix-small">This review is for a local rehearsal. It is not a signature, a verified proof, a live quote, or a transaction submitted to a blockchain.</p><div class="ix-actions">${B('confirm','Apply in rehearsal →','','primary')}${B('cancel-review','Keep editing')}</div>`;}
function simpleReview(title,description,html,fn){sourceCheck();ixReview={title,description,html,fn,anchor:anchor(),state:ixEngine.export().checksum};draw();}
function planReview(p){ixReview={title:p.intent.kind==='GIFT'?'Review a gift with consent.':p.intent.kind==='LOCK'?'Review this time commitment.':'Review the exact outcome.',description:'Nothing changes until you apply this review.',anchor:p.anchor,plan:p,html:`<div class="ix-flow">${p.preview.steps.map((s,i)=>(i?'<b>→</b>':'')+'<span>'+s+'</span>').join('')}</div>${fact('Provide',money(p.intent.amount,p.intent.input))}${fact('Model result',money(p.preview.received,p.preview.asset))}${fact('Minimum received',money(p.intent.minOut,p.intent.output))}${p.preview.recipient?fact('Recipient',p.preview.recipient.kind==='person'?personLabel(p.preview.recipient.id)+' · personal wallet':'Artifact #'+p.preview.recipient.id+' account'):''}${p.preview.acceptBy?fact('Acceptance deadline',when(p.preview.acceptBy)):''}${p.preview.releaseAt?fact('Fixed release date',when(p.preview.releaseAt)):''}${exReview(p)}${fact('Review expires',when(p.deadline))}<span class="ix-code">Plan ${esc(p.digest)}</span>`};draw();}
function modalForm(title,html,id){ix$('#ix-content').innerHTML=`<h3>${title}</h3><form id="${id}" style="margin-top:20px">${html}<button type="submit" class="ix-button primary">Review →</button></form>`;}
async function action(act,id){const w=W();if(act.startsWith('ex-'))return exAction(act,id);if(act.startsWith('mem-'))return memAction(act,id);if(act.startsWith('op-'))return opAction(act,id);
 if(act==='tab')return nav(id);
 if(act==='cancel-review'){ixReview=null;return draw();}
 if(act==='confirm'){const r=ixReview;if(!r)return;return perform(()=>{if(r.plan){if(r.note)ixEngine.executeNoted(r.plan,anchor(),r.note);else ixEngine.execute(r.plan,anchor());memDraft=null;}else{if(anchor()!==r.anchor||ixEngine.export().checksum!==r.state)throw Error('The reviewed state changed. Open a new review.');r.fn();}},'Applied locally. Open Receipts to inspect the recorded outcome.',!!r.replaceCorrupt);}
 if(['capabilities','clock','receipts'].includes(act))return nav(act);
 if(act==='export'){saveFile({schema:'idfbi/instrument-file/1.7',originSeed:ixSeed,originAudit:anchor(),engine:ixEngine.export()},'idfbi-instruments-rehearsal.json');return;}
 if(act==='import'){ix$('#ix-import').click();return;}
 if(act==='trace'){ixView='receipts';ixInspect=Number(id);ixReview=null;draw();return;}
 if(act==='discuss'){ixReceipt=Number(id);ixReply=0;ixView='world';ixRoom=1;draw();ix$('#ix-message').focus();return;}
 if(act==='clear-reply'){ixReceipt=0;ixReply=0;draw();return;}
 if(act==='reply'){ixReply=Number(id);ixReceipt=0;draw();ix$('#ix-message').focus();return;}
 if(act==='react')return perform(()=>w.react(Number(id),'♡'),'Reaction updated locally.');
 if(act==='open-room'){ixRoom=Number(id);return nav('world');}
 if(act==='inspect-estate'){ixInspect=Number(id);ixView='market';ixReview=null;draw();return;}
 if(act==='cancel-listing')return simpleReview('Cancel this listing.','Control returns to the recorded seller. No sale occurs.',fact('Listing',esc(id)),()=>w.cancel(Number(id)));
 if(act==='market-back'){ixInspect=0;draw();return;}
 if(act==='advance')return simpleReview('Advance only the rehearsal clock.', 'This is a model mutation, not a projection.',fact('Move forward',Number(id)/DAY+' days')+fact('New time',when(w.s.now+Number(id))),()=>w.advance(Number(id)));
 if(act==='accept-gift'){const g=ixEngine.gift(id);return simpleReview('Accept this gift.','Acceptance makes the gift non-recallable and fixes the release date.',fact('Amount',money(g.amount,g.asset))+fact('Release',when(g.releaseAt))+fact('Recipient',esc(recipient(g))),()=>ixEngine.accept(id));}
 if(act==='return-gift')return simpleReview('Return the pending offer.','The assets return only to the donor artifact account.',fact('Gift',esc(id)),()=>ixEngine.cancel(id));
 if(act==='release-gift')return simpleReview('Release to the beneficiary.','The caller cannot redirect this payment.',fact('Recipient',esc(recipient(ixEngine.gift(id)))),()=>ixEngine.releaseGift(id));
 if(act==='release-lock')return simpleReview('Release the available amount.','The fixed beneficiary receives it. The original schedule does not change.',fact('Lock',esc(id)),()=>w.release(Number(id)));
 if(act==='contribute'||act==='withdraw-contribution'){ixInspect=Number(id);modalForm(act==='contribute'?'Contribute to '+esc(w.sale(id).symbol):'Withdraw before closing.',field('ETH amount','contribution','1'),act==='contribute'?'ix-contribute-form':'ix-withdraw-form');return;}
 if(act==='close-sale-clock'){const s=w.sale(id);return simpleReview('Advance to the sale close.','Only the local test clock changes. No settlement occurs automatically.',fact('Close',when(s.closes)),()=>w.advance(s.closes-w.s.now));}
 if(act==='settle')return simpleReview('Settle against the committed terms.','A successful sale creates its market and locks; a failed sale enables refunds.',fact('Launch',esc(w.sale(id).symbol)),()=>w.settle(id));
 if(act==='claim')return simpleReview('Claim the allocation or refund.','Claiming twice is rejected.',fact('Launch',esc(w.sale(id).symbol)),()=>w.claim(id));
 if(act==='buy'){const l=w.checkListing(id);return simpleReview('Acquire artifact #'+l.identity+'.','The declared contents are checked again. Historical authorship and release schedules are not rewritten.',fact('Rehearsal wallet pays',money(l.price,'ETH'))+fact('Current wallet balance',money(w.s.wallets[w.s.actor],'ETH'))+`<span class="ix-code">${esc(l.manifest)}</span>`,()=>w.buy(l.id,l.manifest));}
 if(act==='list-estate'){modalForm('List artifact #'+id+'.',field('Ask · ETH','price','5')+field('Listing duration · days','listing-days',7,'number'),'ix-list-form');return;}
 if(act==='presence'){modalForm('Use without spending authority.',`<p>The other rehearsal persona receives a temporary presence grant, not treasury control.</p>${field('Days','presence-days',1,'number')}`,'ix-presence-form');return;}
 if(act==='follow'||act==='block')return perform(()=>w.view(id,act),'Your personal view changed. Nobody lost World access.');
 if(act==='invite')return perform(()=>w.invite(ixRoom,id),'Invitation created. The recipient must accept it.');
 if(act==='accept-room')return perform(()=>w.accept(Number(id)),'Room invitation accepted.');
 if(act==='remove-member')return simpleReview('Remove future posting access.','Old messages remain. This does not revoke World access.',fact('Member',personLabel(id)),()=>w.remove(ixRoom,id));
 if(act==='new-room'){modalForm('A room with its own rules.',field('Room name','room-name','A conversation')+'<label class="ix-check"><input id="ix-gated" type="checkbox"> Invitation-only posting (not private reading)</label>','ix-room-form');return;}
 if(act==='room-settings'){const r=w.room(ixRoom);modalForm('Configure this room.',field('Name','room-name',r.name)+field('Topic','topic',r.topic)+field('Slow mode · seconds','slow',r.slow,'number')+field('Room accent','room-color',r.color,'color')+`<label class="ix-check"><input id="ix-gated" type="checkbox" ${r.gated?'checked':''}> Invitation-only posting</label>`,'ix-settings-form');return;}
}
async function compose(kind){const a=input('input'),b=kind==='LOCK'?a:input('output'),n=wei(input('amount'));const q=a===b?{out:n}:W().quote(a,b,n);let days=kind==='LOCK'||kind==='GIFT'?Number(input('days')):ix$('#ix-autolock').checked?Number(input('days')):0;const min=kind==='LOCK'?n:q.out*BigInt(10000-Number(input('slip')))/10000n;let r=null;if(kind==='GIFT'){const [k,v]=input('recipient').split(':');r={kind:k,id:k==='artifact'?Number(v):v};}sourceCheck();const raw={kind,input:a,output:b,amount:n.toString(),minOut:min.toString(),days,recipient:r};if(kind==='SWAP'&&days&&ix$('#ix-exit-enabled')?.checked)raw.exit=exRead();const p=ixEngine.prepare(raw,anchor());if(kind==='SWAP')return memPrepareTrade(p);planReview(p);}
async function submit(form){if(form==='ex-fund-form')return exSubmit();if(form.startsWith('mem-'))return memSubmit(form);const w=W();if(form.startsWith('op-'))return opSubmit(form);
 if(form==='ix-trade-form')return compose('SWAP');if(form==='ix-give-form')return compose('GIFT');if(form==='ix-lock-form')return compose('LOCK');
 if(form==='ix-launch-form'){const terms={name:input('name'),symbol:input('symbol'),supply:wei(input('supply')),soft:wei(input('soft')),hard:wei(input('hard')),hours:Number(input('hours')),founderBps:Number(input('founder')),liquidityBps:Number(input('liquidity'))};const temp=KingdomModel.restore(w.export()),s=temp.createLaunch(terms);return simpleReview('Review '+s.symbol+' before it exists.','No allocation can be changed after creation in this model.',fact('Name',esc(s.name))+fact('Total supply',money(s.supply,s.symbol))+fact('Public allocation',format(s.public))+fact('Seed allocation',format(s.lp))+fact('Founder vesting',format(s.founder))+fact('Raise range',money(s.soft,'ETH')+' – '+money(s.hard,'ETH')),()=>w.createLaunch(terms));}
 if(form==='ix-contribute-form'||form==='ix-withdraw-form'){const n=wei(input('contribution')),sale=ixInspect;return simpleReview(form==='ix-contribute-form'?'Review contribution.':'Review withdrawal.', 'Only the rehearsal treasury changes.',fact('Amount',money(n,'ETH')),()=>form==='ix-contribute-form'?w.contribute(sale,n):w.withdrawContribution(sale,n));}
 if(form==='ix-message-form'){const body=input('message'),r=ixReceipt,reply=ixReply;return perform(()=>{w.post(ixRoom,body,reply,r);ixReceipt=0;ixReply=0;},'Public rehearsal message recorded. It is not an encrypted message.');}
 if(form==='ix-list-form'){const n=wei(input('price')),days=Number(input('listing-days')),id=w.s.selected;return simpleReview('Escrow artifact #'+id+'.','The local operating manifest includes gifts, commitments, venues and isolated laboratory positions. Listing invalidates old grants. A material change requires a new seller manifest and buyer review.',fact('Ask',money(n,'ETH'))+fact('Duration',days+' days'),()=>w.listEstate(id,n,days));}
 if(form==='ix-presence-form'){const d=Number(input('presence-days')),id=w.s.selected,p=w.s.actor==='you'?'guest':'you';return simpleReview('Grant temporary presence.','No token spending permission is granted.',fact('User',personLabel(p))+fact('Duration',d+' days'),()=>w.grantPresence(id,p,d));}
 if(form==='ix-room-form'){const name=input('room-name'),gated=ix$('#ix-gated').checked;return simpleReview('Create this conversation.','Posting rules and readable content are different permissions.',fact('Room',esc(name))+fact('Posting',gated?'Invitation + acceptance':'Public'),()=>{ixRoom=w.createRoom(name,gated).id;});}
 if(form==='ix-settings-form'){const data={name:input('room-name'),topic:input('topic'),slow:Number(input('slow')),color:input('room-color'),gated:ix$('#ix-gated').checked};return simpleReview('Review room rules.','Changing these rules cannot rewrite earlier authors.',fact('Topic',esc(data.topic))+fact('Slow mode',data.slow+' seconds'),()=>w.configureRoom(ixRoom,data));}
}
function setupInstruments(){
 ixSeed=window.__idfbi.world().state.seed;ixStore='idfbi.instruments.1.7:'+ixSeed;ixEngine=new ExitEngine(undefined,ixSeed);
 let raw=null;try{raw=localStorage.getItem(ixStore);}catch{notice('Browser storage is unavailable. Export this rehearsal to retain it.');}
 if(raw){ixStored=raw;try{const data=JSON.parse(raw);if(data.originSeed!==ixSeed||data.schema!=='idfbi/instrument-file/1.7')throw Error('Wrong identity');ixEngine=ExitEngine.restore(data.engine,ixSeed);if(ixEngine.mem.origin!==ixSeed)throw Error('Memory origin mismatch.');}catch{ixCorrupt=true;notice('Saved instruments could not be loaded. Their bytes were not overwritten. Import a valid export to replace them explicitly.',true);}}
 const opener=document.createElement('button');opener.id='instrument-open';opener.className='quiet';opener.textContent='USE NFT ↗';opener.setAttribute('aria-haspopup','dialog');opener.addEventListener('click',()=>{draw();ix$('#instrument-dialog').showModal();});ix$('.scene-toolbar').append(opener);
 ix$('#ix-close').addEventListener('click',()=>ix$('#instrument-dialog').close());
 ix$('#instrument-dialog').addEventListener('close',()=>{ixReview=null;memDraft=null;opener.focus();});
 ix$('#instrument-dialog').addEventListener('click',e=>{const b=e.target.closest('[data-act]');if(b&&!b.disabled)Promise.resolve().then(()=>action(b.dataset.act,b.dataset.id)).catch(e=>notice(e.message,true));});
 ix$('#instrument-dialog').addEventListener('submit',e=>{e.preventDefault();Promise.resolve().then(()=>submit(e.target.id)).catch(e=>notice(e.message,true));});
 ix$('#instrument-dialog').addEventListener('input',e=>{if(e.target.id==='ix-future'){ixFuture=Number(e.target.value);ix$('#ix-future-label').textContent='+'+ixFuture+' days';ix$('#ix-future-balances').innerHTML=futureFacts(ixView==='market'?ixInspect:W().s.selected);}else if(['ix-amount','ix-input','ix-output','ix-slip'].includes(e.target.id)&&ix$('#ix-estimate'))updateEstimate();});
 ix$('#instrument-dialog').addEventListener('change',e=>{if(e.target.id==='ix-autolock')ix$('#ix-lock-duration').hidden=!e.target.checked;else if(e.target.id==='ix-actor'){memLock();W().setActor(e.target.value);ixReview=null;draw();notice('Testing as '+personLabel(W().s.actor)+'. No wallet account changed.');}else if(e.target.id==='ix-identity'){W().select(Number(e.target.value));memFormThrough=null;ixReview=null;draw();}else if(e.target.id==='ix-room'){ixRoom=Number(e.target.value);ixReply=0;ixReceipt=0;draw();}else if(e.target.id==='ix-filter'){ixFilter=e.target.value;draw();}});
 ix$('#ix-import').addEventListener('change',async e=>{try{const f=e.target.files[0];if(!f)return;if(f.size>67108864)throw Error('Archive exceeds the 64 MiB import limit.');const data=JSON.parse(await f.text());if(!['idfbi/instrument-file/1.5','idfbi/instrument-file/1.6','idfbi/instrument-file/1.7'].includes(data.schema)||data.originSeed!==ixSeed)throw Error('This archive belongs to a different organism.');const restored=ExitEngine.restore(data.engine,ixSeed);if(restored.mem.origin!==ixSeed)throw Error('Memory origin mismatch.');simpleReview('Replace this local instrument archive?','The original organism and its history stay unchanged. Export the current instruments first to keep them.',fact('Records',restored.world.s.events.length),()=>{ixEngine=restored;ixRoom=1;ixInspect=0;});ixReview.replaceCorrupt=true;}catch(e){notice(e.message,true);}finally{e.target.value='';}});
 window.__instruments={version:'1.7.0',memory:()=>({ready:lifeReady,form:lifeDescriptor,original:memFormOriginal,through:memFormThrough}),engine:()=>ixEngine,view:()=>ixView,anchor,open:()=>opener.click(),openView:(view)=>{if(!IX_TABS.some(t=>t[0]===view)&&!['form','receipts','capabilities','clock','applications','venues'].includes(view))throw Error('Unknown instrument view');nav(view);const d=ix$('#instrument-dialog');if(!d.open)d.show();},catalog:IX_CATALOG};
 document.body.dataset.instrumentsReady='true';
}
const ixBoot=setInterval(()=>{if(window.__idfbi&&document.body.dataset.ready==='true'){clearInterval(ixBoot);try{setupInstruments();}catch(e){console.error(e);document.body.dataset.instrumentsError=e.message;}}},30);

/* Additive operating views. Every write uses the existing review/atomic/storage boundary. */
let opLabView='granary';
const opTitles={granary:'Strategy shares',issue:'Basket issuance',strips:'Matched rights',counter:'Fixed-term credit',scrivener:'Covered calls'};
function opLibraryPage(){const w=W(),a=w.identity(),o=ixEngine.op;return `<div class="ix-grid"><section><div class="op-heading"><div><span class="ix-tag">CODEX / CONTENT ≠ AUTHORITY</span><h3>A library the artifact can use.</h3></div>${B('op-publish','Publish an edition →','','primary',a.owner!==w.s.actor)}</div><p>Describe an exact procedure. Adopt it as content. Authorize only its chosen caller, budget and lifetime. No uploaded JavaScript or arbitrary bytecode runs here.</p>${o.editions.length?o.editions.map(e=>{const installed=o.installations.find(x=>x.identity===a.id&&x.edition===e.id);return `<article class="ix-card op-edition"><header><span class="ix-tag">EDITION ${e.id} · ${esc(e.recipe.kind)}</span><small>by ${personLabel(e.author)}</small></header><h4>${esc(e.title)}</h4><div class="ix-flow"><span>${money(e.recipe.amount,e.recipe.input)}</span><b>→</b><span>${esc(e.recipe.kind)} · ${e.recipe.days} days</span></div><p class="ix-code">${esc(e.contentHash)}</p>${installed?fact('Installation',w.s.now<installed.readyAt?'Review until '+when(installed.readyAt):'Ready to request a grant'):''}<div class="ix-actions">${B('op-inspect-edition','Inspect',e.id)}${!installed?B('op-install','Adopt into library',e.id,'',a.owner!==w.s.actor):B('op-grant','Authorize a use',e.id,'primary',w.s.now<installed.readyAt||a.owner!==w.s.actor)}${B('op-collect','Create rehearsal collectible',e.id,'',a.owner!==w.s.actor)}${B('discuss','Discuss edition',e.receipt)}</div></article>`;}).join(''):`<div class="op-empty"><span>01</span><h4>One useful procedure, held permanently.</h4><p>Start with a reserve lock. The edition does not gain money permissions just because you published it.</p>${B('op-publish','Create the first edition','','primary',a.owner!==w.s.actor)}</div>`}</section><aside><h4>Current execution grants</h4>${o.grants.filter(g=>g.identity===a.id).map(g=>`<article class="ix-card"><span class="ix-tag">GRANT ${g.id} · ${esc(ixEngine.grantStatus(g))}</span><h4>${esc(ixEngine.edition(g.edition).title)}</h4>${fact('May trigger',personLabel(g.caller))}${fact('Remaining budget',money(BigInt(g.total)-BigInt(g.spent),ixEngine.edition(g.edition).recipe.input))}${fact('Calls',g.used+' / '+g.calls)}${fact('Expires',when(g.expires))}<div class="ix-actions">${B('op-run','Review execution',g.id,'primary',ixEngine.grantStatus(g)!=='active'||g.caller!==w.s.actor)}${B('op-revoke','Revoke now',g.id,'',g.revoked||a.owner!==w.s.actor)}</div></article>`).join('')||'<p class="ix-small">No active authority. Published content cannot spend.</p>'}<details open><summary>What survives a sale?</summary><p>Code, fixed obligations, and historical authorship survive. Old grants do not: listing and transfer change the custody epoch.</p><p class="ix-small">Seven-day installation review. Exact recipe. Bounded cumulative budget. Immediate revocation. Local rehearsal only.</p></details>${B('tab','Inspect all commitments →','ledger')}${B('op-applications','Application editions →')}</aside></div>`;}
function opWorkPage(){const a=W().identity();return `<div class="op-heading"><div><span class="ix-tag">PATRONAGE / EXPLICIT AGREEMENTS</span><h3>Turn a conversation into funded work.</h3></div>${B('op-commission','Fund a commission →','','primary',a.owner!==W().s.actor)}</div><p>A fixed worker, deliverable, deadline and reviewer. The reviewer is trusted to judge the work—not an AI oracle. A silent reviewer leads to the disclosed refund, not automatic payment.</p><div class="ix-cards">${ixEngine.op.commissions.map(c=>`<article class="ix-card"><header><span class="ix-tag">COMMISSION ${c.id} · ${esc(c.status)}</span><small>${money(c.amount,c.asset)}</small></header><h4>${esc(c.title)}</h4>${fact('Worker / reviewer',personLabel(c.worker)+' / '+personLabel(c.reviewer))}${fact('Submit before',when(c.deadline))}${fact('Review ends',when(c.reviewEnds))}${fact('Funding account','#'+c.identity)}${c.edition?fact('Delivered edition','#'+c.edition):''}<div class="ix-actions">${c.status==='offered'?B('op-accept-work','Accept terms',c.id,'primary',W().s.actor!==c.worker||W().s.now>=c.deadline):''}${c.status==='accepted'?B('op-submit-work','Submit your edition',c.id,'primary',W().s.actor!==c.worker||W().s.now>=c.deadline||!ixEngine.op.editions.some(e=>e.author===c.worker)):''}${c.status==='submitted'?B('op-approve-work','Accept & pay',c.id,'primary',W().s.actor!==c.reviewer||W().s.now>=c.reviewEnds)+B('op-reject-work','Reject & refund',c.id,'',W().s.actor!==c.reviewer||W().s.now>=c.reviewEnds):''}${['offered','accepted','submitted'].includes(c.status)?B('op-refund-work','Refund after timeout',c.id,'',W().s.now<(c.status==='submitted'?c.reviewEnds:c.deadline)):''}${B('discuss','Open discussion',c.receipt)}</div></article>`).join('')||`<div class="op-empty"><h4>A promise should carry its funding.</h4><p>Fund work for Guest, then switch to Guest and select artifact #2 to publish a deliverable. Nothing is posted in someone’s name automatically.</p></div>`}</div>`;}
function opLedgerPage(){const l=ixEngine.ledger(),m=l.modules;const cards=[['Committed gifts',m.gifts.filter(x=>['offered','accepted'].includes(x.status))],['Funded work',m.commissions.filter(x=>['offered','accepted','submitted'].includes(x.status))],['Operating venues',m.venues.filter(x=>x.status!=='closed')],['Laboratory cells',m.cells],['Credit obligations',m.loans.filter(x=>['offered','active'].includes(x.status))],['Covered options',m.options.filter(x=>['offered','active'].includes(x.status))]];return `<div class="ix-grid"><section><span class="ix-tag">ONE ARTIFACT / SEPARATE RIGHTS</span><h3>Know what is free. Know what is promised.</h3><p>These are native quantities, not a combined dollar valuation. Changing the owner does not change another person’s accepted deadline.</p><div class="ix-cards">${cards.map(([label,items])=>`<article class="ix-card"><h4>${label}</h4><strong class="op-count">${items.length}</strong><details><summary>Inspect records</summary><pre>${esc(JSON.stringify(items,null,2))}</pre></details></article>`).join('')}</div><h4 style="margin-top:24px">Authority is not an asset balance</h4>${l.activeGrants.map(g=>fact('Grant '+g.id,esc(g.status)+' · remaining '+format(g.remaining))).join('')||'<p>No spending grants.</p>'}</section><aside><h4>Available in the main account</h4>${balanceFacts()}<h4 style="margin-top:25px">Personal wallet · ${personLabel(W().s.actor)}</h4>${Object.entries(ixEngine.extra.personalBalances[W().s.actor]||{}).map(([k,v])=>fact(esc(k),money(v,k))).join('')||'<p class="ix-small">No personal gift or work proceeds.</p>'}<p class="ix-small">${esc(l.note)}</p>${B('op-manifest','Inspect complete module snapshot')}${B('tab','Whole-artifact market →','market')}${B('op-venues','Operate collectible shelves →')}</aside></div>`;}
function opVenuesPage(){const a=W().identity(),o=ixEngine.op;return `<div class="op-heading"><div><span class="ix-tag">BOURSE / FIXED-TERM SELL SHELVES</span><h3>The artifact can own the venue.</h3></div>${B('op-open-shelf','Open a shelf →','','primary')}</div><p>One local collectible, a fixed ETH ask, and a fixed bond date. Receipts and reserved sale proceeds follow the owning account. A bond promises neither a buyer nor a price floor.</p><div class="ix-cards">${o.venues.map(v=>`<article class="ix-card"><span class="ix-tag">SHELF ${v.id} · ${esc(v.status)}</span><h4>Collectible #${v.collectible} · edition #${opGet(o.collectibles,v.collectible).edition}</h4>${fact('Venue account','#'+v.identity)}${fact('Fixed ask',money(v.price,'ETH'))}${fact('Withdrawal date',when(v.bondUntil))}${fact('Reserved proceeds',money(v.reserve,'ETH'))}<div class="ix-actions">${v.status==='open'?B('op-buy-shelf','Review purchase',v.id,'primary'):''}${v.status!=='closed'?B('op-close-shelf','Close / collect proceeds',v.id):''}</div></article>`).join('')||'<p class="ix-small">Publish an edition in Library, create a rehearsal collectible, then put it on a shelf. Code remains public; the collectible grants no exclusive intellectual-property rights.</p>'}</div><h4 style="margin-top:25px">Your free collectibles</h4>${o.collectibles.filter(t=>t.holder===a.id&&!t.venue).map(t=>fact('Collectible '+t.id,'Edition '+t.edition)).join('')}${B('tab','Return to Market','market')}`;}
function opLabPage(){const a=W().identity(),c=ixEngine.op.cells.find(x=>x.identity===a.id),mod=opLabView;return `<div class="op-heading"><div><span class="ix-tag op-experimental">EXPERIMENTAL / NO LIVE EXECUTION</span><h3>One architecture. A separate risk compartment.</h3></div>${B('op-allocate','Allocate laboratory capital →','','primary',a.owner!==W().s.actor)}</div><p>The lab cannot debit your main account. Each module requires explicit opt-in in this local engine. Oracle-based leverage, outcome books and exclusive keeper seats remain specified but not executable.</p><nav class="op-lab-tabs" aria-label="Laboratory modules">${LAB_MODULES.map(k=>B('op-lab-tab',opTitles[k],k,k===mod?'primary':'')).join('')}</nav>${c?`<div class="ix-grid"><section><div class="ix-card"><h4>${opTitles[mod]}</h4>${c.enabledEpoch===a.epoch&&c.enabled.includes(mod)?opLabControls(mod,c):`<p>This module is locked for artifact #${a.id}. Enabling it permits only the following explicit experiments against this cell.</p>${B('op-enable','Enable this local experiment',mod,'primary',a.owner!==W().s.actor)}${['granary','issue','strips'].includes(mod)?`<div class="ix-actions">${B(mod==='granary'?'op-reap':mod==='issue'?'op-basket-redeem':'op-unstrip','Exit an existing position')}</div>`:''}`}</div>${opLabRecords(mod,c)}</section><aside><h4>Cell #${c.id} · spendable</h4>${Object.entries(c.balances).map(([k,v])=>fact(esc(k),money(v,k))).join('')||'<p>Empty.</p>'}<p class="ix-small">Collateral and deployed strategy principal do not appear in this free balance.</p>${B('op-return','Return free capital →')}<details><summary>Main account stays separate</summary>${balanceFacts()}</details>${B('tab','Commitment ledger →','ledger')}</aside></div>`:`<div class="op-empty"><span>00</span><h4>No cell. No access to the main balance.</h4><p>Choose an explicit allocation first. Nothing in this laboratory is allowed to pull directly from the NFT’s main account.</p></div>`}<details style="margin-top:24px"><summary>Not runnable: House / Wager / Wake</summary><p>House requires a coherent payout and loss waterfall. Wager requires a question commitment, resolution policy and nonoverlapping deadlines. Wake requires a pull-payment takeover and a rent/liveness model. Their interface specifications, failure cases and proposed contract boundaries ship in the source package. There is no “unlock” that makes those missing implementations appear.</p></details>`;}
function opLabControls(mod,c){if(mod==='granary')return `<p>Illustrative AUR strategy. Deposit and withdrawal receipts are pinned to this cell. No live strategy or yield feed.</p>${fact('Assets / shares',format(c.strategy.assets)+' / '+format(c.strategy.shares))}<div class="ix-actions">${B('op-sow','Deposit AUR')}${B('op-reap','Redeem shares')}${B('op-stress','Test a 50% loss','granary','danger')}</div>`;if(mod==='issue')return `<p>Fixed recipe per share: 0.0001 ETH + 10 AUR. Required inputs round up; redemption rounds down. Final dust returns when the local basket closes.</p>${fact('Issued shares',format(c.basket.supply))}${fact('ETH reserve',money(c.basket.ETH,'ETH'))}${fact('AUR reserve',money(c.basket.AUR,'AUR'))}<div class="ix-actions">${B('op-basket-mint','Mint basket shares')}${B('op-basket-redeem','Redeem shares')}</div>`;if(mod==='strips')return `<p>Matched principal-and-income pairs. Recombination bears a pro-rata share of losses even before maturity. Independent PT/YT trading and yield accrual are not implemented here.</p>${c.strip?fact('Series assets / paired units',format(c.strip.assets)+' / '+format(c.strip.pairs))+fact('Maturity',when(c.strip.maturity)):''}<div class="ix-actions">${B('op-strip','Create matched pairs')}${B('op-unstrip','Recombine pairs')}${B('op-stress','Test a 50% loss','strips','danger')}</div>`;if(mod==='counter')return `<p>Negotiated, fixed-term ETH credit against explicitly escrowed AUR. No price oracle, automatic valuation, root-NFT pawn, or root recourse. The lender can lose if the collateral is worth less than the debt.</p>${B('op-request-loan','Request funded credit →','','primary')}`;return `<p>Physically settled AUR calls. Cover stays escrowed after a buyer accepts. Premium and strike are in ETH. No naked options or automatic exercise.</p>${B('op-write-option','Write a covered call →','','primary')}`;}
function opLabRecords(mod,c){const owned=W().identity(c.identity).owner===W().s.actor,entry=owned&&c.enabledEpoch===W().identity(c.identity).epoch&&c.enabled.includes(mod);if(mod==='counter')return ixEngine.op.loans.map(l=>`<article class="ix-card"><span class="ix-tag">CREDIT ${l.id} · ${esc(l.status)}</span>${fact('Borrower / lender','#'+l.borrower+' / '+(l.lender?'#'+l.lender:'not funded'))}${fact('AUR collateral',format(l.collateral))}${fact('ETH principal / repay',format(l.principal)+' / '+format(l.repayment))}${fact(l.due?'Due':'Offer ends',when(l.due||l.offerUntil))}<div class="ix-actions">${l.status==='offered'?B('op-fund-loan','Fund from your cell',l.id,'',!entry||c.identity===l.borrower||W().s.now>=l.offerUntil):''}${l.status==='active'?B('op-repay-loan','Repay',l.id,'',!owned||c.identity!==l.borrower||W().s.now>=l.due):''}${['active','offered'].includes(l.status)?B('op-close-loan','Cancel / settle deadline',l.id):''}</div></article>`).join('');if(mod==='scrivener')return ixEngine.op.options.map(o=>`<article class="ix-card"><span class="ix-tag">OPTION ${o.id} · ${esc(o.status)}</span>${fact('Writer / buyer','#'+o.writer+' / '+(o.buyer?'#'+o.buyer:'not taken'))}${fact('Cover',money(o.underlying,'AUR'))}${fact('Premium / strike',money(o.premium,'ETH')+' / '+money(o.strike,'ETH'))}${fact('Expires',when(o.expiry))}<div class="ix-actions">${o.status==='offered'?B('op-take-option','Pay premium',o.id,'',!entry||c.identity===o.writer||W().s.now>=o.expiry):''}${o.status==='active'?B('op-exercise','Exercise with ETH',o.id,'',!owned||c.identity!==o.buyer||W().s.now>=o.expiry):''}${['active','offered'].includes(o.status)?B('op-close-option','Cancel / reclaim at expiry',o.id):''}</div></article>`).join('');return '';}
function opApplicationPage(){return `<span class="ix-tag">SITEKERNEL / VERSIONED APPLICATION CONTENT</span><h3>Know which application you are opening.</h3><p>Record an exact SHA-256 and byte length. This saves a commitment only; it does not fetch, execute or deploy arbitrary code. The package separately contains the recoverable HTML and its prepared onchain chunks.</p>${B('op-application-form','Register an application manifest →','','primary')}<div class="ix-cards" style="margin-top:20px">${ixEngine.op.applications.map(x=>`<article class="ix-card"><h4>${esc(x.title)}</h4>${fact('Byte length',x.bytes)}<p class="ix-code">${esc(x.hash)}</p><p class="ix-small">Immutable local record ${x.id} · not deployed.</p></article>`).join('')}</div>`;}
function opRead(title,body){ixReview=null;ix$('#ix-content').innerHTML=`<h3>${esc(title)}</h3>${body}${B('tab','Return to Library','library')}`;}
function opReview(title,body,fn){simpleReview(title,'Explicit local rehearsal only. The original organism and live wallets remain unchanged.',body,fn);}
async function opAction(act,id){const e=ixEngine,w=W();
 if(act==='op-lab-tab'){opLabView=id;return nav('lab');}
 if(act==='op-venues')return nav('venues');if(act==='op-applications')return nav('applications');
 if(act==='op-publish'){modalForm('Publish a typed edition.',field('Edition title','ed-title','Reserve lock')+field('Amount · AUR','ed-amount','100')+field('Lock duration · days','ed-days',30,'number')+'<p>First instrument: exact AUR → fixed-beneficiary lock. No arbitrary code or variable recipient. Other composed recipes are supported by the model API.</p>','op-edition-form');return;}
 if(act==='op-inspect-edition')return opRead('Edition '+id,`<pre>${esc(JSON.stringify(e.edition(id),null,2))}</pre>`);
 if(act==='op-install')return opReview('Adopt this edition.',fact('Edition',esc(e.edition(id).title))+fact('Permission','None; seven-day installation review'),()=>e.install(Number(id)));
 if(act==='op-grant'){ixInspect=Number(id);modalForm('Grant a bounded use.',`<label for="ix-grant-caller">Allowed caller</label><select id="ix-grant-caller"><option value="you">You</option><option value="guest">Guest</option></select>${field('Per-call ceiling · AUR','grant-per',format(e.edition(id).recipe.amount).replaceAll(',',''))}${field('Total lifetime ceiling · AUR','grant-total',format(e.edition(id).recipe.amount).replaceAll(',',''))}${field('Maximum calls','grant-calls',1,'number')}${field('Valid for days','grant-days',7,'number')}`,'op-grant-form');return;}
 if(act==='op-run')return opReview('Execute the authorized edition.',fact('Grant',id)+fact('Bound procedure',esc(e.edition(opGet(e.op.grants,id).edition).title)),()=>e.run(Number(id)));
 if(act==='op-revoke')return opReview('Revoke now.',fact('Grant',id),()=>e.revoke(Number(id)));
 if(act==='op-collect')return opReview('Create a rehearsal collectible.',`<p>Public code remains public. This local collectible is not an exclusive license or a deployed ERC-721.</p>${fact('Edition',id)}`,()=>e.mintCollectible(Number(id)));
 if(act==='op-commission'){modalForm('Fund a clear promise.',field('Deliverable','work-title','Publish a useful reserve instrument')+`<label for="ix-worker">Worker</label><select id="ix-worker"><option value="${w.s.actor==='you'?'guest':'you'}">${personLabel(w.s.actor==='you'?'guest':'you')}</option></select>`+field('Funding · AUR','work-amount','250')+field('Delivery within days','work-days',14,'number')+'<p>You remain the named reviewer even if the artifact changes hands. No decision by the end of the three-day review grace refunds the funding account. The worker accepts this policy explicitly.</p>','op-work-form');return;}
 if(act==='op-accept-work')return opReview('Accept the commission terms.',`<pre>${esc(JSON.stringify(opGet(e.op.commissions,id),null,2))}</pre>`,()=>e.acceptCommission(Number(id)));
 if(act==='op-submit-work'){ixInspect=Number(id);modalForm('Link your delivered edition.',`<label for="ix-work-edition">Your publication</label><select id="ix-work-edition">${e.op.editions.filter(x=>x.author===w.s.actor).map(x=>`<option value="${x.id}">${esc(x.title)}</option>`).join('')}</select>`,'op-deliver-form');return;}
 if(act==='op-approve-work'||act==='op-reject-work')return opReview(act==='op-approve-work'?'Accept work and pay.':'Reject and refund.',fact('Commission',id),()=>e.decideCommission(Number(id),act==='op-approve-work'));
 if(act==='op-refund-work')return opReview('Apply the fixed timeout.',fact('Commission',id),()=>e.refundCommission(Number(id)));
 if(act==='op-manifest')return opRead('Declared operating contents.',`<pre>${esc(JSON.stringify(e.moduleSnapshot(Number(id)||w.s.selected),null,2))}</pre>`);
 if(act==='op-refresh-listing')return opReview('Refresh changed contents.',`<p>This replaces the advertised manifest. A buyer must review the new commitment; their old review cannot complete.</p>`,()=>e.refreshListing(Number(id)));
 if(act==='op-open-shelf'){modalForm('Open a bonded sell shelf.',`<label for="ix-shelf-item">Free collectible</label><select id="ix-shelf-item">${e.op.collectibles.filter(t=>t.holder===w.s.selected&&!t.venue).map(t=>`<option value="${t.id}">Collectible ${t.id} · edition ${t.edition}</option>`).join('')}</select>`+field('Fixed ask · ETH','shelf-price','0.1')+field('Bonded for days','shelf-days',7,'number'),'op-shelf-form');return;}
 if(act==='op-buy-shelf')return opReview('Buy the declared collectible.',fact('Shelf',id)+fact('ETH ask',format(opGet(e.op.venues,id).price)),()=>e.buyShelf(Number(id)));
 if(act==='op-close-shelf')return opReview('Close the shelf after its bond.',fact('Shelf',id),()=>e.closeShelf(Number(id)));
 if(act==='op-allocate'||act==='op-return'){modalForm(act==='op-allocate'?'Allocate a bounded laboratory balance.':'Return only uncommitted laboratory funds.',`<label for="ix-lab-asset">Asset</label><select id="ix-lab-asset">${assets('AUR')}</select>`+field('Amount','lab-amount',act==='op-allocate'?'1000':'100')+'<p>Collateral, funded obligations and invested principal cannot be withdrawn as free balance.</p>',act==='op-allocate'?'op-allocate-form':'op-return-form');return;}
 if(act==='op-enable')return opReview('Enable '+opTitles[id]+'.',`<p>Only this already-funded laboratory cell. No main-account spending authority or live deployment.</p>`,()=>e.enableLab(id));
 if(act==='op-stress')return opReview('Apply a hypothetical 50% loss.',`<p>Half of this local position’s remaining underlying moves into the scenario loss ledger. Main balances are not touched. This is a stress test, not a market quote.</p>`,()=>e.stress(id,5000));
 const amounts={'op-sow':['Deposit AUR into strategy shares.','100'],'op-reap':['Redeem strategy shares.','50'],'op-basket-mint':['Mint basket shares.','10'],'op-basket-redeem':['Redeem basket shares.','5'],'op-strip':['Create matched principal / income pairs.','100'],'op-unstrip':['Recombine matched pairs.','50']};
 if(amounts[act]){const[t,n]=amounts[act];modalForm(t,field('Amount or paired units','lab-quantity',n),act+'-form');return;}
 if(act==='op-request-loan'){modalForm('Request fixed-term credit.',field('Collateral · AUR','credit-cover','500')+field('Receive · ETH','credit-principal','0.02')+field('Fixed repayment · ETH','credit-repay','0.022')+field('Term after funding · days','credit-days',14,'number'),'op-credit-form');return;}
 const actions={'op-fund-loan':['Fund this negotiated loan.',()=>e.fundLoan(Number(id))],'op-repay-loan':['Repay and recover collateral.',()=>e.repayLoan(Number(id))],'op-close-loan':['Apply the credit exit rules.',()=>e.closeLoan(Number(id))],'op-take-option':['Pay the option premium.',()=>e.takeOption(Number(id))],'op-exercise':['Exchange the strike for the covered asset.',()=>e.exercise(Number(id))],'op-close-option':['Apply the option expiry or cancellation.',()=>e.closeOption(Number(id))]};if(actions[act])return opReview(actions[act][0],fact('Record',id),actions[act][1]);
 if(act==='op-write-option'){modalForm('Write a physically covered call.',field('Escrowed cover · AUR','option-cover','100')+field('Total exercise price · ETH','option-strike','0.015')+field('Premium · ETH','option-premium','0.001')+field('Expires in days','option-days',14,'number'),'op-option-form');return;}
 if(act==='op-application-form'){modalForm('Record an application edition.',field('Title','app-title','Instrument client edition')+field('Exact SHA-256 · 0x prefix','app-hash','0x')+field('Byte length','app-bytes',1,'number'),'op-application-form');return;}
 throw Error('Unknown operating action.');
}
async function opSubmit(form){const e=ixEngine;let fn,body='';
 if(form==='op-edition-form'){const n=wei(input('ed-amount')),title=input('ed-title'),raw={kind:'LOCK',input:'AUR',output:'AUR',amount:String(n),minOut:String(n),days:Number(input('ed-days')),recipient:null};e.normalize(raw);body=fact('Title',esc(title))+fact('Amount',money(n,'AUR'))+fact('Fixed lock',raw.days+' days');fn=()=>e.publish(title,raw);}
 else if(form==='op-grant-form'){const id=ixInspect,g={caller:input('grant-caller'),perCall:wei(input('grant-per')),total:wei(input('grant-total')),calls:Number(input('grant-calls')),days:Number(input('grant-days'))};body=fact('Caller',personLabel(g.caller))+fact('Total ceiling',money(g.total,'AUR'))+fact('Expiry',when(W().s.now+g.days*DAY));fn=()=>e.grant(id,g);}
 else if(form==='op-work-form'){const c={title:input('work-title'),worker:input('worker'),asset:'AUR',amount:wei(input('work-amount')),days:Number(input('work-days')),room:ixRoom};body=fact('Deliverable',esc(c.title))+fact('Prefunded',money(c.amount,'AUR'));fn=()=>e.commission(c);}
 else if(form==='op-deliver-form'){const id=ixInspect,edition=Number(input('work-edition'));body=fact('Delivered edition',edition);fn=()=>e.submitCommission(id,edition);}
 else if(form==='op-shelf-form'){const id=Number(input('shelf-item')),price=wei(input('shelf-price')),days=Number(input('shelf-days'));body=fact('Collectible',id)+fact('Fixed price',money(price,'ETH'))+fact('Bond',days+' days');fn=()=>e.openShelf(id,price,days);}
 else if(form==='op-allocate-form'||form==='op-return-form'){const asset=input('lab-asset'),n=wei(input('lab-amount'));body=fact('Asset',money(n,asset))+fact('Direction',form==='op-allocate-form'?'Main account → laboratory cell':'Free laboratory funds → main account');fn=()=>form==='op-allocate-form'?e.allocate(asset,n):e.withdrawLab(asset,n);}
 else if(['op-sow-form','op-reap-form','op-basket-mint-form','op-basket-redeem-form','op-strip-form','op-unstrip-form'].includes(form)){const n=wei(input('lab-quantity')),method={'op-sow-form':'sow','op-reap-form':'reap','op-basket-mint-form':'mintBasket','op-basket-redeem-form':'redeemBasket','op-strip-form':'strip','op-unstrip-form':'unstrip'}[form];body=fact('Quantity / paired units',format(n));fn=()=>e[method](n);}
 else if(form==='op-credit-form'){const c=wei(input('credit-cover')),p=wei(input('credit-principal')),r=wei(input('credit-repay')),d=Number(input('credit-days'));body=fact('Collateral',money(c,'AUR'))+fact('Principal / repay',money(p,'ETH')+' / '+money(r,'ETH'))+fact('Term',d+' days');fn=()=>e.requestLoan(c,p,r,d);}
 else if(form==='op-option-form'){const c=wei(input('option-cover')),s=wei(input('option-strike')),p=wei(input('option-premium')),d=Number(input('option-days'));body=fact('Covered underlying',money(c,'AUR'))+fact('Premium / strike',money(p,'ETH')+' / '+money(s,'ETH'));fn=()=>e.writeOption(c,s,p,d);}
 else if(form==='op-application-form'){const title=input('app-title'),hash=input('app-hash'),bytes=Number(input('app-bytes'));body=fact('Bytes',bytes)+`<p class="ix-code">${esc(hash)}</p>`;fn=()=>e.registerApplication(title,hash,bytes);}
 else throw Error('Unknown operating form.');
 return opReview('Review this operating change.',body,fn);
}

function opMarketFacts(id){const snap=ixEngine.moduleSnapshot(id),a=W().identity(id);return `<hr class="ix-divider"><h4>Operating contents</h4>${fact('Known gifts',snap.gifts.length)}${fact('Installed editions',snap.installations.length)}${fact('Spending grants',snap.grants.length+' · previous epoch retires on transfer')}${fact('Funded work records',snap.commissions.length)}${fact('Market shelves',snap.venues.length)}${fact('Laboratory cells',snap.cells.length)}${fact('Credit / option records',snap.loans.length+' / '+snap.options.length)}${B('op-manifest','Inspect declared module snapshot',id)}<p class="ix-small" style="margin-top:12px">A sale keeps existing commitments. Old grants and laboratory entry permissions do not remain valid for the buyer. A counterparty action can change this manifest; the seller must rebind it and the buyer must review it again.</p>`;}

/* Private bodies and memory passphrases exist only in this controller's volatile cache. */
let memOpen=0,memParent=0,memDraft=null,memQuery='',memMode='all',memFormThrough=null,memFormOriginal=false,memWorking=false;
let memSessionPass='',memSessionActor='',memSessionAt=0,memSessionEpoch=0;
const memUnlocked=new Map();
function memLock(){memSessionEpoch++;memSessionPass='';memSessionActor='';memSessionAt=0;memUnlocked.clear();memDraft=null;memParent=0;memQuery='';if(ixReview?.memoryPrivate)ixReview=null;}
function memCheckSession(){if(memSessionActor&& (memSessionActor!==W().s.actor||Date.now()-memSessionAt>600000)){memLock();return true;}return false;}
function memSession(pass){memSessionPass=pass;memSessionActor=W().s.actor;memSessionAt=Date.now();}
function memText(label,id,value='',rows=3){return `<div class="ix-field"><label for="mem-${id}">${label}</label><textarea id="mem-${id}" rows="${rows}" maxlength="3000">${esc(value)}</textarea></div>`;}
function memVal(id){return ix$('#mem-'+id)?.value||'';}
function memGetBody(){return memoryBody(Object.fromEntries(['title','thesis','setup','invalidation','exitPlan','horizon','risk','tags','feeling','lesson'].map(k=>[k,memVal(k)])));}
function memEditor(trade=false){const d=memDraft||{};return `<div class="mem-editor">${trade?'<label for="mem-thesis">Why this trade?</label>':''}${trade?`<textarea id="mem-thesis" rows="3" maxlength="3000" placeholder="The thesis, not just the ticker. Leaving this blank keeps the swap journal optional.">${esc(d.thesis||'')}</textarea>`:field('Memory title','mem-title',d.title||'').replaceAll('ix-mem-title','mem-title')+memText(memParent?'What changed? What did you learn?':'Something worth remembering','thesis',d.thesis||'',5)}
 <details ${trade?'':'open'}><summary>${trade?'Decision details · optional':'Context · optional'}</summary><div class="ix-row">${memText('Setup / evidence','setup',d.setup||'',2)}${memText('What would invalidate this?','invalidation',d.invalidation||'',2)}</div><div class="ix-row">${memText('Exit / review plan','exitPlan',d.exitPlan||'',2)}${memText('Time horizon','horizon',d.horizon||'',2)}</div><div class="ix-row">${memText('Planned risk · state amount and units','risk',d.risk||'',2)}${memText('Tags · comma separated','tags',d.tags||'',2)}</div>${memText('How I felt · your description, not an AI diagnosis','feeling',d.feeling||'',2)}<p class="ix-small">These are notes, not executable stop-loss orders, forecasts, or risk limits. The swap's separate minimum output still controls execution.</p></details>
 <div class="ix-row"><div class="ix-field"><label for="mem-privacy">Who can read the body?</label><select id="mem-privacy"><option value="encrypted">Private · encrypted with my memory passphrase</option><option value="public">Public · readable in the archive</option></select></div><div class="ix-field"><label for="mem-pass">Memory passphrase ${memSessionPass?'· unlocked for this session':''}</label><input id="mem-pass" type="password" autocomplete="new-password" placeholder="${memSessionPass?'Leave blank to use this session':'At least 12 characters. Not a wallet seed.'}"></div></div>
 <label class="ix-check"><input id="mem-imprint" type="checkbox" checked> Let this entry's opaque commitment influence the form</label><p class="ix-small">The body is not analyzed for shape. Private bodies stay ciphertext in exports; timestamps, authorship, links and approximate length remain visible. Save a unique passphrase separately—losing it loses access. Neither selling the NFT nor switching the demo persona reveals the key.</p></div>`;}
function memTradeEditor(){return `<details class="mem-trade"><summary>Remember why · optional trading journal</summary>${memEditor(true)}</details>`;}
function memBinding(n){return ixEngine.mem.bindings.find(b=>b.entry===n.header.id);}
function memBodyFor(n){return n.payload.mode==='public'?n.payload.body:memUnlocked.get(n.digest);}
function memNoteCard(n){const h=n.header,b=memBodyFor(n),binding=memBinding(n),refs=ixEngine.mem.entries.filter(x=>x.header.parent===h.id);return `<article class="ix-card mem-note"><div class="ix-inline"><h4>${esc(b?.title|| (h.phase==='before'?'Decision before a swap':h.phase==='reflection'?'A later reflection':'A memory'))}</h4><span class="ix-tag">${n.payload.mode==='public'?'PUBLIC':b?'DECRYPTED HERE':'ENCRYPTED'}</span></div><p class="ix-small">${esc(personLabel(h.author))} · artifact #${h.identity} · custody ${h.epoch} · ${when(h.time)} · ${h.phase}</p><p class="mem-excerpt">${b?esc((b.thesis||b.lesson).slice(0,200)):'Only the body is locked. Its context and commitment are inspectable.'}</p>${binding?`<p class="ix-small">Linked successful local swap #${binding.receipt} · ${money(binding.result.received,binding.result.asset)}</p>`:''}<div class="ix-actions">${B('mem-open','Read & compare →',h.id)}${refs.length?`<span class="ix-small">${refs.length} later reflection${refs.length>1?'s':''}</span>`:''}</div></article>`;}
function memPage(){memCheckSession();if(memOpen)return memReadPage();const w=W(),own=ixEngine.mem.entries.filter(n=>n.header.author===w.s.actor),trades=w.s.events.filter(e=>e.kind==='SWAPPED'&&e.actor===w.s.actor),before=own.filter(n=>n.header.phase==='before'),reviewed=before.filter(n=>own.some(r=>r.header.parent===n.header.id));const notes=own.filter(n=>{const b=memBodyFor(n);return(memMode==='all'||memMode==='review'?memMode!=='review'||n.header.phase==='before'&&!own.some(r=>r.header.parent===n.header.id):n.header.phase===memMode)&&(!memQuery||b&&stable(b).toLowerCase().includes(memQuery.toLowerCase()));});
 const invalidations=before.map(memBodyFor).filter(Boolean).filter(b=>b.invalidation).length,readable=before.filter(n=>memBodyFor(n)).length;
 return `<div class="mem-heading"><span class="ix-kicker">MEMORY / DECISIONS, NOT JUST TRANSACTIONS</span><h3>Remember what you knew then.</h3><p>Keep the reason before an action and the reflection after it. Neither overwrites the other.</p></div><div class="mem-stats">${fact('Recorded swaps',trades.length)}${fact('With a before-note',before.length)}${fact('Reviewed decisions',reviewed.length+' / '+before.length)}${fact('Invalidation recorded',invalidations+' / '+readable+' readable notes')}</div>
 <div class="ix-actions">${B('mem-new','Write any memory →')}${B('mem-form','Explore lived form →')}${B('mem-lock','Lock private bodies')}${B('mem-export-fills','Export execution CSV')}</div><p class="ix-small">Process statistics, not a profitability score. No current prices, tax basis, gas valuation, portfolio P&L, or guaranteed improvement are inferred.</p>
 <div class="ix-row"><div class="ix-field"><label for="mem-search">Search readable bodies</label><input id="mem-search" value="${esc(memQuery)}" placeholder="Thesis, tags, lesson…"></div><div class="ix-field"><label for="mem-filter">View</label><select id="mem-filter">${[['all','All my memories'],['before','Before a swap'],['memory','General memories'],['reflection','Reflections'],['review','Awaiting reflection']].map(([v,n])=>`<option value="${v}" ${memMode===v?'selected':''}>${n}</option>`).join('')}</select></div></div>
 <p class="ix-small">Private search works only on bodies explicitly decrypted in this session. You are viewing ${esc(personLabel(w.s.actor))}'s journal; changing the demo persona is not authentication.</p><div class="ix-stack">${notes.length?notes.slice().reverse().map(memNoteCard).join(''):'<div class="ix-card"><h4>A record begins with one honest sentence.</h4><p>Use “Remember why” inside Trade, or write a memory here without moving any money.</p></div>'}</div>`;}
function memReadPage(){const n=ixEngine.entry(memOpen),h=n.header,b=memBodyFor(n),bind=memBinding(n),r=bind?W().s.events.find(e=>e.seq===bind.receipt):null;return `${B('mem-back','← My journal')}<h3>${esc(b?.title||'A preserved moment.')}</h3>${fact('Authored by',esc(personLabel(h.author)))}${fact('Artifact / custody at inscription','#'+h.identity+' / '+h.epoch)}${fact('Recorded local time',when(h.time))}${fact('Phase',h.phase)}<span class="ix-code">${esc(n.digest)}</span>
 ${!b?`<form id="mem-unlock-form"><label for="mem-unlock-pass">Memory passphrase</label><input id="mem-unlock-pass" type="password" autocomplete="current-password"><button class="ix-button primary" type="submit">Decrypt on this device →</button></form><p class="ix-small">No server receives this passphrase. A compromised browser can still read an unlocked memory. Lost passphrases cannot be reset.</p>`:`<div class="mem-body">${Object.entries(b).filter(([,v])=>v).map(([k,v])=>`<section><h4>${esc({thesis:'What I thought',exitPlan:'Exit / review plan',invalidation:'What would invalidate this',risk:'Planned risk',feeling:'Self-described feeling',lesson:'Lesson'}[k]||k)}</h4><p>${esc(v)}</p></section>`).join('')}</div>`}
 ${r?`<hr class="ix-divider"><h4>What actually happened in the rehearsal</h4>${fact('Provided',money(r.data.amount,r.data.input))}${fact('Received',money(r.data.received,r.data.output))}${fact('Linked receipt','#'+r.seq)}${fact('Output commitment',bind?.result.exitPlanId?'Vesting exit #'+bind.result.exitPlanId:r.data.lock?'Lock #'+r.data.lock:'None')}${bind?.result.exitPlanId?B('tab','Follow scheduled sales & receipts','exits'):''}<span class="ix-code">${esc(r.digest)}</span><p class="ix-small">The note was appended before execution within one atomic local operation. It is not an earlier blockchain timestamp or evidence that a decision was good.</p>`:''}
 <div class="ix-actions">${h.author===W().s.actor?B('mem-reflect','Add a reflection →',h.id):''}${B('trace','Inspect inscription receipt',n.event)}${B('mem-form-at','View this activity layer',n.event)}</div><p class="ix-small">Reflections append. They cannot silently edit this moment. Personal plaintext and decryption keys are never automatically transferred with the artifact.</p><div class="ix-stack">${ixEngine.mem.entries.filter(x=>x.header.parent===h.id).map(memNoteCard).join('')}</div>`;}
function memWritePage(){return `${B('mem-back','← My journal')}<h3>${memParent?'A reflection, not a rewrite.':'Keep a moment.'}</h3><p>${memParent?'The original entry stays untouched. This note is authored now, under your original authorship.':'An idea, a lesson, a milestone, or a personal memory. No trade is necessary.'}</p><form id="mem-write-form">${memEditor()}<button class="ix-button primary" type="submit">Review the inscription →</button></form>`;}
function memFormPage(){const live=ixEngine.form(),at=memFormThrough===null?live:ixEngine.form(undefined,memFormThrough);return `<span class="ix-kicker">LIVED FORM / DETERMINISTIC LOCAL HISTORY</span><h3>The same organism. A life of its own.</h3><p>Its density domain, axis proportions, angular folds and twist now develop from typed activity. This is geometry and light structure—not a new mascot or just a hue change.</p><div class="ix-actions">${B('mem-toggle-form',memFormOriginal?'Return to lived form':'Compare original form')}${B('mem-present','Return to present')}${B('mem-watch','Close panel & watch')}</div>${fact('View',memFormOriginal?'Original optical field':memFormThrough===null?'Current lived form':'Historical activity layer · current base genome')}${fact('Contributing records',at.count)}<div class="mem-stats">${FORM_CHANNELS.map((k,i)=>fact(k,at.counts[i])).join('')}</div><span class="ix-code">History root ${esc(at.root)}</span><p class="ix-small">Amount and profit are not growth scores. Each category has diminishing influence; all deformation is bounded. Private text is never read to shape the body. A visual projection is not guaranteed unique and has no rarity or return promise.</p>
 <div class="mem-timeline">${B('mem-form-at','Before this chapter',ixEngine.mem.start)}${live.marks.slice(-100).reverse().map(m=>B('mem-form-at','#'+m.seq+' · '+esc(m.kind)+' · '+when(m.time),m.seq)).join('')}</div><p class="ix-small">This chapter begins when v1.7 opens or an older archive is explicitly imported. It does not secretly reinterpret earlier activity. This timeline replays the activity layer on the current base genome; manual Evolve snapshots remain in the original History. The original audit remains separate. Viewing an older form does not roll back any asset or permission.</p>`;}
async function memSealDraft(phase,plan=null){const sessionEpoch=memSessionEpoch;const body=memGetBody(),mode=memVal('privacy'),pass=memVal('pass')||memSessionPass;if(!['encrypted','public'].includes(mode))throw Error('Select a visibility mode.');memDraft=structuredClone(body);const header=ixEngine.memoryHeader({phase,parent:phase==='reflection'?memParent:0,plan:plan?.digest||MEM_ZERO,form:!!ix$('#mem-imprint')?.checked});const payload=mode==='public'?{mode:'public',body}:await encryptMemory(header,body,pass);if(memSessionEpoch!==sessionEpoch||document.hidden||W().s.actor!==header.author||W().s.selected!==header.identity||W().s.now!==header.time)throw Error('The journal context changed while preparing encryption. Review again.');if(mode==='encrypted')memSession(pass);return memorySeal(header,payload);}
async function memPrepareTrade(plan){if(!memVal('thesis').trim())return planReview(plan);if(memWorking)return;memWorking=true;try{const n=await memSealDraft('before',plan);planReview(plan);ixReview.note=n;ixReview.memoryPrivate=n.payload.mode==='encrypted';ixReview.html+=`<hr class="ix-divider"><h4>Reason preserved with this trade</h4>${fact('Visibility',n.payload.mode==='encrypted'?'Encrypted body · key stays with you':'Public body · readable by everyone with the archive')}${fact('Form contribution',n.header.form?'Opaque commitment only':'None')}<p>${esc(memDraft.thesis)}</p><span class="ix-code">${esc(n.digest)}</span>`;draw();}finally{memWorking=false;}}
async function memSubmit(form){if(memWorking)return;memWorking=true;try{
 if(form==='mem-unlock-form'){const n=ixEngine.entry(memOpen),pass=memVal('unlock-pass'),sessionEpoch=memSessionEpoch,actor=W().s.actor;const body=await decryptMemory(n.header,n.payload,pass);if(memSessionEpoch!==sessionEpoch||document.hidden||W().s.actor!==actor||memOpen!==n.header.id)throw Error('The private session changed while decrypting. No body was opened.');memUnlocked.set(n.digest,body);memSession(pass);draw();notice('Decrypted locally. Lock private bodies to clear this session.');return;}
 if(form==='mem-write-form'){sourceCheck();const n=await memSealDraft(memParent?'reflection':'memory');simpleReview('Inscribe this moment.',n.payload.mode==='encrypted'?'Only encrypted body bytes enter the archive. The passphrase is not saved.':'This body is public in the local archive. Future public-chain storage would be permanently readable.',`<p>${esc(memDraft.thesis)}</p>${fact('Authored by',personLabel(n.header.author))}${fact('Phase',n.header.phase)}<span class="ix-code">${n.digest}</span>`,()=>{ixEngine.appendMemory(n);memOpen=n.header.id;memDraft=null;memParent=0;ixView='journal';});ixReview.memoryPrivate=n.payload.mode==='encrypted';return;}
 }finally{memWorking=false;}}
function memExportFills(){const rows=[['provenance','sequence','local_unix_seconds','artifact','author','input','input_raw','output','output_raw','receipt_digest','before_entry']];for(const e of W().s.events.filter(x=>x.kind==='SWAPPED'&&x.actor===W().s.actor)){const n=ixEngine.mem.bindings.find(x=>x.receipt===e.seq);rows.push(['UNSIGNED LOCAL REHEARSAL',e.seq,e.time,e.identity,e.actor,e.data.input,e.data.amount,e.data.output,e.data.received,e.digest,n?.entry||'']);}const csv=rows.map(r=>r.map(x=>'"'+String(x).replaceAll('"','""')+'"').join(',')).join('\r\n');const url=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'})),a=document.createElement('a');a.href=url;a.download='idfbi-local-executions.csv';a.click();setTimeout(()=>URL.revokeObjectURL(url),10000);}
async function memAction(act,id){if(act==='mem-new'){memParent=0;memDraft=null;memOpen=0;return nav('memory-write');}if(act==='mem-back'){memOpen=0;memParent=0;memDraft=null;return nav('journal');}if(act==='mem-open'){memOpen=Number(id);return nav('journal');}if(act==='mem-reflect'){const n=ixEngine.entry(id);if(n.header.author!==W().s.actor)throw Error('Only the original author can reflect.');W().select(n.header.identity);memParent=n.header.id;memDraft=null;return nav('memory-write');}if(act==='mem-lock'){memLock();draw();return notice('Private bodies and the session passphrase were cleared. Encrypted exports are unchanged.');}if(act==='mem-export-fills')return memExportFills();if(act==='mem-form')return nav('form');if(act==='mem-form-at'){memFormThrough=Number(id);memFormOriginal=false;nav('form');return;}if(act==='mem-present'){memFormThrough=null;memFormOriginal=false;return draw();}if(act==='mem-toggle-form'){memFormOriginal=!memFormOriginal;return draw();}if(act==='mem-watch'){ix$('#instrument-dialog').close();return;}throw Error('Unknown memory action.');}
function memAfterDraw(){memCheckSession();const q=ix$('#mem-search');if(q)q.addEventListener('change',()=>{memQuery=q.value;draw();});const f=ix$('#mem-filter');if(f)f.addEventListener('change',()=>{memMode=f.value;draw();});if(ixView==='trade'&&!ixReview){ix$('#ix-trade-form .primary').insertAdjacentHTML('beforebegin',memTradeEditor());}memorySyncField();}

/* Native controls are captured by the same blue-object creation coordinator. */
function exEditor(required=false){return `<section class="ex-compose"><label class="ix-check"><input id="ix-exit-enabled" type="checkbox" ${required?'checked disabled':''}> Sell as the lock matures</label><p class="ix-small">A separate funded schedule. Each installment keeps its own minimum received amount and sale window.</p><label for="ix-exit-asset">Receive from future sales</label><select id="ix-exit-asset">${assets('ETH')}</select><div class="ix-row">${field('Installments · 1–64','exit-count',4,'number')}${field('Days between installments','exit-interval',7,'number')}</div>${field('Minimum total received · chosen asset units','exit-total','0.01')}${B('ex-generate','Generate equal installments')}${B('ex-maturity','Sell everything at maturity')}<label for="ix-exit-rows">Custom schedule · days after lock, percentage, minimum received</label><textarea id="ix-exit-rows" rows="5" spellcheck="false">0, 25, 0.0025\n7, 25, 0.0025\n14, 25, 0.0025\n21, 25, 0.0025</textarea>${field('Each sale may execute for this many days','exit-grace',7,'number')}<p class="ix-small">Edit every row. Percentages must total 100%. These are fixed minimums, not price predictions. The last installment receives indivisible allocation dust. Failed sales keep their tokens.</p></section>`;}
function exRead(){const rows=String(input('exit-rows')).trim().split('\n').map(line=>{const [day,percent,min,...rest]=line.split(',').map(v=>v.trim());if(rest.length||!/^\d+(?:\.\d{1,2})?$/.test(percent||''))throw Error('Use one row per installment: days, percentage, minimum received.');const bps=Math.round(Number(percent)*100);return {offsetDays:Number(day),bps,minOut:String(wei(min)),graceDays:Number(input('exit-grace'))};});return {asset:input('exit-asset'),rows};}
function exReview(p){if(!p.intent.exit)return '';const spec=p.intent.exit,rows=exitSlices(p.preview.received,spec,W().s.now+p.intent.days*DAY);return `<h4>Your vesting exit</h4>${fact('Sales receive',esc(spec.asset))}${fact('Proceeds recipient','This artifact account · follows ownership')}<div class="ex-slices">${rows.map(s=>`<article><span>${when(s.due)}</span><strong>${money(s.amount,p.preview.asset)} → at least ${money(s.minOut,spec.asset)}</strong><small>Sale window ends ${when(s.expires)}</small></article>`).join('')}</div><p class="ix-small">Sales require execution. Cancelling stops future sales and preserves each vesting date. Custody changes suspend this authorization. No principal is assigned to fee recipients.</p>`;}
function exPage(){const w=W(),plans=ixEngine.exits.filter(p=>p.identity===w.s.selected);return `<div class="ex-intro"><span class="ix-tag">LOCAL REHEARSAL</span><h3>Let time unfold your decision.</h3><p>Remember the reason. Vest the position. Sell in chosen installments. Return to what actually happened.</p><div class="ix-actions">${B('tab','Buy, remember & schedule','trade')}${B('clock','Rehearsal clock')}${B('ex-new','Schedule an existing balance')}${B('ex-live','Open onchain exit desk')}${B('ex-export','Export exit ledger')}</div><p class="ix-small">No live keeper is configured here. Dates make sales eligible; run an eligible installment to rehearse its execution. Live execution uses the separate VestedExitVault contract and a funded executor.</p></div>${plans.length?plans.slice().reverse().map(p=>{const a=w.identity(p.identity),notes=ixEngine.mem.bindings.filter(b=>b.receipt===p.originReceipt);return `<article class="ix-card ex-plan"><span class="ix-tag">SCHEDULE ${p.id} · ${p.cancelled?'CANCELLED':a.epoch!==p.epoch?'CUSTODY CHANGED':p.paused?'PAUSED':'AUTHORIZED'}</span><h4>${money(p.total,p.input)} becomes ${esc(p.output)}</h4><p>Proceeds stay with artifact #${p.identity}. Locked tokens are excluded from its free balance.</p><div class="ix-actions">${B('trace','Original commitment',p.receipt)}${notes.map(n=>B('mem-open','Read original decision',n.entry)).join('')}${B('ex-pause',p.paused?'Resume sales':'Pause sales',p.id,'',p.cancelled)}${B('ex-cancel','Cancel future sales',p.id,'',p.cancelled)}${a.epoch!==p.epoch?B('ex-reauthorize','Review new owner authority',p.id,'',p.cancelled):''}</div><div class="ex-slices">${p.slices.map(s=>{const status=ixEngine.exitStatus(p,s);return `<article><span>${s.index+1} / ${esc(status.toUpperCase())} · ${when(s.due)}</span><strong>${money(s.amount,p.input)} → ${s.status==='sold'?money(s.received,p.output):'minimum '+money(s.minOut,p.output)}</strong><small>Window closes ${when(s.expires)}</small><div class="ix-actions">${status==='eligible'?B('ex-sell','Review & execute installment',p.id+':'+s.index):''}${['recoverable','expired'].includes(status)?B('ex-recover','Recover matured tokens',p.id+':'+s.index):''}${s.receipt?B('trace','Inspect actual receipt',s.receipt):''}</div></article>`;}).join('')}</div></article>`;}).join(''):'<div class="ix-card"><h4>No future sale is authorized.</h4><p>Start with a swap and enable “Sell as the lock matures,” or schedule an existing balance.</p></div>'}`;}
async function exAction(act,id){if(act==='ex-live')return window.__confluence.open('exit-live');
 if(act==='ex-new')return modalForm('Give an existing balance a schedule.',`${amountBox('AUR','100')}${field('Initial lock in days','days',30,'number')}${exEditor(true)}`,'ex-fund-form');
 if(act==='ex-export')return saveFile({schema:'anima.exit-ledger/1',scope:'local rehearsal',origin:ixSeed,plans:ixEngine.exits,receipts:W().s.events.filter(e=>e.kind.startsWith('EXIT_'))},'anima-genesis-exits.json');
 if(['ex-generate','ex-maturity'].includes(act)){const count=act==='ex-maturity'?1:Number(input('exit-count')),interval=Number(input('exit-interval'));if(!Number.isSafeInteger(count)||count<1||count>64||!Number.isSafeInteger(interval)||interval<1)throw Error('Choose 1–64 installments and a positive interval.');const total=wei(input('exit-total'));if(total<=0n)throw Error('Enter a positive total minimum.');let used=0,minimum=0n;ix$('#ix-exit-rows').value=Array.from({length:count},(_,i)=>{const bps=i===count-1?10000-used:Math.floor(10000/count);used+=bps;const n=i===count-1?total-minimum:total*BigInt(bps)/10000n;minimum+=n;return `${i*interval}, ${bps/100}, ${exDecimal(n)}`;}).join('\n');ix$('#ix-exit-enabled').checked=true;return;}
 const [pid,idx]=String(id).split(':').map(Number),p=ixEngine.exitPlan(pid);
 if(act==='ex-sell'){const s=p.slices[idx];if(ixEngine.exitStatus(p,s)!=='eligible')throw Error('This installment is not eligible.');const q=W().quote(p.input,p.output,BigInt(s.amount));return simpleReview('Execute this matured installment.', 'Only its segregated tokens may be sold. A rejected minimum leaves them pending.',fact('Sell',money(s.amount,p.input))+fact('Current model quote',money(q.out,p.output))+fact('Minimum',money(s.minOut,p.output))+fact('Recipient','Artifact #'+p.identity),()=>ixEngine.executeExit(pid,idx));}
 if(act==='ex-recover')return simpleReview('Recover matured tokens.', 'Return only this installment to its fixed artifact account; no swap occurs.',fact('Amount',money(p.slices[idx].amount,p.input)),()=>ixEngine.recoverExit(pid,idx));
 const action=act==='ex-pause'?(p.paused?'resume':'pause'):act==='ex-cancel'?'cancel':act==='ex-reauthorize'?'reauthorize':null;if(!action)throw Error('Unknown exit action.');return simpleReview(action==='cancel'?'Stop future sales.':action==='reauthorize'?'Bind this schedule to the current owner.':'Change this schedule’s execution state.',action==='cancel'?'Cancelling is final. Tokens remain vested until their original dates.':action==='reauthorize'?'All amounts, minimums and dates stay fixed. Reauthorization leaves sales paused; resume separately.':'This does not move tokens or change their vesting dates.',fact('Schedule',p.id)+fact('Action',action),()=>ixEngine.changeExit(pid,action));
}
function exDecimal(n){const s=String(n).padStart(19,'0');const tail=s.slice(-18).replace(/0+$/,'');return s.slice(0,-18)+(tail?'.'+tail:'');}
function exSubmit(){const a=input('input'),n=wei(input('amount')),raw={kind:'LOCK',input:a,output:a,amount:String(n),minOut:String(n),days:Number(input('days')),recipient:null,exit:exRead()};planReview(ixEngine.prepare(raw,anchor()));}

/* Versioned visual projection, NOT an account state transition. */
let lifeReady=false,lifeInstalling=false,lifeLast='',lifeTarget=Array(8).fill(0),lifeCurrent=Array(8).fill(0),lifeStamp=0,lifeDescriptor=null,lifeProjectionKey='';
const lifeWorker=`let e;onmessage=async({data:d})=>{try{if(d.init){e=(await WebAssembly.instantiate(d.wasm,{math:{sin:Math.sin,cos:Math.cos,exp:Math.exp,pow:Math.pow,atan2:Math.atan2}})).instance.exports;postMessage({ready:true});return;}const start=performance.now();d.params.forEach((v,i)=>e.set_param(i,v));if(!e.render_region(d.w,d.h,d.center,d.y0,d.y1))throw Error('Invalid framebuffer region');const rgba=new Uint8ClampedArray(e.memory.buffer,e.buffer_ptr(),d.w*(d.y1-d.y0)*4).slice();postMessage({rgba,w:d.w,h:d.h,y0:d.y0,y1:d.y1,ms:performance.now()-start,seq:d.seq},[rgba.buffer]);}catch(e){postMessage({error:e.message});}};`;
async function memoryInstallField(){
 const r=window.__idfbi?.renderer;if(lifeInstalling||lifeReady||!r||(!r.gl&&!r.cpuReady))return;lifeInstalling=true;
 try{
  const oldUniforms=r.uniforms.bind(r);r.uniforms=function(){const p=oldUniforms(),now=performance.now(),dt=Math.min(.10,(now-(lifeStamp||now))/1000);lifeStamp=now;let moving=false;lifeCurrent=lifeCurrent.map((x,i)=>{const y=this.motion?x+(lifeTarget[i]-x)*(1-Math.exp(-dt*1.5)):lifeTarget[i];if(Math.abs(y-lifeTarget[i])>.00001)moving=true;return y;});for(let i=0;i<8;i++)p[23+i]=lifeCurrent[i];if(moving)this.dirty=true;return p;};
  if(r.gl){const oldProgram=r.program;r.program=r.makeProgram(LIVED_ASSETS.shader);r._approvedProgram=oldProgram;const oldDraw=r.drawGL.bind(r);r.drawGL=function(){if(!this.original){const gl=this.gl,pr=this.program;gl.useProgram(pr.pr);gl.uniform4fv(gl.getUniformLocation(pr.pr,'life0'),this.params.slice(23,27));gl.uniform4fv(gl.getUniformLocation(pr.pr,'life1'),this.params.slice(27,31));}oldDraw();};r.backend='WebGL 2 · lived field';}
  else{
   r.workers?.forEach(w=>w.terminate());r.workers=[];r.cpu=null;r.cpuBusy=false;r.cpuReady=false;r.cpuJob=null;r.sequence++;r.acceptedSequence=r.sequence;
   const bytes=Uint8Array.from(atob(LIVED_ASSETS.wasm),c=>c.charCodeAt(0));
   try{const count=Math.min(4,Math.max(1,(navigator.hardwareConcurrency||2)-1)),url=URL.createObjectURL(new Blob([lifeWorker],{type:'text/javascript'}));let ready=0;
    for(let i=0;i<count;i++){const w=new Worker(url);r.workers.push(w);w.onerror=e=>{r.cpuReady=false;r.cpuBusy=false;r.notify('Lived field worker unavailable: '+e.message);};w.onmessage=({data:d})=>{if(d.ready){if(++ready===count)r.cpuReady=true;return;}if(d.error){r.cpuBusy=false;r.notify(d.error);return;}const j=r.cpuJob;if(!j||d.seq!==j.seq)return;j.rgba.set(d.rgba,d.y0*d.w*4);j.done++;if(j.done===count){r.cpuBusy=false;r.cpuMs=performance.now()-j.start;r.acceptCPU({...j,ms:r.cpuMs});}};const copy=bytes.slice();w.postMessage({init:true,wasm:copy.buffer},[copy.buffer]);}URL.revokeObjectURL(url);r.backend='WASM lived optical field';
   }catch{r.workers?.forEach(w=>w.terminate());r.workers=null;r.cpu=(await WebAssembly.instantiate(bytes,{math:{sin:Math.sin,cos:Math.cos,exp:Math.exp,pow:Math.pow,atan2:Math.atan2}})).instance.exports;r.cpuReady=true;r.backend='WASM main-thread lived field';}
  }
  const oldInit=r.init.bind(r);r.init=function(){oldInit();if(this.gl){this.program=this.makeProgram(LIVED_ASSETS.shader);this.backend='WebGL 2 · lived field';}else{lifeReady=false;}};
  r.livedFieldSource=LIVED_ASSETS.shader;lifeReady=true;r.dirty=true;r.lastJob=0;r.notify(r.backend);
 }catch(e){console.error(e);r.notify('Lived form unavailable: '+e.message);}finally{lifeInstalling=false;}
}
function memorySyncField(){if(!window.__idfbi||!ixEngine?.mem)return;const original=window.__idfbi,blocked=original.chain()||original.state().root!==original.world().state.root;const projectionKey=[ixEngine.mem.origin,ixEngine.mem.start,W().s.selected,W().s.head,memFormThrough].join('|');if(projectionKey!==lifeProjectionKey){lifeDescriptor=ixEngine.form(undefined,memFormThrough===null?W().s.seq:memFormThrough);lifeProjectionKey=projectionKey;}const descriptor=lifeDescriptor;
 const key=[descriptor.root,memFormOriginal,!!blocked].join('|');if(key!==lifeLast){lifeLast=key;lifeTarget=memFormOriginal||blocked?Array(8).fill(0):descriptor.traits.slice();const r=original.renderer;r.dirty=true;r.lastJob=0;}
 let badge=ix$('#life-badge');if(!badge){badge=document.createElement('button');badge.id='life-badge';badge.type='button';badge.addEventListener('click',()=>{nav('form');ix$('#instrument-dialog').showModal();});document.body.append(badge);}
 badge.textContent=blocked?'ORIGINAL HISTORY / LOCAL LIFE NOT APPLIED':memFormOriginal?'ORIGINAL FORM · COMPARE':(memFormThrough===null?'LIVED FORM':'PAST LIVED FORM')+' / '+descriptor.count+' TRACES';
 badge.title='Inspect activity, compare the original, and revisit earlier forms';badge.hidden=!!original.chain();
 memoryInstallField();
}
setInterval(()=>{if(window.__instruments){memorySyncField();if(memCheckSession())draw();}},400);

document.addEventListener('visibilitychange',()=>{if(document.hidden&&window.__instruments){memLock();if(['journal','memory-write','trade'].includes(ixView))draw();}});

})();